---
phase: 05-platforms-operations
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/17-deploy/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.17 프로덕션 배포 챕터가 mdBook에서 렌더링된다 (17-deploy/index.md, 빌드 오류 없음)"
    - "Ch.17이 최소 하나의 시나리오($5 VPS/Docker/SSH/Modal/Daytona)로 Hermes를 프로덕션 배포하는 방법을 다룬다 (PLAT-02, 성공기준 #3)"
    - "Ch.17 Docker 시나리오가 공식 이미지 nousresearch/hermes-agent + gateway run 명령 + restart unless-stopped + 포트 8642(API/health)/9119(대시보드)를 정확히 기술한다"
    - "Ch.17이 Docker 비루트 UID 10000 + s6-overlay 수퍼비전을 기술한다"
    - "Ch.17 VPS 시나리오가 hermes gateway install(사용자 systemd/launchd) + start, 그리고 sudo hermes gateway install --system(시스템 레벨)을 다루되 --system 정확 경로는 MEDIUM으로 헤지한다"
    - "Modal/Daytona 시나리오의 핵심 YAML 키(backend, container_cpu/memory/disk/persistent, modal_image)와 인증(modal setup CLI 또는 MODAL_TOKEN_ID+SECRET, DAYTONA_API_KEY)을 기술하되, Modal managed mode 흐름과 --system 경로는 `> 검증 필요`로 헤지한다"
    - "Ch.17이 두 컨테이너/프로세스가 같은 ~/.hermes를 공유하면 안 되는 함정과 docker_run_as_host_user root 소유 파일 함정을 경고한다"
    - "Ch.17의 모든 API 키/토큰 예시는 placeholder(<YOUR_DAYTONA_API_KEY> 등)이며 실제처럼 보이는 값을 쓰지 않는다"
    - "게이트웨이 실행 명령이 Docker는 gateway run, 서비스는 install→start로 정확히 구분되며 start를 포그라운드 시작으로 오기하지 않는다"
    - "모든 명령/설정 코드블록이 '# 검증: ...'(YAML/bash) 또는 '// 검증: ...'(JSON) 주석으로 시작한다"
    - "이 플랜은 src/SUMMARY.md를 수정하지 않는다"
  artifacts:
    - path: "src/17-deploy/index.md"
      provides: "Ch.17 배포 본문 (Docker + VPS systemd + SSH + Modal/Daytona + API 서버 + 함정)"
      contains: "nousresearch/hermes-agent"
      min_lines: 140
  key_links:
    - from: "src/17-deploy/index.md"
      to: "src/15-more-gateways/index.md, src/18-security/index.md"
      via: "이전/다음 챕터 인라인 링크"
      pattern: "(15-more-gateways|18-security)/index.md"
---

<objective>
Ch.17 "프로덕션 배포" 본문을 작성한다 — Hermes를 항상 켜진 상태로 프로덕션 배포하는 시나리오별 단계를 다룬다.

Purpose: 독자가 Docker, VPS+systemd, SSH 백엔드, Modal/Daytona 중 최소 하나로 게이트웨이를 영구 운영할 수 있게 한다. 공식 Docker 이미지·systemd 서비스 등록·컨테이너 백엔드 함정을 정확히 전달한다 (성공 기준 #3, PLAT-02).
Output: src/17-deploy/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-platforms-operations/05-RESEARCH.md
@.planning/phases/05-platforms-operations/05-01-SUMMARY.md

기존 파일 (참고용, 건드리지 말 것):
- src/12-cron/index.md — Phase 4 스타일/주석 관례/챕터 구조 참고.
- src/14-gateways/index.md — 선행(게이트웨이 실행 방식). 인라인 링크 참조 가능.
- src/08-backends/index.md — Phase 3 터미널 백엔드 챕터. Modal/Daytona/SSH는 Ch.8에서 백엔드로 다뤘으므로, Ch.17은 "Hermes 자체를 서버에 배포"하는 관점에서 게이트웨이 운영에 집중하고 백엔드 개념은 Ch.8 참조로 연결.

스타일 기준:
- 한국어. 구조: 개요 → 시나리오 A(Docker) → 시나리오 B(VPS systemd) → 시나리오 C(SSH) → 시나리오 D/E(Modal/Daytona) → API 서버(선택) → 흔한 오류/주의 → 다음 단계.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`(bash/YAML/ini). JSON은 `// 검증: ...`. systemd 유닛 등 커뮤니티 검증 패턴은 `# 검증: 커뮤니티 검증 패턴, 로컬 확인 권장, 2026-06-11`.
- MEDIUM/DEFERRED 항목(--system 경로, Modal managed mode)은 `> 검증 필요` callout.
- 모든 API 키/토큰 예시는 placeholder 값.

**중요:** 이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (05-01이 단독 소유).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker + VPS systemd 시나리오 작성</name>
  <files>src/17-deploy/index.md</files>
  <action>
src/17-deploy/index.md를 새로 작성한다(placeholder 덮어씀). RESEARCH.md "Ch.17: 프로덕션 배포" 시나리오 A(Docker) + B(VPS systemd) 사용. 이 Task에서 상단부터 VPS systemd 끝까지 작성하고, Task 2에서 SSH/Modal/Daytona/API서버/함정/다음 단계를 이어 붙인다.

1. 제목: `# 프로덕션 배포`
   상단 "이 챕터를 시작하기 전에" callout: 선행 [Ch.14 메시징 게이트웨이](../14-gateways/index.md)(게이트웨이 실행 방식), [Ch.8 터미널 백엔드](../08-backends/index.md)(Docker/SSH/Modal/Daytona 백엔드 개념). 이 챕터는 Hermes 자체(게이트웨이)를 서버에 항상 켜진 상태로 배포하는 방법임을 한 줄로 잇는다.

2. "## 개요" — 두 가지 검증된 배포 경로((A) 공식 Docker 이미지, (B) 네이티브 설치 + systemd/launchd 서비스)와 추가 컨테이너 백엔드 시나리오(SSH/Modal/Daytona). 최소 하나만 따라 해도 성공.

3. "## 시나리오 A: Docker 배포" (HIGH) — RESEARCH "Official Docker Image" bash 코드블록 그대로(첫 줄 `# 검증: hermes rolling + Docker, 2026-06-11`): 초기 설정(`docker run -it --rm -v ~/.hermes:/opt/data nousresearch/hermes-agent setup`), 서비스 실행(`docker run -d --name hermes --restart unless-stopped -v ~/.hermes:/opt/data -p 8642:8642 nousresearch/hermes-agent gateway run`), 로그(`docker logs -f hermes`), 업그레이드(pull → rm -f → run). 그리고 RESEARCH "Docker Compose" YAML 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-11`): image/command: gateway run/ports 8642·9119/volumes/HERMES_DASHBOARD=1/resource limits. `docker compose up -d` 포함.
   - "### Docker 기술 사항" — base debian:13.4 + s6-overlay v3(PID1 수퍼바이저, 게이트웨이 크래시 시 자동 재시작), 비루트 UID 10000(`hermes` 사용자), 포트 8642(Gateway API/health), 9119(대시보드, HERMES_DASHBOARD=1 필요). 리소스 권장 표(RAM/CPU/디스크) 포함.

4. "## 시나리오 B: VPS 네이티브 설치 + systemd" (MEDIUM) —
   - 권장 VPS 사양 표(RESEARCH). 월 ~$4–5 예시(Hetzner CX22/Hostinger)는 커뮤니티 사례로 표기.
   - 설치 + 서비스 등록 bash 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-11`): install.sh → `hermes setup --portal` → `hermes gateway install`(사용자 레벨 systemd) → `hermes gateway start` → `hermes gateway status`. 그리고 `sudo hermes gateway install --system`(시스템 레벨, 재부팅 후 자동 시작).
   - `--system`의 정확한 서비스 파일 경로는 MEDIUM이므로 다음 헤지:
     ```
     > 검증 필요: sudo hermes gateway install --system이 생성하는 정확한 서비스 파일
     > 경로(/etc/systemd/system/...)는 환경/버전에 따라 다를 수 있습니다. Ubuntu VPS에서
     > 직접 실행해 확인하십시오.
     ```
   - "### 수동 systemd 유닛 (커뮤니티 패턴)" — RESEARCH ini 유닛 파일 그대로(첫 줄 `# 검증: 커뮤니티 검증 패턴, 로컬 확인 권장, 2026-06-11`). `<your-user>` placeholder. daemon-reload → enable --now → status → journalctl -u 코드블록. macOS launchd 경로(`~/Library/LaunchAgents/ai.hermes.gateway.plist`)와 Linux user systemd 경로(`~/.config/systemd/user/hermes-gateway.service`) 명시. RESEARCH "Pitfall: macOS launchd PATH (A-15)" callout 포함.

제약:
- 게이트웨이 실행 명령 구분: Docker는 `gateway run`, 서비스는 `install` → `start`. start를 포그라운드 시작으로 쓰지 말 것.
- 모든 명령/설정 코드블록 첫 줄에 적절한 `# 검증: ...` 주석.
- RESEARCH.md에 없는 명령/플래그/포트를 만들지 말 것.
  </action>
  <verify>파일 placeholder 덮어씀: `! grep -qx "# placeholder" src/17-deploy/index.md` 통과.
Docker: `grep -q "nousresearch/hermes-agent" src/17-deploy/index.md && grep -q "gateway run" src/17-deploy/index.md && grep -q "restart unless-stopped" src/17-deploy/index.md && grep -q "8642" src/17-deploy/index.md && grep -q "9119" src/17-deploy/index.md && grep -q "10000" src/17-deploy/index.md && grep -q "s6-overlay" src/17-deploy/index.md` 통과.
VPS systemd: `grep -q "hermes gateway install" src/17-deploy/index.md && grep -q "install --system" src/17-deploy/index.md && grep -q "journalctl" src/17-deploy/index.md` 통과.
--system 헤지: `grep -q "검증 필요" src/17-deploy/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-11" src/17-deploy/index.md` 통과.</verify>
  <done>Docker(공식 이미지·gateway run·8642/9119·UID 10000·s6-overlay)와 VPS systemd(install/start, --system 헤지, 수동 유닛) 시나리오가 정확히 작성된다.</done>
</task>

<task type="auto">
  <name>Task 2: SSH/Modal/Daytona + API 서버 + 함정 + 다음 단계</name>
  <files>src/17-deploy/index.md</files>
  <action>
Task 1이 작성한 src/17-deploy/index.md 뒤에 다음을 이어 붙인다. RESEARCH.md 시나리오 C/D/E + API 서버 + 함정 사용.

5. "## 시나리오 C: SSH 백엔드" (MEDIUM) — Hermes는 로컬 실행, 터미널 명령만 원격 서버에서 실행(Hermes 자체 배포와 다름을 명시). YAML 코드블록(첫 줄 `# 검증: ...`): `terminal.backend: ssh`, `persistent_shell: true`. `.env` 코드블록: `TERMINAL_SSH_HOST`, `TERMINAL_SSH_USER`, `TERMINAL_SSH_KEY`(선택). ControlMaster 멀티플렉싱 한 줄. 백엔드 상세는 [Ch.8 터미널 백엔드](../08-backends/index.md) 참조.

6. "## 시나리오 D: Modal 백엔드" (MEDIUM) — RESEARCH YAML 코드블록(첫 줄 `# 검증: ...`): `terminal.backend: modal`, `container_cpu`, `container_memory`, `container_disk`, `container_persistent`, `modal_image`. 인증: `pip install modal && modal setup`(브라우저 OAuth → ~/.modal.toml) 또는 `MODAL_TOKEN_ID`+`MODAL_TOKEN_SECRET`(.env). modal_image 키와 managed mode는 MEDIUM/DEFERRED이므로 RESEARCH의 다음 헤지 그대로:
   ```
   > 검증 필요: modal_image 키 이름과 Modal managed mode의 정확한 설정 흐름을
   > 로컬 Modal 계정으로 확인하십시오.
   ```
   에페머럴 특성(스냅샷/복원, 유휴 비용 없음, cold start) 한 줄.

7. "## 시나리오 E: Daytona 백엔드" (MEDIUM) — RESEARCH YAML 코드블록(첫 줄 `# 검증: ...`): `terminal.backend: daytona`, 공통 container 키들. `.env`: `DAYTONA_API_KEY=<YOUR_DAYTONA_API_KEY>`. SDK가 서버 URL 자동 처리, 유휴 시 히버네이션. RESEARCH "검증 필요: Daytona 백엔드 실제 동작" 헤지 한 줄.

8. "## (선택) API 서버" (HIGH) — RESEARCH bash 코드블록(첫 줄 `# 검증: ...`): `API_SERVER_ENABLED=true`, `API_SERVER_KEY=$(openssl rand -hex 32)`, `API_SERVER_HOST=0.0.0.0`(주석으로 외부 노출 주의). 헬스 엔드포인트 `GET http://localhost:8642/health`, `/health/detailed`. Open WebUI/LobeChat/LibreChat 연결 가능 한 줄.

9. "## 흔한 오류 / 주의" — RESEARCH "Ch.17 주요 Pitfall Callouts" 세 개 callout 그대로:
   - 주의 1 (HIGH): 초기 설정은 SSH 필수, 웹 콘솔 금지(`:`,`@`,`=` 특수문자 손상 → API 키/토큰 손상).
   - 주의 2 (HIGH): 두 컨테이너/프로세스가 같은 `~/.hermes` 데이터 디렉터리 동시 사용 금지(세션/메모리 손상). 멀티 프로파일은 단일 컨테이너 `hermes profile create` 사용.
   - 주의 3 (A-13): 공식 Docker 이미지는 UID 10000으로 root 소유 파일 방지. `docker_run_as_host_user` 또는 다른 이미지 사용 시 파일 소유권 확인.

10. "## 다음 단계" — 다음 [Ch.18 보안 하드닝](../18-security/index.md)으로 연결. 이전 [Ch.15 추가 게이트웨이](../15-more-gateways/index.md).

제약:
- 모든 API 키/토큰 예시는 placeholder(`<YOUR_DAYTONA_API_KEY>` 등). 실제처럼 보이는 값 금지.
- 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`.
- RESEARCH.md에 없는 키/명령을 만들지 말 것.
  </action>
  <verify>SSH/Modal/Daytona: `grep -q "backend: ssh" src/17-deploy/index.md && grep -q "backend: modal" src/17-deploy/index.md && grep -q "backend: daytona" src/17-deploy/index.md && grep -q "DAYTONA_API_KEY" src/17-deploy/index.md && grep -q "modal setup" src/17-deploy/index.md` 통과.
Modal managed mode 헤지: `grep -q "modal_image" src/17-deploy/index.md && grep -q "검증 필요" src/17-deploy/index.md` 통과.
API 서버: `grep -q "API_SERVER_ENABLED" src/17-deploy/index.md && grep -q "/health" src/17-deploy/index.md` 통과.
함정: `grep -q "docker_run_as_host_user" src/17-deploy/index.md && grep -q "웹 콘솔" src/17-deploy/index.md` 통과.
placeholder 키: `grep -q "<YOUR_DAYTONA_API_KEY>" src/17-deploy/index.md` 통과.
다음/이전 챕터 링크: `grep -q "18-security/index.md" src/17-deploy/index.md && grep -q "15-more-gateways/index.md" src/17-deploy/index.md` 통과.
SUMMARY 미수정: `git diff --name-only src/SUMMARY.md` 출력이 비어 있음.
빌드: `mdbook build` exit 0, 출력에 "error" 없음.</verify>
  <done>SSH/Modal/Daytona 백엔드(헤지 포함)·API 서버·세 함정·다음 단계 링크가 추가되고, SUMMARY.md는 수정되지 않으며, mdbook build가 exit 0이다.</done>
</task>

</tasks>

<verification>
- src/17-deploy/index.md가 Docker(nousresearch/hermes-agent·gateway run·8642/9119·UID 10000·s6-overlay) + VPS systemd(install/start, --system) + SSH + Modal/Daytona + API 서버를 담는다.
- --system 경로와 Modal managed mode/modal_image는 검증 필요로 헤지된다.
- 두 컨테이너 ~/.hermes 공유 함정 + docker_run_as_host_user 함정이 존재한다.
- 모든 API 키/토큰은 placeholder. 모든 코드블록에 적절한 `# 검증: ...`/`// 검증: ...` 주석.
- 이 플랜은 src/SUMMARY.md를 수정하지 않는다 (`git diff --name-only src/SUMMARY.md` 비어 있음).
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.17을 읽고 Docker/VPS systemd/SSH/Modal/Daytona 중 최소 하나로 Hermes 게이트웨이를 프로덕션에 항상 켜진 상태로 배포할 수 있다 (성공 기준 #3, PLAT-02).
</success_criteria>

<output>
After completion, create `.planning/phases/05-platforms-operations/05-03-SUMMARY.md`
</output>
