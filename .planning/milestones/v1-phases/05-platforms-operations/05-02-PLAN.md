---
phase: 05-platforms-operations
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/15-more-gateways/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.15 추가 게이트웨이 챕터가 mdBook에서 렌더링된다 (15-more-gateways/index.md, 빌드 오류 없음)"
    - "Ch.15가 Discord/Slack/WhatsApp/Signal/Email 중 최소 하나를 추가 설정하는 방법을 다룬다 (PLAT-01, 성공기준 #2)"
    - "Ch.15가 Discord에 TWO Privileged Gateway Intents = Server Members Intent + Message Content Intent가 필요함을 정확히 기술하고, Presence Intent는 불필요함을 명시한다"
    - "Ch.15가 Message Content Intent 미활성화 시 봇이 이벤트는 받지만 메시지 텍스트가 비어 #1 무반응 원인이 됨을 함정 callout으로 경고한다"
    - "Ch.15가 Discord 봇 토큰을 DISCORD_BOT_TOKEN으로, Slack을 SLACK_BOT_TOKEN(xoxb)+SLACK_APP_TOKEN(xapp) Socket Mode로 정확히 기술한다"
    - "WhatsApp/Signal/Email은 개요 수준으로 다루며 MEDIUM 신뢰 항목(WhatsApp 비공식 프로토콜, Signal signal-cli 의존)을 명시한다"
    - "Ch.15의 모든 토큰 예시는 placeholder(<YOUR_DISCORD_BOT_TOKEN> 등)이며 실제처럼 보이는 토큰을 쓰지 않는다"
    - "게이트웨이 시작 명령이 hermes gateway(포그라운드)/hermes gateway run으로 기술되며 start를 포그라운드 시작 명령으로 잘못 쓰지 않는다"
    - "검증되지 않은 출력(gateway setup 마법사, Slack manifest 내용 등)은 [로컬 실행 후 캡처 필요] + `> 검증 필요`로 표시된다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-11' 주석으로 시작한다"
    - "이 플랜은 src/SUMMARY.md를 수정하지 않는다"
  artifacts:
    - path: "src/15-more-gateways/index.md"
      provides: "Ch.15 추가 게이트웨이 본문 (Discord 2-Intents + Slack Socket Mode + WhatsApp/Signal/Email 개요 + DM 페어링)"
      contains: "Message Content Intent"
      min_lines: 140
  key_links:
    - from: "src/15-more-gateways/index.md"
      to: "src/14-gateways/index.md, src/17-deploy/index.md"
      via: "이전/다음 챕터 인라인 링크"
      pattern: "(14-gateways|17-deploy)/index.md"
---

<objective>
Ch.15 "추가 게이트웨이" 본문을 단일 파일로 작성한다 — Discord/Slack/WhatsApp/Signal/Email을 추가로 설정하는 방법을 다룬다. (Ch.15–16을 합친 단일 파일이며 16-* 파일은 만들지 않는다.)

Purpose: 독자가 Ch.14의 Telegram에 이어 추가 플랫폼 중 최소 하나를 설정할 수 있게 한다. 특히 Discord의 두 Privileged Gateway Intents(Server Members + Message Content) 함정을 정확히 전달해 #1 무반응 원인을 예방한다 (성공 기준 #2, PLAT-01).
Output: src/15-more-gateways/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-platforms-operations/05-RESEARCH.md
@.planning/phases/05-platforms-operations/05-01-SUMMARY.md

기존 파일 (참고용, 건드리지 말 것):
- src/12-cron/index.md — Phase 4 스타일/주석 관례/챕터 구조 참고.
- src/14-gateways/index.md — 선행(게이트웨이 아키텍처·실행 방식·DM 페어링). 인라인 링크 참조 가능. 게이트웨이 실행 방식(hermes gateway/run/start install)을 Ch.14가 이미 설명했으므로 Ch.15는 플랫폼별 자격증명·.env·동작에 집중하고 게이트웨이 실행은 간략 참조.

스타일 기준:
- 한국어. 구조: 개요 → (플랫폼별) 개념·실습 → 공통 게이트웨이 설정/DM 페어링 → 흔한 오류/주의 → 다음 단계.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`. JSON은 `// 검증: ...`.
- 검증 안 된 출력/마법사 UI는 `[로컬 실행 후 캡처 필요]` placeholder + `> 검증 필요` callout.
- 모든 토큰/키 예시는 placeholder 값 — 실제처럼 보이는 토큰 금지.

**중요:** 이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (05-01이 단독 소유). 16-* 디렉터리/파일도 만들지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.15 Discord + Slack 본문 작성</name>
  <files>src/15-more-gateways/index.md</files>
  <action>
src/15-more-gateways/index.md를 새로 작성한다(현재 placeholder 덮어씀). RESEARCH.md "Ch.15: 추가 게이트웨이"의 Discord + Slack 섹션 HIGH confidence 사실만 사용. 이 Task에서 챕터 상단부터 Slack 끝까지 작성하고, Task 2에서 WhatsApp/Signal/Email + 공통 설정 + 함정 + 다음 단계를 이어 붙인다.

작성할 내용:

1. 제목: `# 추가 게이트웨이`
   상단 "이 챕터를 시작하기 전에" callout: 선행 [Ch.14 메시징 게이트웨이: Telegram](../14-gateways/index.md)(게이트웨이 아키텍처·실행 방식·DM 페어링). 단일 게이트웨이 프로세스가 .env에 설정된 모든 플랫폼을 동시에 처리한다는 점을 한 줄로 잇는다.

2. "## 개요" — 다섯 가지 추가 게이트웨이(Discord/Slack/WhatsApp/Signal/Email)는 동일 패턴: 자격증명 생성 → `.env` 변수 설정 → `hermes gateway setup`(또는 .env 직접 편집) → `hermes gateway`(포그라운드) 또는 install/start. 최소 하나만 설정해도 성공.

3. "## Discord" 섹션:
   - "### Privileged Gateway Intents (필수 2개)" — RESEARCH 표 그대로: **Server Members Intent**(서버 멤버 조회) + **Message Content Intent**(메시지 텍스트 읽기) 두 가지를 Developer Portal Bot 탭에서 반드시 활성화. **Presence Intent는 Hermes에 불필요**. 2026년 현재 <100서버 봇은 수동 심사 없이 즉시 활성화.
   - "### Discord 앱 생성" — RESEARCH의 7단계 번호 목록(New Application → Bot 탭 → Reset Token → 두 Intent 활성화 → Save Changes). 토큰은 한 번만 표시됨을 명시.
   - "### 봇 서버 초대" — Installation 탭 권장 흐름 + 수동 OAuth URL. OAuth scope `bot+applications.commands`, permission 정수 `274878286912`(= View Channels + Send Messages + Read Message History + Attach Files + Embed Links + Send in Threads + Add Reactions). client_id는 `<YOUR_APP_ID>` placeholder.
   - "### Hermes Discord 설정" — bash 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-11`): `DISCORD_BOT_TOKEN=<YOUR_DISCORD_BOT_TOKEN>`, `DISCORD_ALLOWED_USERS`(개발자 모드 → 우클릭 → ID 복사). 설정 후 `hermes gateway setup`(Discord 선택) 또는 .env 직접 편집 → `hermes gateway` 포그라운드 테스트.
   - "### 기대 동작" — DM(멘션 없이 응답), 서버 채널(@멘션 시만), 스레드(추가 멘션 불필요).

4. "## Slack" 섹션:
   - "### Slack App 생성 (Manifest 권장)" — `hermes slack manifest --write` 코드블록(첫 줄 `# 검증: ...`)으로 `~/.hermes/slack-manifest.json` 생성. api.slack.com/apps → From an app manifest → JSON 붙여넣기. 생성된 manifest 내용은 검증되지 않았으므로 다음 헤지를 넣는다:
     ```
     > 검증 필요: hermes slack manifest --write가 생성하는 JSON의 정확한 내용은
     > 버전에 따라 다를 수 있습니다. 로컬 실행 후 확인하십시오.
     ```
   - "### Socket Mode 활성화 (필수)" — Settings → Socket Mode → Enable. App-Level Token(scope `connections:write`, `xapp-`로 시작) 생성. Hermes는 Socket Mode만 사용(WebSocket, 공개 HTTP 엔드포인트 불필요, 방화벽/NAT 뒤 동작). Classic RTM은 2025-03 deprecated.
   - "### OAuth Scopes" — RESEARCH의 필수 스코프 목록 그대로. `channels:history`/`groups:history` 없이는 채널 메시지를 못 받고 DM 전용이 됨을 주의로 명시.
   - "### Bot Token 발급 + Hermes 설정" — Install App → `xoxb-`로 시작하는 Bot User OAuth Token. bash 코드블록(첫 줄 `# 검증: ...`): `SLACK_BOT_TOKEN=xoxb-<YOUR_BOT_TOKEN>`, `SLACK_APP_TOKEN=xapp-<YOUR_APP_TOKEN>`, `SLACK_ALLOWED_USERS`(멤버 ID 복사).
   - "### 기대 동작" — DM(멘션 없이), 채널(@멘션 시 스레드 답글), 스레드(추가 멘션 불필요).

제약:
- 모든 토큰 예시는 placeholder(`<YOUR_DISCORD_BOT_TOKEN>`, `xoxb-<YOUR_BOT_TOKEN>`, `xapp-<YOUR_APP_TOKEN>`, `<YOUR_APP_ID>`). 실제처럼 보이는 토큰 금지.
- 모든 실제 실행 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`.
- `hermes gateway start`를 포그라운드 시작으로 쓰지 말 것(포그라운드는 `hermes gateway`/`hermes gateway run`).
  </action>
  <verify>파일 존재 및 placeholder 덮어씀: `! grep -qx "# placeholder" src/15-more-gateways/index.md` 통과.
Discord 2-Intents: `grep -q "Server Members Intent" src/15-more-gateways/index.md && grep -q "Message Content Intent" src/15-more-gateways/index.md && grep -q "Presence Intent" src/15-more-gateways/index.md` 통과.
Discord env + permission: `grep -q "DISCORD_BOT_TOKEN" src/15-more-gateways/index.md && grep -q "274878286912" src/15-more-gateways/index.md` 통과.
Slack: `grep -q "Socket Mode" src/15-more-gateways/index.md && grep -q "xoxb-" src/15-more-gateways/index.md && grep -q "xapp-" src/15-more-gateways/index.md && grep -q "SLACK_APP_TOKEN" src/15-more-gateways/index.md` 통과.
placeholder 토큰: `grep -q "<YOUR_DISCORD_BOT_TOKEN>" src/15-more-gateways/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-11" src/15-more-gateways/index.md` 통과.</verify>
  <done>Discord 섹션이 두 Intent(Server Members + Message Content, Presence 불필요)·앱 생성·초대(permission 274878286912)·DISCORD_BOT_TOKEN을, Slack 섹션이 Socket Mode·xoxb/xapp 토큰을 placeholder로 정확히 담는다.</done>
</task>

<task type="auto">
  <name>Task 2: WhatsApp/Signal/Email 개요 + 공통 설정 + 함정 + 다음 단계</name>
  <files>src/15-more-gateways/index.md</files>
  <action>
Task 1이 작성한 src/15-more-gateways/index.md의 Slack 섹션 뒤에 다음을 이어 붙인다. RESEARCH.md의 WhatsApp/Signal/Email + 공통 게이트웨이 설정 + 함정 섹션 사용.

5. "## WhatsApp (개요)" — MEDIUM 신뢰. `hermes whatsapp` 마법사 → QR 코드 터미널 출력 → 폰 WhatsApp 연결된 기기로 스캔. `.env`: `WHATSAPP_ENABLED=true`, `WHATSAPP_MODE=bot`, `WHATSAPP_ALLOWED_USERS`(국가코드 포함, + 없이). 코드블록 첫 줄 `# 검증: ...`. RESEARCH "주의: 공식 WhatsApp Business API 아님" callout 그대로(WhatsApp Web 프로토콜, 전용 번호 권장, 스팸 금지).

6. "## Signal (개요)" — MEDIUM 신뢰. 외부 의존성 signal-cli(Java 17+) 필요. `signal-cli link` → 연결된 기기 스캔 → `signal-cli daemon --http`. `.env`: `SIGNAL_HTTP_URL`, `SIGNAL_ACCOUNT`, `SIGNAL_ALLOWED_USERS`. 코드블록 첫 줄 `# 검증: ...`. 알려진 제한(메시지 편집 미지원 → 툴 진행 버블 없음, 첨부 100MB/이미지 32개)을 간단히.

7. "## Email (개요)" — HIGH 신뢰. IMAP/SMTP `.env` 코드블록(첫 줄 `# 검증: ...`): `EMAIL_ADDRESS`(전용 계정), `EMAIL_PASSWORD`(Gmail 2FA 시 앱 비밀번호), `EMAIL_IMAP_HOST`/`EMAIL_SMTP_HOST`(Gmail/Outlook 예), `EMAIL_ALLOWED_USERS`. 15초 IMAP 폴링으로 UNSEEN 감지, In-Reply-To로 스레드 유지. 제한(HTML→텍스트만, 개인 이메일 금지, 15초 지연).

8. "## 공통: 게이트웨이 시작과 DM 페어링" —
   - 설정 완료 후 `hermes gateway setup`(미설정 플랫폼 추가) → `hermes gateway`(포그라운드, 모든 플랫폼 동시) 코드블록(첫 줄 `# 검증: ...`). 단일 프로세스가 .env의 모든 플랫폼을 동시에 처리함을 한 줄.
   - DM 페어링: `hermes pairing list/approve/revoke` 코드블록(첫 줄 `# 검증: ...`). 알 수 없는 사용자가 DM 시 8자리 일회성 코드 발급 → 운영자 승인, 1시간 후 만료.
   - gateway setup 마법사 정확 UI는 검증 안 됨 → `> 검증 필요` 헤지 한 줄(이미 14장에서 다뤘으면 간단 참조 가능하나 여기서도 한 줄 헤지 유지).

9. "## 흔한 오류 / 주의" — RESEARCH "Pitfall: Message Content Intent (A-10, CRITICAL)" callout을 그대로 핵심 함정으로 배치:
   ```
   > 경고 (Discord #1 문제): 봇이 온라인인데 메시지를 완전히 무시한다면
   > Message Content Intent가 비활성화된 것입니다.
   > 증상: Developer Portal에서 "온라인"으로 보이지만 모든 메시지를 무시.
   > 해결: Bot 탭 → Privileged Gateway Intents → Message Content Intent 활성화 → Save Changes
   >       → hermes gateway stop && hermes gateway start (재시작 필수)
   ```
   추가로 Slack `channels:history`/`groups:history` 누락 시 채널 메시지 미수신(DM 전용) 주의, WhatsApp 비공식 프로토콜 주의를 짧게 재확인.

10. "## 다음 단계" — 다음 [Ch.17 프로덕션 배포](../17-deploy/index.md)로 연결. 이전 [Ch.14 메시징 게이트웨이: Telegram](../14-gateways/index.md).

제약:
- WhatsApp/Signal은 MEDIUM 신뢰임을 명시(개요 수준, 깊은 보장 금지).
- 모든 토큰/비밀 예시는 placeholder. 모든 실제 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`.
- RESEARCH.md에 없는 env 변수명/명령을 만들지 말 것.
  </action>
  <verify>WhatsApp/Signal/Email: `grep -q "WHATSAPP_ENABLED" src/15-more-gateways/index.md && grep -q "signal-cli" src/15-more-gateways/index.md && grep -q "EMAIL_IMAP_HOST" src/15-more-gateways/index.md` 통과.
공통 설정 + 페어링: `grep -q "hermes pairing" src/15-more-gateways/index.md && grep -q "hermes gateway setup" src/15-more-gateways/index.md` 통과.
Message Content Intent 함정 callout: `grep -q "Message Content Intent" src/15-more-gateways/index.md && grep -q "재시작" src/15-more-gateways/index.md` 통과.
다음/이전 챕터 링크: `grep -q "17-deploy/index.md" src/15-more-gateways/index.md && grep -q "14-gateways/index.md" src/15-more-gateways/index.md` 통과.
SUMMARY 미수정: `git diff --name-only src/SUMMARY.md` 출력이 비어 있음.
빌드: `mdbook build` exit 0, 출력에 "error" 없음.</verify>
  <done>WhatsApp/Signal/Email 개요·공통 게이트웨이 설정·DM 페어링·Message Content Intent 함정·다음 단계 링크가 추가되고, SUMMARY.md는 수정되지 않으며, mdbook build가 exit 0이다.</done>
</task>

</tasks>

<verification>
- src/15-more-gateways/index.md가 Discord 2-Intents(Server Members + Message Content, Presence 불필요)·Slack Socket Mode·WhatsApp/Signal/Email 개요·DM 페어링을 담는다.
- Message Content Intent 미활성화 함정이 #1 무반응 원인 callout으로 존재한다.
- 모든 토큰은 placeholder. 모든 명령/설정 코드블록에 `# 검증: hermes rolling, 2026-06-11`.
- 검증 안 된 출력(Slack manifest, gateway setup)은 검증 필요 헤지.
- 이 플랜은 src/SUMMARY.md를 수정하지 않는다 (`git diff --name-only src/SUMMARY.md` 비어 있음).
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.15를 읽고 Discord/Slack/WhatsApp/Signal/Email 중 최소 하나를 추가 설정할 수 있으며, 특히 Discord 두 Intent를 정확히 활성화해 #1 무반응 함정을 피한다 (성공 기준 #2, PLAT-01).
</success_criteria>

<output>
After completion, create `.planning/phases/05-platforms-operations/05-02-SUMMARY.md`
</output>
