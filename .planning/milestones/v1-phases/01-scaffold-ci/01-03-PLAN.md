---
phase: 01-scaffold-ci
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - book.toml
autonomous: false

must_haves:
  truths:
    - "Local `mdbook build` succeeds end-to-end with both the scaffold and workflow in place (the full repo state that will be pushed)"
    - "book.toml's site-url and git-repository-url match the actual GitHub owner/repo once the remote exists"
    - "A `git push` to main triggers the GitHub Actions workflow and auto-deploys to GitHub Pages"
    - "The deployed site is reachable at https://<owner>.github.io/<repo>/ with the Korean SUMMARY.md table of contents rendering"
  artifacts:
    - path: "book.toml"
      provides: "site-url/git-repository-url reconciled to the real owner/repo"
      contains: "site-url"
  key_links:
    - from: "git push to main"
      to: "GitHub Actions workflow run"
      via: "on.push.branches=[main] trigger"
      pattern: "main"
    - from: "GitHub Pages source = GitHub Actions"
      to: "github-pages environment"
      via: "deploy-pages step"
      pattern: "github-pages"
---

<objective>
Final verification of the Phase 1 pipeline. Two halves with a hard autonomy boundary:
(A) Autonomous: confirm the complete local repo state builds with `mdbook build` (scaffold + workflow present).
(B) Human-action: create the GitHub remote, reconcile book.toml site-url/git-repository-url to the real owner/repo, enable Pages Source = "GitHub Actions" BEFORE pushing, push main, then verify the live deployed site.

Purpose: This is the only point where the pipeline is proven end-to-end against the real public URL. Steps in (B) require a GitHub account and cannot be performed by the autonomous executor (no remote exists yet, and clicking repo Settings has no CLI-only equivalent for the autonomous agent here).
Output: A verified local build, reconciled book.toml, and a live site at https://<owner>.github.io/<repo>/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-ci/01-RESEARCH.md
@.planning/phases/01-scaffold-ci/01-01-SUMMARY.md
@.planning/phases/01-scaffold-ci/01-02-SUMMARY.md

Satisfies Phase Success Criteria 2 (push triggers auto-deploy) and 3 (deployed site reachable with Korean TOC). Closes INFRA-02's site-url reconciliation.

CRITICAL sequencing constraint from research (Pitfall 3) — the order is NON-NEGOTIABLE:
  1. Create the GitHub remote repo.
  2. Reconcile book.toml (site-url + git-repository-url + edit-url-template) to the real owner/repo, commit.
  3. Enable Pages → Source = "GitHub Actions" in repo Settings.
  4. THEN push main.
If you push before enabling Pages source, the deploy-pages step fails because the github-pages environment doesn't exist yet.

AUTONOMY BOUNDARY: Task 1 is autonomous (local build). Tasks 2-3 are a human-action checkpoint — the executor MUST NOT claim deployment succeeded based on the local build alone. Live deploy is only verifiable after the human completes the GitHub steps.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autonomous final local build check</name>
  <files>book.toml, src/SUMMARY.md, .github/workflows/deploy.yml</files>
  <action>
With both 01-01 (scaffold) and 01-02 (workflow) in place, run the full local build to confirm the exact repo state that will be pushed is sound.

Run `mdbook build` (use the mdBook 0.5.3 binary installed in 01-01). Confirm exit 0, no "unknown field"/"error" output, and `book/index.html` exists. Also confirm the placeholder chapter rendered: `book/00-intro/index.html` should exist.

This is the autonomously-verifiable portion of Phase Success Criterion 1, re-confirmed against the final pre-push state. It does NOT prove live deployment — that requires the human steps in Task 2.
  </action>
  <verify>`mdbook build` exits 0; `mdbook build 2>&1 | grep -iE "unknown|error"` returns nothing; `test -f book/index.html && test -f book/00-intro/index.html` succeeds</verify>
  <done>The complete repo (scaffold + workflow) builds cleanly locally and produces the expected pages including the placeholder chapter.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
A buildable mdBook 0.5.3 project (book.toml, src/, SUMMARY.md, placeholder chapter) and a GitHub Actions workflow (.github/workflows/deploy.yml) that builds with mdBook 0.5.3 and deploys to GitHub Pages via the official artifact flow. Local `mdbook build` is verified green.

The following steps require a GitHub account and cannot be automated by the executor: there is no remote yet, repo Settings → Pages has no autonomous-CLI path here, and the push targets a remote the executor cannot authenticate to.
  </what-built>
  <how-to-verify>
Perform these IN ORDER (the order matters — see Pitfall 3):

1. Create the GitHub remote repository (any name; the dir is `hermes-tutorial`). Either:
   - `gh repo create <owner>/<repo> --public --source=. --remote=origin` (if you have the gh CLI authenticated), OR
   - create it via the GitHub web UI and then `git remote add origin https://github.com/<owner>/<repo>.git`.
   Note the actual `<owner>` and `<repo>` you used.

2. Reconcile book.toml to the real owner/repo, then commit:
   - Set `site-url = "/<repo>/"` (leading and trailing slash; e.g. `/hermes-tutorial/`).
   - Set `git-repository-url = "https://github.com/<owner>/<repo>"`.
   - Set `edit-url-template = "https://github.com/<owner>/<repo>/edit/main/{path}"`.
   - `git add book.toml && git commit -m "chore(01): reconcile book.toml site-url to <owner>/<repo>"`.
   (If the repo name is exactly `hermes-tutorial`, site-url is already correct; you still must fix the `<owner>` markers in the two URLs.)

3. In the GitHub repo: Settings → Pages → "Build and deployment" → Source → select "GitHub Actions". Do this BEFORE pushing.

4. Push main: `git push -u origin main`.

5. Watch the workflow: GitHub → Actions tab → the "Deploy mdBook to GitHub Pages" run should go green (both build and deploy jobs). With gh: `gh run watch` or `gh run list`.

6. Open the live site: `https://<owner>.github.io/<repo>/` — confirm the Korean intro page renders and the left sidebar shows the SUMMARY.md table of contents ("소개", "소개 및 배경"). Then open `https://<owner>.github.io/<repo>/404.html` — if CSS/styling loads correctly there, site-url is right (per research Pitfall 2 verification).

If the deploy job fails with a missing `github-pages` environment, you pushed before step 3 — enable Pages source, then re-run the workflow (Actions → re-run jobs, or push an empty commit).
  </how-to-verify>
  <resume-signal>Reply "deployed" with the live URL once the site renders, or paste the workflow error if it failed.</resume-signal>
</task>

</tasks>

<verification>
Autonomous (Task 1):
- `mdbook build` exits 0, no errors, book/index.html and book/00-intro/index.html exist.

Human-verified (Task 2 — NOT autonomously verifiable):
- Workflow run is green on push to main.
- https://<owner>.github.io/<repo>/ renders the Korean intro + SUMMARY TOC.
- book.toml site-url/git-repository-url/edit-url-template reflect the real owner/repo.
</verification>

<success_criteria>
- Phase Success Criterion 1: local `mdbook build` produces book/ (autonomous, re-confirmed here).
- Phase Success Criterion 2: git push triggers Actions and auto-deploys (human-verified).
- Phase Success Criterion 3: deployed site reachable at the Pages URL with Korean TOC (human-verified).
- INFRA-02 fully closed: site-url matches the real repo, not the placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-ci/01-03-SUMMARY.md`. Record the actual owner/repo and live URL so later phases can reference them.
</output>
