---
phase: 03-core-concepts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SUMMARY.md
  - src/04-agent-loop/index.md
autonomous: true

must_haves:
  truths:
    - "SUMMARY.md에 '# 핵심 개념' 섹션과 5개 챕터 항목(04-agent-loop/05-context-files/06-memory/07-tools/08-backends)이 추가되며 기존 기초 4개 항목은 보존된다"
    - "SUMMARY.md에 중복 경로가 없다 (mdBook 0.5.3 하드 빌드 오류 방지)"
    - "Ch.4 에이전트 루프 챕터가 mdBook에서 렌더링된다 (04-agent-loop/index.md, 빌드 오류 없음)"
    - "Ch.4가 공식 루프 사이클 'Assemble → Call → Parse → Execute → Persist → Loop'를 설명한다 (perceive→decide→act 프레이밍 사용 금지)"
    - "Ch.4가 3티어 프롬프트 시스템(Stable→Context→Volatile)을 언급하고 Ch.5로 연결한다"
    - "Ch.4가 동기 엔진·툴 디스패치(단일 직접/복수 ThreadPoolExecutor ≤8/인터랙티브 순차)·max_turns 90 종료 조건을 설명한다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-09' 주석으로 시작한다"
  artifacts:
    - path: "src/SUMMARY.md"
      provides: "기초 4개 + 핵심 개념 5개 챕터 목차 (총 9개)"
      contains: "08-backends/index.md"
    - path: "src/04-agent-loop/index.md"
      provides: "Ch.4 에이전트 루프 본문 (공식 사이클 + 3티어 + 툴 디스패치 + max_turns)"
      contains: "Assemble"
      min_lines: 80
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/04-agent-loop/index.md, src/05-context-files/index.md, src/06-memory/index.md, src/07-tools/index.md, src/08-backends/index.md"
      via: "mdBook 핵심 개념 섹션 목차 링크"
      pattern: "0[4-8]-.*index.md"
---

<objective>
Phase 3 전체의 SUMMARY.md 스켈레톤("# 핵심 개념" 섹션 + 5개 챕터 항목)을 한 번에 확정하고, Ch.4 "에이전트 루프" 본문을 작성한다.

Purpose: 이 플랜이 SUMMARY.md를 단독 소유함으로써 후속 4개 챕터 플랜(03-02~03-05)이 SUMMARY.md 쓰기 충돌 없이 Wave 2에서 병렬 실행되도록 한다. 동시에 독자가 AIAgent 코어 루프와 3티어 프롬프트 시스템의 동작 원리를 이해하게 한다 (성공 기준 #1 / CORE-01).
Output: src/SUMMARY.md (9-챕터 목차), src/04-agent-loop/index.md (Ch.4 본문).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-concepts/03-RESEARCH.md

기존 파일 (반드시 통합, 빌드 깨뜨리지 말 것):
- src/SUMMARY.md — 현재 기초 4개 항목만 존재. "# 핵심 개념" 섹션과 5개 새 항목을 ADD (기존 4개 항목 텍스트는 절대 변경/삭제 금지, 새 경로만 추가). RESEARCH.md "Updated SUMMARY.md entries" 그대로.
- src/01-install/index.md — Phase 2의 완성된 챕터. 스타일/주석 관례 참고용. 건드리지 말 것.

스타일 기준 (Phase 2 챕터와 동일):
- 한국어 작성. RESEARCH.md "Chapter file template" 구조: 개요 → 개념 → 실습 → 심화 → 흔한 오류/주의 → 다음 단계.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄은 `# 검증: hermes rolling, 2026-06-09`.
- 검증 안 된 출력은 `# [로컬 실행 후 캡처 필요 — ...]` placeholder + `> 검증 필요` callout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SUMMARY.md에 핵심 개념 섹션 + 5개 챕터 추가</name>
  <files>src/SUMMARY.md</files>
  <action>
src/SUMMARY.md 끝에 새 섹션 구분자(`---`)와 `# 핵심 개념` 헤딩, 그리고 5개 챕터 항목을 ADD한다. 기존 "# 기초" 섹션의 4개 항목은 그대로 보존한다. RESEARCH.md "After Phase 3, add the # 핵심 개념 section" 블록 그대로 사용.

최종 SUMMARY.md 전체는 정확히 다음과 같아야 한다:

```markdown
# Summary

[소개](README.md)

---

# 기초

- [소개: Hermes란 무엇인가](00-intro/index.md)
- [설치](01-install/index.md)
- [첫 실행](02-first-run/index.md)
- [모델 설정](03-model/index.md)

---

# 핵심 개념

- [에이전트 루프](04-agent-loop/index.md)
- [컨텍스트 파일](05-context-files/index.md)
- [메모리](06-memory/index.md)
- [툴 게이트웨이](07-tools/index.md)
- [터미널 백엔드](08-backends/index.md)
```

주의 (RESEARCH.md Pitfall B-11): 기존 4개 경로(00-intro/01-install/02-first-run/03-model)를 다시 추가하지 말 것 — 중복 경로는 mdBook 0.5.3 하드 빌드 오류. 5개 새 경로(04~08)는 전부 신규이므로 안전하다. 후속 플랜(03-02~03-05)이 05~08 챕터 본문을 작성하므로, 이 플랜에서는 04-agent-loop만 본문을 만든다. mdBook 0.5.3는 SUMMARY에 링크됐으나 존재하지 않는 파일(05~08)에 대해 빈 파일을 자동 생성하므로 빌드는 통과한다.
  </action>
  <verify>5개 새 경로가 각 1회씩 등장하고 총 9개 index.md 링크, 중복 없음을 확인:
`grep -c "index.md" src/SUMMARY.md` 출력이 9.
`grep -q "04-agent-loop/index.md" src/SUMMARY.md && grep -q "05-context-files/index.md" src/SUMMARY.md && grep -q "06-memory/index.md" src/SUMMARY.md && grep -q "07-tools/index.md" src/SUMMARY.md && grep -q "08-backends/index.md" src/SUMMARY.md` 통과.
`grep -q "핵심 개념" src/SUMMARY.md` 통과.
중복 경로 없음: `grep -o "(.*index.md)" src/SUMMARY.md | sort | uniq -d` 출력이 비어 있음.</verify>
  <done>SUMMARY.md가 기초 4개 + 핵심 개념 5개 = 9개 챕터 링크를 가지며 중복 경로가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.4 에이전트 루프 본문 작성</name>
  <files>src/04-agent-loop/index.md</files>
  <action>
src/04-agent-loop/index.md를 새로 작성(또는 mdBook 자동 생성된 빈 파일을 overwrite)한다. RESEARCH.md "Ch.4: 에이전트 루프" 섹션의 HIGH confidence 사실만 사용. 한국어. Phase 2 챕터 구조(개요 → 개념 → 실습 → 심화 → 흔한 오류/주의 → 다음 단계)를 따른다.

포함해야 할 내용 (모두 RESEARCH.md 근거):

1. 제목: `# 에이전트 루프`
   상단에 "이 챕터를 시작하기 전에" callout (선행: Ch.2 첫 실행을 완료해 hermes 대화 경험이 있을 것). 이 챕터는 개념 위주이며 새 설치 명령이 없음을 명시.

2. "## 개요" — AIAgent가 모든 진입점(CLI/Gateway/ACP/API)이 공유하는 플랫폼 무관 오케스트레이션 엔진임. 이 챕터에서 루프의 작동 원리와 3티어 프롬프트 조립을 이해한다.

3. "## 개념: AIAgent 코어와 공식 루프 사이클"
   - 공식 사이클 이름: **Assemble → Call → Parse → Execute → Persist → Loop** (RESEARCH.md "The Official Cycle Name"). 절대 "perceive→decide→act" 같은 비공식 프레이밍을 쓰지 말 것 — 공식 문서에 없으며 독자를 혼동시킨다.
   - RESEARCH.md "Exact Agent Loop Steps"의 9단계 흐름을 코드블록(텍스트)으로 그대로 제시(task_id 생성 → user 메시지 추가 → 시스템 프롬프트 빌드/캐시 → 50% 초과 시 선제 압축 → API 메시지 구성 → 에페머럴 레이어 주입 → 캐싱 마커(Anthropic) → 인터럽트 가능 API 호출 → 응답 파싱: tool_calls면 실행 후 step 5로, 텍스트면 세션 저장+메모리 플러시 후 종료).
   - RESEARCH.md "ASCII Diagram for Ch.4"를 그대로 삽입.

4. "## 개념: 3티어 프롬프트 시스템 (요약)"
   - RESEARCH.md "The 3-Tier Prompt Architecture" 표(Stable/Context/Volatile + 캐시 안정성)를 간략히 제시. 최종 조립 순서 `stable → context → volatile`만 명시.
   - 10-레이어 상세와 컨텍스트 파일 우선순위는 Ch.5에서 다룬다고 plain prose로 연결(인라인 링크 `[Ch.5 컨텍스트 파일](../05-context-files/index.md)` 사용 가능 — 05는 SUMMARY에 링크되어 빌드 시 존재함).

5. "## 개념: 세 가지 API 모드" — RESEARCH.md "Three API Modes" 표(chat_completions / codex_responses / anthropic_messages + 클라이언트 라이브러리). 모두 내부 OpenAI 스타일 메시지 포맷으로 수렴함을 한 줄.

6. "## 개념: 툴 디스패치" — RESEARCH.md "Tool Dispatch in the Loop":
   - 단일 툴 호출 = 직접(동기) 실행
   - 복수 툴 호출 = ThreadPoolExecutor (최대 8 워커) 동시 실행
   - 인터랙티브 툴(`clarify`, `todo`, `memory`, `session_search`, `delegate_task`)은 항상 순차 실행
   - 툴당 실행 흐름 6단계(핸들러 해석 → pre_tool_call 훅 → 위험 시 승인 게이트 → 실행 → post_tool_call 훅 → 결과 추가)를 간단히.

7. "## 실습: 루프를 직접 관찰하기" — RESEARCH.md "Exact Commands for Ch.4":
   - 코드블록 (첫 줄 `# 검증: hermes rolling, 2026-06-09`): `hermes chat -q "현재 디렉터리의 파일을 나열하고 가장 큰 파일을 알려줘"` — terminal 툴 호출 → 결과 추가 → 텍스트 응답 흐름을 관찰.
   - 코드블록 (첫 줄 `# 검증: hermes rolling, 2026-06-09`): `hermes config set agent.max_turns 50` — 반복 횟수 제한 설정(기본 90).
   - 툴 호출 진행 출력은 LOW confidence이므로 placeholder + 검증 필요 callout:
     코드블록 안에 `# [로컬 실행 후 캡처 필요 — 툴 호출 진행 출력]` 그리고 그 아래 `> 검증 필요:` callout으로 "이 출력은 display.tool_progress 설정과 터미널 스킨에 따라 다르므로 로컬 실행 후 캡처로 교체해야 한다" 명시.

8. "## 심화: 종료 조건과 콜백/폴백" — RESEARCH.md "Loop Termination Conditions" 표(텍스트 응답 / max_turns 90 소진 → 작업 요약 / `/stop`). 콜백 8종과 폴백(429/5xx/401 → fallback_providers)은 한두 문장 name-drop만 (개발자 수준 상세이므로 깊이 들어가지 말 것).

9. "## 흔한 오류 / 주의" — RESEARCH.md의 두 callout을 그대로:
   - 주의 1: 동기 엔진의 의미 ("AIAgent는 동기 엔진입니다 ... 응답이 느리면 도구가 실행 중인 것").
   - 주의 2: max_turns 소진 ("agent.max_turns(기본 90) 초과 시 작업 요약 반환 후 중단 ... delegate_task 활용").

10. "## 다음 단계" — 이전: `[Ch.3 모델 설정](../03-model/index.md)`, 다음: `[Ch.5 컨텍스트 파일](../05-context-files/index.md)`.

제약:
- RESEARCH.md에 없는 수치/단계/콜백 시그니처를 만들지 말 것.
- 모든 명령/설정 코드블록 첫 줄은 반드시 `# 검증: hermes rolling, 2026-06-09`. ASCII 다이어그램과 9단계 흐름은 명령이 아닌 설명용 텍스트 블록이므로 `# 검증:` 불필요(명령/설정이 아님). 단 `hermes`/`config set` 등 실제 실행 명령 블록에는 필수.
  </action>
  <verify>`grep -q "Assemble" src/04-agent-loop/index.md && grep -q "ThreadPoolExecutor" src/04-agent-loop/index.md && grep -q "max_turns" src/04-agent-loop/index.md && grep -q "3티어" src/04-agent-loop/index.md` 통과.
비공식 프레이밍 부재: `! grep -qi "perceive" src/04-agent-loop/index.md` 통과.
실행 명령 블록 주석: `grep -q "# 검증: hermes rolling, 2026-06-09" src/04-agent-loop/index.md` 통과.
LOW placeholder 존재: `grep -q "로컬 실행 후 캡처 필요" src/04-agent-loop/index.md` 통과.</verify>
  <done>04-agent-loop/index.md가 공식 루프 사이클·3티어 요약·API 모드·툴 디스패치·종료 조건·주의 callout을 담은 Ch.4 본문이며, perceive→decide→act 프레이밍을 쓰지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. SUMMARY.md가 아직 본문이 없는 05~08 챕터를 링크하므로 mdBook 0.5.3가 빈 파일을 자동 생성한다(정상). exit code 0이면 통과. stderr/stdout에 "error"가 포함되면 실패로 보고하고 원인을 진단한다.

mdBook이 누락 링크 파일(05~08) 때문에 비-제로로 종료하는 경우의 폴백: 각 디렉터리에 최소 placeholder를 생성해 스켈레톤이 빌드되도록 한다 (Wave 2 플랜이 본문으로 덮어씀):
```bash
# 검증: shell, 2026-06-09
for d in 05-context-files 06-memory 07-tools 08-backends; do
  mkdir -p "src/$d"
  printf '# placeholder\n' > "src/$d/index.md"
done
```
이 폴백을 적용했다면 SUMMARY.md 작업과 함께 보고한다.
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음.</verify>
  <done>mdbook build가 오류 없이 완료된다.</done>
</task>

</tasks>

<verification>
- src/SUMMARY.md가 기초 4개 + 핵심 개념 5개 = 9개 챕터 링크(중복 경로 없음)를 가진다.
- src/04-agent-loop/index.md가 공식 루프 사이클(Assemble→Call→Parse→Execute→Persist→Loop)·3티어 프롬프트 요약·툴 디스패치·max_turns 종료 조건을 담는다.
- perceive→decide→act 프레이밍이 없다.
- 모든 실행 명령/설정 코드블록에 `# 검증: hermes rolling, 2026-06-09` 주석이 있다.
- LOW confidence 출력은 placeholder + 검증 필요 callout으로 표시된다.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.4를 읽고 AIAgent 코어 루프와 3티어 프롬프트 시스템의 동작 원리를 설명할 수 있다 (CORE-01, 성공 기준 #1). SUMMARY.md 스켈레톤이 후속 4개 챕터 플랜의 병렬 실행 기반을 제공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-concepts/03-01-SUMMARY.md`
</output>
