---
phase: 06-reference
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/20-config-reference/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.20 config.yaml 레퍼런스가 mdBook에서 렌더링된다 (20-config-reference/index.md, 빌드 오류 없음)"
    - "Ch.20이 상단에 'verified core + 검증 필요 remainder' 프레이밍을 명시하는 > 참고 callout을 포함한다 (REF-01, 성공기준 #2)"
    - "Ch.20이 영역별(display/model/agent/memory/compression/toolsets/approvals/security/terminal/delegation/mcp_servers/skills/cron/curator)로 키의 용도·기본값·허용값을 수록한다"
    - "Ch.20이 .env vs config.yaml 분리 원칙(비밀→.env, 설정→config.yaml)과 해결 우선순위를 기술한다"
    - "approvals.timeout 기본값(60s vs 300s)과 cron_mode 값(deny|approve vs manual|auto) 불일치를 양쪽 모두 제시하고 어느 한쪽도 사실로 단정하지 않으며 'hermes config show로 로컬 확인' 헤지를 단다"
    - "agent.disabled_toolsets는 '존재하지 않음'으로 명시되고 사용 가능한 키로 제시되지 않는다 (toolsets 배열/hermes tools UI가 대안)"
    - "security.redact_secrets가 MCP 오류 메시지의 자격증명 패턴 난독화로 정확히 기술되며 일반 PII 필터가 아님을 명시한다 (별도 PII 리댁션 키 없음)"
    - "기타 미확인 기본값(toolsets 기본 이름, modal_image 등)이 > 검증 필요로 표시되고 단정되지 않는다"
    - "모든 설정 코드블록이 '# 검증: hermes rolling, 2026-06-11' 주석으로 시작한다 (JSON은 // 검증:)"
    - "src/SUMMARY.md는 이 플랜에서 수정되지 않는다 (06-01 단독 소유)"
  artifacts:
    - path: "src/20-config-reference/index.md"
      provides: "Ch.20 config.yaml 키 레퍼런스 본문 (영역별 키 용도/기본값/허용값 + .env 분리 + approvals 불일치 헤지 + disabled_toolsets/redact_secrets 정정)"
      contains: "redact_secrets"
      min_lines: 180
  key_links:
    - from: "src/20-config-reference/index.md"
      to: "src/21-troubleshooting/index.md"
      via: "관련 레퍼런스/다음 단계 인라인 링크"
      pattern: "21-troubleshooting/index.md"
---

<objective>
Ch.20 "config.yaml 레퍼런스" 본문을 작성한다. 06-01이 생성한 스켈레톤(src/20-config-reference/index.md)을 본문으로 덮어쓴다.

Purpose: 독자가 config.yaml 각 키의 용도·기본값·허용값을 영역별로 조회할 수 있게 한다 (REF-01 / 성공 기준 #2, Ch.20). 레퍼런스 페이지는 일반 챕터보다 정확성이 더 중요하므로, 연구에서 데이터 충돌(approvals.timeout/cron_mode)이 명시된 항목은 한쪽을 단정하지 않고 양쪽을 제시 + 로컬 확인 헤지를 단다.
Output: src/20-config-reference/index.md (Ch.20 본문). SUMMARY.md는 건드리지 않는다.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reference/06-RESEARCH.md

선행:
- 06-01-PLAN.md (Wave 1)이 src/SUMMARY.md에 "# 레퍼런스" 섹션 + 3개 항목을 이미 추가했고, src/20-config-reference/index.md 스켈레톤(`# placeholder`)을 생성했다. 이 플랜은 그 스켈레톤을 본문으로 overwrite한다.

기존 파일 (참고용, 건드리지 말 것):
- src/18-security/index.md — security/approvals 키 표현과 redact_secrets 정정 callout이 이미 확립되어 있다. Ch.20에서 일관되게 재사용(특히 redact_secrets = 자격증명 패턴만, 일반 PII 아님).

스타일 기준 (Phase 5 챕터와 동일):
- 한국어 작성. 레퍼런스 구조: 상단 "이 챕터를 시작하기 전에" callout → "verified core + 검증 필요" 프레이밍 > 참고 callout → .env vs config.yaml 분리 → 영역별 키 표/YAML 코드블록 → 미존재 키 표 → 다음 단계.
- MANDATORY 주석 관례: 모든 설정 YAML 코드블록 첫 줄은 `# 검증: hermes rolling, 2026-06-11`. JSON 코드블록은 `// 검증: hermes rolling, 2026-06-11`.
- 검증 안 된 기본값은 개별적으로 `> 검증 필요:` callout 또는 표 표기. 발명 금지.

**핵심 정확성 제약 (RESEARCH.md):**
- `agent.disabled_toolsets` — **존재하지 않음.** "미존재 키" 표/정정에서만 언급. 사용 가능한 키 목록에 절대 넣지 말 것. 대안: `toolsets:` 배열 또는 `hermes tools` UI.
- `approvals.timeout` 기본값과 `approvals.cron_mode` 값은 연구 파일 간 **실제 데이터 충돌**이 있다 — 60s vs 300s, deny|approve vs manual|auto. **어느 한쪽도 사실로 단정하지 말 것.** 양쪽 값을 모두 제시하고 `> 검증 필요: hermes config show로 로컬 확인` 헤지를 단다.
- `security.redact_secrets`는 MCP 오류 메시지의 자격증명 패턴(ghp_/sk-/Bearer) 난독화만 — 일반 PII 필터 아님. 별도 PII 리댁션 키 없음.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.20 config.yaml 레퍼런스 본문 작성</name>
  <files>src/20-config-reference/index.md</files>
  <action>
src/20-config-reference/index.md(현재 `# placeholder`)를 본문으로 overwrite한다. RESEARCH.md "06-02: config.yaml 레퍼런스 (Chapter 20)" 섹션의 HIGH confidence 사실만 사용. 한국어. 레퍼런스 구조를 따른다.

포함해야 할 내용 (모두 RESEARCH.md 06-02 근거):

1. 제목: `# config.yaml 레퍼런스`
   상단 "이 챕터를 시작하기 전에" callout: 이 페이지는 `~/.hermes/config.yaml`의 키를 한곳에 모은 부록임을 명시. 관련 [Ch.19 CLI 레퍼런스](../19-cli-reference/index.md)(config 명령), [Ch.21 마스터 트러블슈팅 색인](../21-troubleshooting/index.md)으로 잇는다 (둘 다 같은 레퍼런스 섹션 내 실제 파일 — 정상 mdBook 상대 링크).

2. "verified core + 검증 필요" 프레이밍 — RESEARCH.md 06-02 "Meta-accuracy risk statement"의 > 참고 callout을 페이지 상단에 그대로 배치:
   ```
   > 참고: 이 레퍼런스는 공식 문서와 튜토리얼 검증 과정에서 확인된 키만 수록합니다.
   > Hermes는 롤링 릴리스 방식으로 배포되므로 키 이름과 기본값이 변경될 수 있습니다.
   > `hermes config show`로 현재 실제 설정값을 항상 확인하십시오.
   > `> 검증 필요` 표시 항목은 로컬에서 추가 확인이 필요합니다.
   ```

3. "## .env vs config.yaml 분리 원칙" — RESEARCH.md 06-02의 분리 원칙(비밀→.env, 설정→config.yaml, OAuth→auth.json) + 해결 우선순위(1.CLI 인수 2.config.yaml 3..env 4.내장 기본값) + 환경변수 치환 `${VAR_NAME}`(중괄호 필수). 공식 예시 파일 URL(cli-config.yaml.example) 언급.

4. 영역별 키 섹션 — RESEARCH.md 06-02 A~N의 YAML 코드블록을 그대로 사용(각 블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`). 각 키의 용도/기본값/허용값을 코드블록 인라인 주석 + 보조 표로 기술. 다음 헤딩을 모두 둔다:
   - "## display" (language 지원 값 목록 + ko 부분 지원 주의; platforms.*.tool_progress off|brief|verbose).
   - "## model" (default provider/model-id, provider 허용값, context_length 최소 64000, reasoning_effort 허용값).
   - "## agent" (max_turns 기본 90, personalities 커스텀 정의). 여기 `agent.disabled_toolsets`를 넣지 말 것 — 미존재 키 표(아래 항목 8)에서만.
   - "## memory" (memory_enabled/user_profile_enabled 기본 true, memory_char_limit 2200, user_char_limit 1375, provider 선택) + 메모리 파일 경로(MEMORY.md/USER.md/SOUL.md).
   - "## compression" (enabled, threshold 0.50, target_ratio 0.20, protect_last_n 20; protect_first_n=3 하드코딩 설정 불가) + auxiliary.compression/vision 보조 모델 블록.
   - "## toolsets / custom_toolsets" (toolsets 배열, kanban opt-in, custom_toolsets 조합, web.use_gateway). 기본 toolsets 키 이름은 `> 검증 필요`(hermes config show 확인).
   - "## approvals" — 아래 항목 5에서 충돌 헤지와 함께 상세 기술.
   - "## security" (tirith_enabled/tirith_timeout/tirith_fail_open, redact_secrets, allow_private_urls, allow_lazy_installs) + Hardline blocklist 주의. redact_secrets는 아래 항목 6 정정과 함께.
   - "## terminal" (backend 허용값 local|docker|ssh|singularity|modal|daytona, docker_* 키들, container_* 리소스, ssh persistent_shell) + .env 환경변수 표(TERMINAL_SSH_*/MODAL_TOKEN_*/DAYTONA_API_KEY). modal_image 키는 `> 검증 필요`(MEDIUM).
   - "## delegation" (max_spawn_depth 1, max_concurrent_children 3).
   - "## mcp_servers" (command/args/env stdio, url/headers HTTP, ssl_verify, auth oauth, enabled/timeout, tools.include/exclude — include 우선).
   - "## skills" (external_dirs).
   - "## cron / curator" (cron.wrap_response/script_timeout_seconds; curator.enabled/interval_hours 168/stale_after_days 30/archive_after_days 90).

5. "## approvals (승인 모드)" — RESEARCH.md 06-02 G의 approvals YAML 블록 + 모드 표(manual|smart|off). **데이터 충돌 헤지 필수:**
   - `timeout`과 `cron_mode` 줄의 인라인 주석에 양쪽 값을 모두 표기(예: `timeout: # 03-RESEARCH: 60, 05-RESEARCH: 300`; `cron_mode: # manual|auto (deny|approve로 기록된 연구도 있음)`).
   - 그 아래 RESEARCH.md 06-02 G의 `> 검증 필요` callout을 그대로:
     ```
     > 검증 필요: `timeout` 기본값이 60초인지 300초인지, `cron_mode` 값이 `deny|approve`인지
     > `manual|auto`인지 두 연구 파일에서 다르게 기록되었습니다. 어느 한쪽도 단정하지 마십시오.
     > `hermes config show`로 로컬에서 실제 값을 확인하십시오.
     ```
   - 본문 표/산문에서 timeout 기본값을 "300초" 또는 "60초"로 단정하는 문장을 절대 쓰지 말 것. cron_mode도 동일.

6. "## security.redact_secrets의 정확한 범위" — RESEARCH.md 06-02 H 주의 + Ch.18 확립 표현 재사용. MCP 오류 메시지의 자격증명 패턴(ghp_.../sk-.../Bearer 토큰)만 난독화; 일반 PII(이름/이메일/전화) 필터 아님; 별도 "PII 리댁션 설정 키"는 존재하지 않음.

7. "## 미존재 키 (혼동 방지)" — RESEARCH.md 06-02 P 표 그대로:
   | 키 | 상태 |
   - `agent.disabled_toolsets` → **존재하지 않음** (조용히 무시). 대안: toolsets 배열 / hermes tools UI.
   - `security.pii_redact` 또는 유사 → **존재하지 않음** (PII 필터 별개 기능 없음).
   - memory.provider 세부 키 → provider: honcho는 존재하나 Honcho 세부는 ~/.honcho/config.json.

8. "## .env 환경변수" — RESEARCH.md 06-02 O의 게이트웨이/플랫폼 .env 변수 표(GATEWAY_ALLOW_ALL_USERS 절대 금지 경고 포함, TELEGRAM/DISCORD/SLACK/WHATSAPP/SIGNAL/EMAIL/API_SERVER/모델 키). 공식 .env.example URL 언급. 이 표는 코드블록이 아닌 마크다운 표이므로 검증 주석 불필요(설명용).

9. "## 발행 전 로컬 확인 항목" — RESEARCH.md "발행 전 필수 로컬 확인 항목" 중 config 관련(기본값 hermes config show, approvals.timeout, approvals.cron_mode, 기본 toolsets 이름, modal_image)을 표로 요약하며 모두 `> 검증 필요`임을 강조.

10. "## 다음 단계" — [Ch.21 마스터 트러블슈팅 색인](../21-troubleshooting/index.md)으로 연결. 이전 [Ch.19 CLI 레퍼런스](../19-cli-reference/index.md).

제약:
- RESEARCH.md 06-02에 없는 키/기본값을 만들지 말 것.
- `agent.disabled_toolsets`를 사용 가능한 키로 제시 절대 금지(미존재 키 표에서만).
- `approvals.timeout` 기본값과 `approvals.cron_mode` 값을 어느 한쪽으로도 단정 절대 금지 — 양쪽 제시 + 로컬 확인 헤지.
- `security.redact_secrets`를 일반 PII 필터로 기술 절대 금지.
- 모든 설정 YAML 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`(JSON은 `// 검증: ...`).
  </action>
  <verify>프레이밍 callout: `grep -q "확인된 키만 수록" src/20-config-reference/index.md && grep -q "검증 필요" src/20-config-reference/index.md` 통과.
.env 분리: `grep -q "config.yaml" src/20-config-reference/index.md && grep -q ".env" src/20-config-reference/index.md` 통과.
영역별 키: `grep -q "max_turns" src/20-config-reference/index.md && grep -q "compression" src/20-config-reference/index.md && grep -q "terminal" src/20-config-reference/index.md && grep -q "delegation" src/20-config-reference/index.md` 통과.
approvals 충돌 헤지(양쪽 제시 + 로컬 확인): `grep -q "60" src/20-config-reference/index.md && grep -q "300" src/20-config-reference/index.md && grep -q "cron_mode" src/20-config-reference/index.md && grep -q "hermes config show" src/20-config-reference/index.md` 통과.
disabled_toolsets 미존재 정정: `grep -q "disabled_toolsets" src/20-config-reference/index.md && grep -q "존재하지 않" src/20-config-reference/index.md` 통과.
redact_secrets 정정: `grep -q "redact_secrets" src/20-config-reference/index.md && grep -q "자격증명" src/20-config-reference/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-11" src/20-config-reference/index.md` 통과.
placeholder 제거됨: `! grep -qx "# placeholder" src/20-config-reference/index.md` 통과.
다음 챕터 링크: `grep -q "21-troubleshooting/index.md" src/20-config-reference/index.md` 통과.</verify>
  <done>20-config-reference/index.md가 영역별 키 용도/기본값/허용값, .env 분리, approvals.timeout/cron_mode 충돌 헤지(단정 없음), disabled_toolsets/redact_secrets 정정을 담는다.</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증 + SUMMARY.md 미수정 확인</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. 또한 이 플랜이 src/SUMMARY.md를 건드리지 않았음을 확인한다(06-01 단독 소유). 파일 변경이 src/20-config-reference/index.md 하나뿐이므로 별도 no-op 커밋은 생략한다(03-03/04-03 패턴).
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음.
SUMMARY.md 미수정: `git diff --name-only src/SUMMARY.md` 출력이 비어 있음(`[ -z "$(git diff --name-only src/SUMMARY.md)" ]` 통과).
파일 존재: `test -f src/20-config-reference/index.md` 통과.</verify>
  <done>mdbook build가 오류 없이 완료되고 src/SUMMARY.md가 이 플랜에서 수정되지 않았다.</done>
</task>

</tasks>

<verification>
- src/20-config-reference/index.md가 "verified core + 검증 필요" 프레이밍, .env vs config.yaml 분리, 영역별 키 용도/기본값/허용값을 담는다.
- approvals.timeout(60 vs 300)·cron_mode(deny|approve vs manual|auto) 충돌이 양쪽 제시 + `hermes config show` 로컬 확인 헤지로 처리되고 어느 한쪽도 단정되지 않는다.
- agent.disabled_toolsets는 미존재 키로만 등장; security.redact_secrets는 자격증명 패턴 난독화로 정확히 기술(일반 PII 아님).
- 모든 설정 코드블록에 `# 검증: hermes rolling, 2026-06-11`(JSON은 `// 검증:`).
- src/SUMMARY.md는 이 플랜에서 수정되지 않았다 (git diff --name-only src/SUMMARY.md 비어 있음).
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.20에서 config.yaml 각 키의 용도·기본값·허용값을 영역별로 조회할 수 있고, 데이터 충돌이 있는 approvals 키는 양쪽 값과 로컬 확인 안내를 통해 정확히 이해할 수 있다 (REF-01, 성공 기준 #2).
</success_criteria>

<output>
After completion, create `.planning/phases/06-reference/06-02-SUMMARY.md`
</output>
