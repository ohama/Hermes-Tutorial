---
phase: 06-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SUMMARY.md
  - src/19-cli-reference/index.md
  - src/20-config-reference/index.md
  - src/21-troubleshooting/index.md
autonomous: true

must_haves:
  truths:
    - "SUMMARY.md에 '# 레퍼런스' 섹션과 3개 항목(19-cli-reference/20-config-reference/21-troubleshooting)이 추가되며 기존 18개 항목(기초 4 + 핵심 개념 5 + 학습과 자동화 5 + 플랫폼과 운영 4)은 한 글자도 바뀌지 않고 보존된다"
    - "SUMMARY.md 총 index.md 링크는 21개이며 중복 경로가 없다 (mdBook 0.5.3 중복 경로 = 하드 빌드 오류)"
    - "SUMMARY.md에 16-* 항목이 존재하지 않는다 (기존 패턴 유지)"
    - "Ch.19 CLI·슬래시 커맨드 레퍼런스가 mdBook에서 렌더링된다 (19-cli-reference/index.md, 빌드 오류 없음)"
    - "Ch.19가 상단에 'verified core + 검증 필요 remainder' 프레이밍을 명시하는 > 참고 callout을 포함한다 (REF-01, 성공기준 #1)"
    - "Ch.19가 실행/모델/툴/스킬/MCP/크론/게이트웨이/운영 카테고리별로 검증된 CLI 명령어를 수록하고 세션 내 슬래시 커맨드 목록을 포함한다"
    - "Ch.19가 'hermes security audit는 존재하지 않음'을 정정 callout으로 명시하며 사용 가능한 명령으로 제시하지 않는다"
    - "Ch.19가 'hermes doctor = 공급망 어드바이저리(설정 검증 아님), 설정 검증 = hermes config check'를 정확히 구분한다"
    - "Ch.19가 8개 로컬 검증 항목(hermes logs 플래그, mcp serve 노출 툴, gateway install --system 경로 등)을 > 검증 필요로 표시한다 (단정 금지)"
    - "20-config-reference / 21-troubleshooting 스켈레톤 파일이 존재하여 SUMMARY 링크가 빌드되며 mdbook build가 exit 0이다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-11' 주석으로 시작한다"
  artifacts:
    - path: "src/SUMMARY.md"
      provides: "기초 4 + 핵심 개념 5 + 학습과 자동화 5 + 플랫폼과 운영 4 + 레퍼런스 3 = 총 21개 챕터 목차"
      contains: "21-troubleshooting/index.md"
    - path: "src/19-cli-reference/index.md"
      provides: "Ch.19 CLI·슬래시 커맨드 레퍼런스 본문 (카테고리별 검증된 명령어 + 슬래시 커맨드 + security audit/doctor 정정 + 검증 필요 항목)"
      contains: "hermes config check"
      min_lines: 180
    - path: "src/20-config-reference/index.md"
      provides: "Ch.20 스켈레톤 (Wave 2의 06-02가 본문으로 덮어씀)"
      contains: "placeholder"
    - path: "src/21-troubleshooting/index.md"
      provides: "Ch.21 스켈레톤 (Wave 2의 06-03이 본문으로 덮어씀)"
      contains: "placeholder"
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/19-cli-reference/index.md, src/20-config-reference/index.md, src/21-troubleshooting/index.md"
      via: "mdBook 레퍼런스 섹션 목차 링크"
      pattern: "(19-cli-reference|20-config-reference|21-troubleshooting)/index.md"
    - from: "src/19-cli-reference/index.md"
      to: "src/20-config-reference/index.md"
      via: "다음 챕터/관련 레퍼런스 인라인 링크"
      pattern: "20-config-reference/index.md"
---

<objective>
Phase 6 전체의 SUMMARY.md 스켈레톤("# 레퍼런스" 섹션 + 3개 챕터 항목)을 한 번에 확정하고, Ch.19 "CLI · 슬래시 커맨드 레퍼런스" 본문을 작성한다.

Purpose: 이 플랜이 SUMMARY.md를 단독 소유함으로써 후속 2개 챕터 플랜(06-02/06-03)이 SUMMARY.md 쓰기 충돌 없이 Wave 2에서 병렬 실행되도록 한다. 동시에 독자가 검증된 CLI 명령어·슬래시 커맨드 전체 목록을 언제든 조회할 수 있게 한다 (REF-01 / 성공 기준 #1, Ch.19). 레퍼런스 페이지는 일반 챕터보다 정확성이 더 중요하므로 "verified core + 검증 필요" 프레임을 페이지에 명시한다.
Output: src/SUMMARY.md (21-챕터 목차), src/19-cli-reference/index.md (Ch.19 본문), 20/21 챕터 스켈레톤 2개.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reference/06-RESEARCH.md

기존 파일 (반드시 통합, 빌드 깨뜨리지 말 것):
- src/SUMMARY.md — 현재 기초 4 + 핵심 개념 5 + 학습과 자동화 5 + 플랫폼과 운영 4 = 18개 항목(00~18, 16 건너뜀) 존재. "# 레퍼런스" 섹션과 3개 새 항목을 ADD (기존 항목 텍스트는 절대 변경/삭제 금지, 새 경로만 추가).
- src/18-security/index.md — Phase 5의 완성된 마지막 챕터. 스타일/주석 관례/정정 callout 표현 참고용. 건드리지 말 것. (특히 hermes doctor / hermes security audit / redact_secrets 정정 표현이 여기 확립되어 있으니 Ch.19에서 일관되게 재사용.)

스타일 기준 (Phase 5 챕터와 동일):
- 한국어 작성. 레퍼런스 챕터 구조: 상단 "이 챕터를 시작하기 전에" callout → "verified core + 검증 필요" 프레이밍 > 참고 callout → 카테고리별 명령어 표/코드블록 → 정정 callout → 다음 단계.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄은 `# 검증: hermes rolling, 2026-06-11` (오늘=2026-06-11). JSON 코드블록은 `// 검증: hermes rolling, 2026-06-11`. 표(마크다운 테이블)는 코드블록이 아니므로 주석 불필요하나, 본문 중 실제 실행 명령을 담은 bash 코드블록은 반드시 주석으로 시작.
- 검증 안 된 항목은 개별적으로 `> 검증 필요:` callout 또는 표의 `> 검증 필요` 표기. 발명 금지: RESEARCH.md에 없는 명령/플래그/키를 만들지 말 것.

**핵심 정확성 제약 (RESEARCH.md "절대로 추가하지 말 것"):**
- `hermes security audit` — **존재하지 않음.** 정정 callout으로만 언급, 사용 가능한 명령 표/목록에 절대 넣지 말 것.
- `agent.disabled_toolsets` — 존재하지 않음 (Ch.20 관할이지만 Ch.19 슬래시 커맨드 설명에서도 toolsets 배열/`/tools disable`만 언급).
- `hermes doctor`는 공급망 어드바이저리 검사기이지 설정 검증 도구가 아님 — 설정 검증은 `hermes config check`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SUMMARY.md에 '레퍼런스' 섹션 + 3개 챕터 추가</name>
  <files>src/SUMMARY.md</files>
  <action>
src/SUMMARY.md 끝(18-security 항목 다음 줄)에 새 섹션 구분자(`---`)와 `# 레퍼런스` 헤딩, 그리고 3개 챕터 항목을 ADD한다. 기존 "# 기초"(4개)·"# 핵심 개념"(5개)·"# 학습과 자동화"(5개)·"# 플랫폼과 운영"(4개) 섹션의 18개 항목은 한 글자도 바꾸지 말고 그대로 보존한다.

추가할 블록은 정확히 다음과 같다 (파일 끝, 18-security 항목 다음 줄에 이어 붙인다):

```markdown

---

# 레퍼런스

- [CLI · 슬래시 커맨드 레퍼런스](19-cli-reference/index.md)
- [config.yaml 레퍼런스](20-config-reference/index.md)
- [마스터 트러블슈팅 색인](21-troubleshooting/index.md)
```

주의 (RESEARCH.md Pitfall B-11): 기존 00~18 경로를 다시 추가하지 말 것 (중복 경로는 mdBook 0.5.3 하드 빌드 오류). 16-* 경로를 만들지 말 것. 3개 새 경로(19-cli-reference/20-config-reference/21-troubleshooting)는 전부 신규이므로 안전하다.
  </action>
  <verify>새 경로가 각 1회씩 등장하고 총 21개 index.md 링크, 중복 없음을 확인:
`grep -c "index.md" src/SUMMARY.md` 출력이 21.
`grep -q "19-cli-reference/index.md" src/SUMMARY.md && grep -q "20-config-reference/index.md" src/SUMMARY.md && grep -q "21-troubleshooting/index.md" src/SUMMARY.md` 통과.
`grep -q "레퍼런스" src/SUMMARY.md` 통과.
16-* 항목 부재: `! grep -q "16-" src/SUMMARY.md` 통과.
중복 경로 없음: `[ -z "$(grep -oE '\([^)]*index.md\)' src/SUMMARY.md | sort | uniq -d)" ]` 통과.
기존 항목 보존: `grep -q "00-intro/index.md" src/SUMMARY.md && grep -q "18-security/index.md" src/SUMMARY.md` 통과.</verify>
  <done>SUMMARY.md가 기초 4 + 핵심 개념 5 + 학습과 자동화 5 + 플랫폼과 운영 4 + 레퍼런스 3 = 21개 챕터 링크를 가지며 16-* 항목과 중복 경로가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.19 CLI·슬래시 커맨드 레퍼런스 본문 작성 + 20/21 스켈레톤 생성</name>
  <files>src/19-cli-reference/index.md, src/20-config-reference/index.md, src/21-troubleshooting/index.md</files>
  <action>
먼저 20/21 챕터 스켈레톤 2개를 생성한다 (Wave 2의 06-02/06-03가 본문으로 overwrite). 각 파일 내용은 정확히 한 줄:
```
# placeholder
```
대상: src/20-config-reference/index.md, src/21-troubleshooting/index.md. (디렉터리는 `mkdir -p`로 먼저 만든다. **16-* 디렉터리는 만들지 않는다.**)

그다음 src/19-cli-reference/index.md를 작성한다. RESEARCH.md "06-01: CLI 커맨드 · 슬래시 커맨드 레퍼런스 (Chapter 19)" 섹션의 HIGH confidence 사실만 사용. 한국어. 레퍼런스 챕터 구조를 따른다.

포함해야 할 내용 (모두 RESEARCH.md 06-01 근거):

1. 제목: `# CLI · 슬래시 커맨드 레퍼런스`
   상단 "이 챕터를 시작하기 전에" callout: 이 페이지는 튜토리얼 본문(Ch.0–18)에서 다룬 명령어를 한곳에 모은 부록임을 명시. 관련 [Ch.20 config.yaml 레퍼런스](../20-config-reference/index.md)와 [Ch.21 마스터 트러블슈팅 색인](../21-troubleshooting/index.md)로 잇는다 (둘 다 백워드/사이드 링크 — 같은 레퍼런스 섹션 내 실제 파일이므로 정상 mdBook 상대 링크).

2. "verified core + 검증 필요" 프레이밍 — RESEARCH.md 06-01 "Meta-accuracy risk statement"의 > 참고 callout을 페이지 상단에 그대로 배치:
   ```
   > 참고: 이 레퍼런스는 공식 문서와 튜토리얼 검증 과정에서 확인된 명령어만 수록합니다.
   > Hermes는 롤링 릴리스 방식으로 배포되므로 명령어가 변경될 수 있습니다.
   > `hermes --help` 및 각 서브커맨드의 `--help` 플래그로 현재 전체 목록을 확인하십시오.
   > 아래 `> 검증 필요` 표시 항목은 로컬에서 추가 확인이 필요합니다.
   ```
   그 아래 공식 참조 소스 표(`hermes --help` / 공식 CLI 레퍼런스 URL)도 RESEARCH 그대로 포함.

3. 카테고리별 CLI 명령어 — RESEARCH.md 06-01 A~J의 bash 코드블록을 그대로 사용(각 블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`). 다음 카테고리를 모두 헤딩으로 둔다:
   - "## A. 최상위 실행 명령어" (hermes / --tui / --continue / chat -q / --help). `hermes help`(플래그 없음)는 미문서화임을 주의로 명시.
   - "## B. 모델 · 설정 명령어" (model / setup / setup --portal / setup terminal / config show / config set / config check / version). `hermes config check`는 `hermes doctor`와 다름을 명시. `config check` 정확 동작은 `> 검증 필요`(config --help로 확인).
   - "## C. 도구 명령어" (tools / tools --summary).
   - "## D. 스킬 명령어" (skills list/browse/search/inspect/install/check/update/uninstall).
   - "## E. MCP 명령어" (mcp / catalog / install / add --preset / configure / login / serve / serve --verbose). `mcp serve`가 노출하는 정확한 툴 이름 목록은 `> 검증 필요`(로컬 --verbose 확인).
   - "## F. 크론 스케줄러 명령어" (cron list/create/edit/pause/resume/run/remove/status/tick) + 스케줄 형식 요약 표(상대 지연/인터벌/Cron 표현식/ISO).
   - "## G. 게이트웨이 명령어" (gateway / run / install / install --system / start / stop / restart / status / setup). `gateway install --system`의 정확한 서비스 파일 경로는 `> 검증 필요`(Ubuntu VPS 로컬 확인).
   - "## H. 운영 명령어" (doctor / doctor --ack / status / version / sessions / memory / curator / dashboard / pairing / portal / kanban …). `hermes logs` 서브커맨드 플래그(`--level WARNING`, `--since 1h`)는 `> 검증 필요`(logs --help로 확인).
   - "## I. Nous Portal / 인증 명령어" (setup --portal / portal info / portal tools). `hermes auth add`가 별도 최상위 명령인지 model 마법사 내부인지 `> 검증 필요`.
   - "## J. Slack/WhatsApp 특수 명령어" (slack manifest --write / whatsapp).

4. "## 정정: hermes doctor vs hermes config check" — RESEARCH.md 06-01 H의 "중요 수정 사항" 정정 callout 사용. `hermes doctor`는 공급망 어드바이저리(Python CVE) 검사기이며 설정 검증이 아님; 설정 검증은 `hermes config check`. (Ch.18에서 확립된 표현과 일관.)

5. "## 정정: hermes security audit는 존재하지 않음" — RESEARCH.md 06-01 H의 정정 callout 그대로:
   ```
   > 참고: `hermes security audit` 명령은 존재하지 않습니다.
   > 보안 관련 작업은 다음으로 나뉩니다:
   > - `hermes doctor` → 공급망 어드바이저리
   > - `hermes pairing` → 인증된 사용자 관리
   > - `hermes config check` → 설정 검증
   > - Tirith 스캔은 모든 명령 실행 시 자동으로 수행됨
   ```
   이 명령을 사용 가능한 명령 표/코드블록에 절대 넣지 말 것.

6. "## 세션 내 슬래시 커맨드" — RESEARCH.md 06-01 K의 슬래시 커맨드 표 그대로(커맨드/기능 2열). 대소문자 비구분(`/HELP`=`/help`) 명시. 14개 내장 퍼소날리티 목록도 함께 기술. `/tools disable <toolset>` 등이 toolset 비활성화의 유효 경로임을 명시(존재하지 않는 `agent.disabled_toolsets` 키 대신).

7. "## 키 바인딩" — RESEARCH.md 06-01 L의 키 바인딩 표 그대로(Enter / Alt+Enter / Ctrl+G / Ctrl+C / Ctrl+D).

8. "## 발행 전 로컬 확인 항목" — RESEARCH.md "발행 전 필수 로컬 확인 항목" 표 중 CLI 관련 항목(전체 명령어 트리, config check 존재, hermes logs 플래그, mcp serve 노출 툴, gateway install --system 경로, auth add 존재 등)을 표로 요약. 이 항목들은 `> 검증 필요`로 표시되어 있음을 다시 강조.

9. "## 다음 단계" — [Ch.20 config.yaml 레퍼런스](../20-config-reference/index.md)로 연결. 이전 본문은 [Ch.18 보안 하드닝](../18-security/index.md).

제약:
- RESEARCH.md 06-01에 없는 명령/플래그/서브커맨드를 만들지 말 것.
- `hermes security audit`를 사용 가능한 명령으로 제시 절대 금지(정정 callout으로만).
- `hermes doctor`를 설정 검증 도구로 기술 절대 금지.
- 8개 로컬 검증 항목(config check 동작, mcp serve 노출 툴, gateway install --system 경로, hermes logs 플래그, auth add 존재, toolsets 기본 이름은 Ch.20 관할이므로 여기서는 생략 가능, modal_image도 Ch.20 관할)은 단정하지 말고 `> 검증 필요`로 표시.
- 모든 실제 실행 명령 bash 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`.
  </action>
  <verify>스켈레톤 존재: `grep -q "placeholder" src/20-config-reference/index.md && grep -q "placeholder" src/21-troubleshooting/index.md` 통과.
16-* 디렉터리 부재: `[ -z "$(ls -d src/16-* 2>/dev/null)" ]` 통과.
프레이밍 callout: `grep -q "검증된 명령어만 수록" src/19-cli-reference/index.md && grep -q "검증 필요" src/19-cli-reference/index.md` 통과.
카테고리·핵심 명령: `grep -q "hermes config check" src/19-cli-reference/index.md && grep -q "hermes mcp serve" src/19-cli-reference/index.md && grep -q "hermes cron create" src/19-cli-reference/index.md && grep -q "hermes gateway" src/19-cli-reference/index.md` 통과.
슬래시 커맨드: `grep -q "/personality" src/19-cli-reference/index.md && grep -q "/compress" src/19-cli-reference/index.md` 통과.
security audit 정정(존재하지 않음): `grep -q "존재하지 않습니다" src/19-cli-reference/index.md && grep -q "security audit" src/19-cli-reference/index.md` 통과.
doctor 정정: `grep -q "공급망" src/19-cli-reference/index.md && grep -q "config check" src/19-cli-reference/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-11" src/19-cli-reference/index.md` 통과.
다음 챕터 링크: `grep -q "20-config-reference/index.md" src/19-cli-reference/index.md` 통과.</verify>
  <done>19-cli-reference/index.md가 카테고리별 검증된 CLI 명령어·슬래시 커맨드·키 바인딩을 담고, security audit/doctor 정정을 정확히 기술하며, 로컬 검증 항목을 `> 검증 필요`로 표시하고, 20/21 스켈레톤이 placeholder로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. SUMMARY.md가 링크하는 19~21 챕터 파일(19 본문 + 20/21 스켈레톤)은 이 플랜에서 모두 생성했으므로 빌드가 통과해야 한다. exit code 0이면 통과. stderr/stdout에 "error"가 포함되면 실패로 보고하고 원인을 진단한다.

만약 mdBook이 링크된 파일 누락으로 비-제로 종료하면, 폴백으로 누락 디렉터리에 최소 placeholder를 생성한다 (Wave 2 플랜이 본문으로 덮어씀). **16-* 디렉터리는 만들지 않는다:**
```bash
# 검증: shell, 2026-06-11
for d in 20-config-reference 21-troubleshooting; do
  mkdir -p "src/$d"
  [ -s "src/$d/index.md" ] || printf '# placeholder\n' > "src/$d/index.md"
done
```
이 폴백을 적용했다면 보고한다. (mdBook 0.5.3은 SUMMARY 링크 미작성 파일을 build 시 자동 생성하기도 하나, 명시적 스켈레톤이 더 안전하다.)
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음. `test -f src/19-cli-reference/index.md && test -f src/20-config-reference/index.md && test -f src/21-troubleshooting/index.md` 통과. 16-* 부재: `[ -z "$(ls -d src/16-* 2>/dev/null)" ]` 통과.</verify>
  <done>mdbook build가 오류 없이 완료되고 19~21 챕터 파일이 모두 존재하며 16-* 디렉터리가 없다.</done>
</task>

</tasks>

<verification>
- src/SUMMARY.md가 21개 챕터 링크(중복 경로 없음, 16-* 항목 없음)를 가진다.
- src/19-cli-reference/index.md가 "verified core + 검증 필요" 프레이밍 callout, 카테고리별 검증된 CLI 명령어(A~J), 슬래시 커맨드, 키 바인딩을 담는다.
- hermes security audit는 정정 callout으로만 등장(사용 명령으로 제시 금지); hermes doctor는 공급망 검사기로 기술(설정 검증 = hermes config check).
- 8개 로컬 검증 항목은 `> 검증 필요`로 표시(단정 금지).
- 모든 실제 실행 명령 코드블록에 `# 검증: hermes rolling, 2026-06-11` 주석.
- 20/21 스켈레톤 placeholder 존재, 16-* 디렉터리 없음.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.19에서 검증된 CLI 명령어·슬래시 커맨드 전체 목록을 카테고리별로 조회할 수 있고, 존재하지 않는 명령(security audit)과 오해(doctor=설정 검증)를 정정 callout으로 확인할 수 있다 (REF-01, 성공 기준 #1). SUMMARY.md 스켈레톤이 후속 2개 챕터 플랜의 Wave 2 병렬 실행 기반을 제공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reference/06-01-SUMMARY.md`
</output>
