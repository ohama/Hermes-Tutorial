---
phase: 04-learning-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SUMMARY.md
  - src/09-skills/index.md
  - src/10-learning-loop/index.md
  - src/11-mcp/index.md
  - src/12-cron/index.md
  - src/13-subagents/index.md
autonomous: true

must_haves:
  truths:
    - "SUMMARY.md에 '# 학습과 자동화' 섹션과 5개 챕터 항목(09-skills/10-learning-loop/11-mcp/12-cron/13-subagents)이 추가되며 기존 9개 항목(기초 4 + 핵심 개념 5)은 그대로 보존된다"
    - "SUMMARY.md에 중복 경로가 없다 (mdBook 0.5.3 하드 빌드 오류 방지)"
    - "Ch.9 스킬 시스템 챕터가 mdBook에서 렌더링된다 (09-skills/index.md, 빌드 오류 없음)"
    - "Ch.9가 SKILL.md = Markdown + YAML frontmatter, 저장 위치 ~/.hermes/skills/, agentskills.io 호환 표준을 설명하고 독자가 직접 SKILL.md를 작성하는 실습을 포함한다 (LEARN-01, 성공기준 #1)"
    - "Ch.9가 번들 스킬 수를 '약 89개(v0.15.x)'로 기술하고 118(v0.10.0 과거값)·19,932 커뮤니티 집계 수치를 사실로 단언하지 않는다"
    - "Ch.9의 스킬 자동 생성/제안 트리거는 큐레이터의 비활동 기반 동작과 구분되며, 정확한 자동 생성 조건은 `> 검증 필요`로 헤지된다 (사실로 '5회 툴 호출' 단언 금지)"
    - "hermes skills list 출력은 [로컬 실행 후 캡처 필요] placeholder + `> 검증 필요`로 표시된다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-10' 주석으로 시작한다"
    - "10~13 챕터 스켈레톤 파일이 존재하여 SUMMARY 링크가 빌드되며, mdbook build가 exit 0이다"
  artifacts:
    - path: "src/SUMMARY.md"
      provides: "기초 4 + 핵심 개념 5 + 학습과 자동화 5 = 총 14개 챕터 목차"
      contains: "13-subagents/index.md"
    - path: "src/09-skills/index.md"
      provides: "Ch.9 스킬 시스템 본문 (SKILL.md 형식 + 저장 위치 + CLI + 직접 작성 실습 + 자동 생성 트리거 헤지)"
      contains: "SKILL.md"
      min_lines: 120
    - path: "src/10-learning-loop/index.md"
      provides: "Ch.10 스켈레톤 (Wave 2의 04-02가 본문으로 덮어씀)"
      contains: "placeholder"
    - path: "src/11-mcp/index.md"
      provides: "Ch.11 스켈레톤 (Wave 2의 04-03이 본문으로 덮어씀)"
      contains: "placeholder"
    - path: "src/12-cron/index.md"
      provides: "Ch.12 스켈레톤 (Wave 2의 04-04가 본문으로 덮어씀)"
      contains: "placeholder"
    - path: "src/13-subagents/index.md"
      provides: "Ch.13 스켈레톤 (Wave 2의 04-05가 본문으로 덮어씀)"
      contains: "placeholder"
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/09-skills/index.md, src/10-learning-loop/index.md, src/11-mcp/index.md, src/12-cron/index.md, src/13-subagents/index.md"
      via: "mdBook 학습과 자동화 섹션 목차 링크"
      pattern: "1[0-3]-.*index.md"
---

<objective>
Phase 4 전체의 SUMMARY.md 스켈레톤("# 학습과 자동화" 섹션 + 5개 챕터 항목)을 한 번에 확정하고, Ch.9 "스킬 시스템" 본문을 작성한다.

Purpose: 이 플랜이 SUMMARY.md를 단독 소유함으로써 후속 4개 챕터 플랜(04-02~04-05)이 SUMMARY.md 쓰기 충돌 없이 Wave 2에서 병렬 실행되도록 한다. 동시에 독자가 스킬 시스템의 SKILL.md 형식·저장 구조·CLI·직접 작성 방법과 자동 생성 트리거 개념을 이해하게 한다 (성공 기준 #1 / LEARN-01).
Output: src/SUMMARY.md (14-챕터 목차), src/09-skills/index.md (Ch.9 본문), 10~13 챕터 스켈레톤 4개.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-learning-automation/04-RESEARCH.md

기존 파일 (반드시 통합, 빌드 깨뜨리지 말 것):
- src/SUMMARY.md — 현재 기초 4개 + 핵심 개념 5개 = 9개 항목 존재. "# 학습과 자동화" 섹션과 5개 새 항목을 ADD (기존 9개 항목 텍스트는 절대 변경/삭제 금지, 새 경로만 추가). RESEARCH.md "SUMMARY.md additions" 블록 그대로.
- src/06-memory/index.md — Phase 3의 완성된 챕터. 스타일/주석 관례 참고용. 건드리지 말 것. (메모리 챕터에서 Skills를 "Procedural memory, Phase 4 범주"로 예고했으므로 Ch.9가 그 연결을 이어받는다.)

스타일 기준 (Phase 2/3 챕터와 동일):
- 한국어 작성. RESEARCH.md "Chapter file template" 구조: 개요 → 개념 → 실습 → 심화 → 흔한 오류/주의 → 다음 단계.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄은 `# 검증: hermes rolling, 2026-06-10` (Phase 4 신규 블록은 06-10 사용 — 기존 챕터의 06-09와 구분).
- 검증 안 된 출력은 `# [로컬 실행 후 캡처 필요 — ...]` placeholder + `> 검증 필요` callout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SUMMARY.md에 '학습과 자동화' 섹션 + 5개 챕터 추가</name>
  <files>src/SUMMARY.md</files>
  <action>
src/SUMMARY.md 끝에 새 섹션 구분자(`---`)와 `# 학습과 자동화` 헤딩, 그리고 5개 챕터 항목을 ADD한다. 기존 "# 기초"(4개) 및 "# 핵심 개념"(5개) 섹션의 9개 항목은 한 글자도 바꾸지 말고 그대로 보존한다. RESEARCH.md "SUMMARY.md additions" 블록 그대로 사용.

최종 SUMMARY.md 전체는 정확히 다음과 같아야 한다:

```markdown
# Summary

[소개](README.md)

---

# 기초

- [소개: Hermes란 무엇인가](00-intro/index.md)
- [설치](01-install/index.md)
- [첫 실행](02-first-run/index.md)
- [모델 설정](03-model/index.md)

---

# 핵심 개념

- [에이전트 루프](04-agent-loop/index.md)
- [컨텍스트 파일](05-context-files/index.md)
- [메모리](06-memory/index.md)
- [툴 게이트웨이](07-tools/index.md)
- [터미널 백엔드](08-backends/index.md)

---

# 학습과 자동화

- [스킬 시스템](09-skills/index.md)
- [학습 루프](10-learning-loop/index.md)
- [MCP 연동](11-mcp/index.md)
- [크론 스케줄러](12-cron/index.md)
- [서브에이전트](13-subagents/index.md)
```

주의 (RESEARCH.md Pitfall B-11): 기존 9개 경로(00~08)를 다시 추가하지 말 것 — 중복 경로는 mdBook 0.5.3 하드 빌드 오류. 5개 새 경로(09~13)는 전부 신규이므로 안전하다.
  </action>
  <verify>새 경로가 각 1회씩 등장하고 총 14개 index.md 링크, 중복 없음을 확인:
`grep -c "index.md" src/SUMMARY.md` 출력이 14.
`grep -q "09-skills/index.md" src/SUMMARY.md && grep -q "10-learning-loop/index.md" src/SUMMARY.md && grep -q "11-mcp/index.md" src/SUMMARY.md && grep -q "12-cron/index.md" src/SUMMARY.md && grep -q "13-subagents/index.md" src/SUMMARY.md` 통과.
`grep -q "학습과 자동화" src/SUMMARY.md` 통과.
중복 경로 없음: `grep -oE "\([^)]*index.md\)" src/SUMMARY.md | sort | uniq -d` 출력이 비어 있음.
기존 항목 보존: `grep -q "00-intro/index.md" src/SUMMARY.md && grep -q "08-backends/index.md" src/SUMMARY.md` 통과.</verify>
  <done>SUMMARY.md가 기초 4 + 핵심 개념 5 + 학습과 자동화 5 = 14개 챕터 링크를 가지며 중복 경로가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.9 스킬 시스템 본문 작성 + 10~13 스켈레톤 생성</name>
  <files>src/09-skills/index.md, src/10-learning-loop/index.md, src/11-mcp/index.md, src/12-cron/index.md, src/13-subagents/index.md</files>
  <action>
먼저 10~13 챕터 스켈레톤 4개를 생성한다 (Wave 2의 04-02~04-05가 본문으로 overwrite). 각 파일 내용은 정확히 한 줄:
```
# placeholder
```
대상: src/10-learning-loop/index.md, src/11-mcp/index.md, src/12-cron/index.md, src/13-subagents/index.md. (디렉터리는 mkdir -p로 먼저 만든다.)

그다음 src/09-skills/index.md를 작성한다. RESEARCH.md "Ch.9: 스킬 시스템" 섹션의 HIGH confidence 사실만 사용. 한국어. Phase 3 챕터 구조(개요 → 개념 → 실습 → 심화 → 흔한 오류/주의 → 다음 단계)를 따른다.

포함해야 할 내용 (모두 RESEARCH.md 근거):

1. 제목: `# 스킬 시스템`
   상단 "이 챕터를 시작하기 전에" callout: 선행 [Ch.6 메모리](../06-memory/index.md)(스킬 = 절차적 메모리), [Ch.7 툴 게이트웨이](../07-tools/index.md)(툴 루프 이해). 메모리 챕터에서 Skills를 Phase 4 범주로 예고했음을 한 줄로 잇는다.

2. "## 개요" — 스킬은 재사용 가능한 절차(워크플로우, 명령, 분기, 함정, 검증 단계)를 담는 **절차적 메모리**임. 이 챕터에서 SKILL.md 형식을 이해하고 직접 스킬을 작성하며, 에이전트가 스킬을 자동 생성/제안하는 개념을 익힌다.

3. "## 개념: 스킬이란 무엇인가" — Markdown + YAML frontmatter 파일. **agentskills.io 오픈 표준**을 따르며 25개+ 호환 에이전트 플랫폼 간 이식 가능. 점진적 공개(progressive disclosure): 시스템 프롬프트에는 스킬 이름·설명(스킬 인덱스)만 로드되고, 트리거 매칭 시 전체 내용이 온디맨드 로드됨 — 200개 스킬도 활성화 전에는 거의 토큰을 소비하지 않음. (스킬 인덱스가 프롬프트에 표시되는 정확한 형식은 LOW이므로 단언하지 말 것.)

4. "## 개념: 저장 구조" — RESEARCH.md "Storage Location"의 디렉터리 트리(`~/.hermes/skills/<category>/<skill-name>/SKILL.md` + references/templates/scripts/assets 선택)를 코드블록으로 제시. 외부 디렉터리 추가 설정(`skills.external_dirs`) YAML도 포함 (첫 줄 `# 검증: hermes rolling, 2026-06-10`).

5. "## 개념: SKILL.md 형식" — RESEARCH.md "SKILL.md Format" 코드블록 그대로 (YAML frontmatter + name/description/version/metadata.hermes/required_environment_variables/requires_toolsets + body 섹션). 본문 필수 섹션 4개를 명시: **When to Use / Procedure / Pitfalls / Verification**. 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-10`.

6. "## 개념: 스킬 자동 생성과 제안" — 에이전트가 복잡한 다단계 작업을 성공적으로 완료하면 그 접근법을 **스킬로 저장할지 제안**한다(사용자가 수락/거절). 자동 생성의 정확한 트리거 조건(예: 특정 툴 호출 횟수)은 검증되지 않았으므로 사실로 단언하지 말 것. 반드시 다음 헤지 callout을 넣을 것:
   ```
   > 검증 필요: 스킬 자동 생성/제안이 발동되는 정확한 조건(툴 호출 횟수 등)과
   > 제안 메시지 문구는 환경/버전에 따라 다를 수 있습니다. 로컬 실행 후 확인하십시오.
   ```
   (큐레이터의 비활동 기반 주기 검토는 Ch.10에서 다룬다고 plain prose로 예고하되, 여기서 "5회 툴 호출마다"·"15작업마다" 같은 표현을 사실로 쓰지 말 것.)

7. "## 개념: 활성화 (슬래시 커맨드)" — `~/.hermes/skills/`에 배치된 스킬은 슬래시 커맨드 `/skill-name [task description]`로 활성화. 플러그인 네임스페이스는 `/plugin:skill-name`.

8. "## 실습: 스킬 목록 확인" — 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-10`)으로 `hermes skills list`. 출력은 LOW이므로 별도 코드블록에 `# [로컬 실행 후 캡처 필요 — 설치된 스킬 목록 출력]` + 그 아래 `> 검증 필요:` callout(출력 형식은 버전/환경에 따라 다름).

9. "## 실습: 직접 SKILL.md 작성하기" — RESEARCH.md "수동으로 스킬 만들기"의 `mkdir -p ... && cat > ... << 'EOF' ... EOF` 블록 그대로 (my-deploy-skill 예시, When to Use/Procedure/Pitfalls/Verification 4섹션 포함). 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-10`. 작성 후 hermes 재시작하면 `/my-deploy-skill`로 활성화 가능함을 명시.

10. "## 실습: 스킬 관리 CLI" — RESEARCH.md "CLI Commands for Skills Management" + "Community Skill Installation" 명령을 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-10`)으로: `hermes skills list/browse/search/inspect/install/check/update`, 그리고 설치 예시(`hermes skills install official/research/arxiv`, URL 직접 설치, `--name`, `uninstall`). 커뮤니티 허브는 "다양한 커뮤니티 스킬 허브(공식 선택 카탈로그, skills.sh, ClawHub, LobeHub 등)"로 기술 — **단일 집계 수치(19,932 등)를 쓰지 말 것**.

11. "## 심화: 번들 스킬과 skill_manage 툴" — 번들 스킬 **약 89개(v0.15.x, 17개 카테고리)** + 선택 카탈로그 **약 90개+**. RESEARCH.md "주의 2" callout(118은 v0.10.0 과거값, 89는 v0.15.x, 항상 `hermes skills list`로 확인)을 그대로. `skill_manage` 툴(create/patch/edit/delete/write_file/remove_file)은 에이전트 내부 사용이며 독자는 자연어로 "이 스킬 개선해줘"라 요청하면 됨을 한 단락. patch가 토큰 절약을 위해 선호됨을 한 줄.

12. "## 흔한 오류 / 주의" — RESEARCH.md "Pitfall Callouts for Ch.9" 세 개 callout 그대로:
    - 주의 1: 스킬 변경은 재시작 후 반영(MEDIUM, `[로컬에서 정확한 동작 확인 필요]` 포함).
    - 주의 2: 번들 스킬 수는 버전마다 다름(89 vs 118).
    - 주의 3: `hermes skills install`은 설치 전 보안 스캔 수행, `--force`는 안전한 항목에만.

13. "## 다음 단계" — 다음 [Ch.10 학습 루프](../10-learning-loop/index.md)로 연결 (스킬을 어떻게 자동 생성·개선하는 Do→Learn→Improve 사이클로 이어짐). 이전 [Ch.8 터미널 백엔드](../08-backends/index.md).

제약:
- RESEARCH.md에 없는 수치/명령/플래그를 만들지 말 것.
- "5회 툴 호출마다 스킬 생성", "118개 번들 스킬", "19,932+ 커뮤니티 스킬"을 **사실로 단언 금지**.
- 모든 실제 실행 명령/설정 코드블록 첫 줄은 `# 검증: hermes rolling, 2026-06-10`. 디렉터리 트리·SKILL.md 형식 예시는 설명용이지만 SKILL.md 형식 코드블록과 디렉터리 트리에도 RESEARCH.md대로 첫 줄 주석을 둔다(RESEARCH가 그렇게 제시함). 순수 ASCII 다이어그램만 주석 생략 가능.
  </action>
  <verify>스켈레톤 존재: `grep -q "placeholder" src/10-learning-loop/index.md && grep -q "placeholder" src/11-mcp/index.md && grep -q "placeholder" src/12-cron/index.md && grep -q "placeholder" src/13-subagents/index.md` 통과.
Ch.9 핵심 사실: `grep -q "SKILL.md" src/09-skills/index.md && grep -q "agentskills" src/09-skills/index.md && grep -q "89" src/09-skills/index.md` 통과.
직접 작성 실습: `grep -q "When to Use" src/09-skills/index.md && grep -q "hermes skills list" src/09-skills/index.md` 통과.
금지 수치 부재: `! grep -q "19,932" src/09-skills/index.md` 통과. `! grep -q "5회.*툴 호출" src/09-skills/index.md` 통과(자동 생성 트리거를 사실로 단언하지 않음).
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-10" src/09-skills/index.md` 통과.
LOW placeholder + 헤지: `grep -q "로컬 실행 후 캡처 필요" src/09-skills/index.md && grep -q "검증 필요" src/09-skills/index.md` 통과.</verify>
  <done>09-skills/index.md가 SKILL.md 형식·저장 구조·직접 작성 실습·자동 생성 제안(헤지)·관리 CLI·번들 89개를 담고, 금지 수치/단언이 없으며, 10~13 스켈레톤이 placeholder로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. SUMMARY.md가 링크하는 09~13 챕터 파일은 이 플랜에서 모두(09 본문 + 10~13 스켈레톤) 생성했으므로 빌드가 통과해야 한다. exit code 0이면 통과. stderr/stdout에 "error"가 포함되면 실패로 보고하고 원인을 진단한다.

만약 mdBook이 링크된 파일 누락으로 비-제로 종료하면, 폴백으로 누락 디렉터리에 최소 placeholder를 생성한다 (Wave 2 플랜이 본문으로 덮어씀):
```bash
# 검증: shell, 2026-06-10
for d in 10-learning-loop 11-mcp 12-cron 13-subagents; do
  mkdir -p "src/$d"
  [ -s "src/$d/index.md" ] || printf '# placeholder\n' > "src/$d/index.md"
done
```
이 폴백을 적용했다면 보고한다.
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음. `test -f src/09-skills/index.md && test -f src/10-learning-loop/index.md && test -f src/11-mcp/index.md && test -f src/12-cron/index.md && test -f src/13-subagents/index.md` 통과.</verify>
  <done>mdbook build가 오류 없이 완료되고 09~13 챕터 파일이 모두 존재한다.</done>
</task>

</tasks>

<verification>
- src/SUMMARY.md가 기초 4 + 핵심 개념 5 + 학습과 자동화 5 = 14개 챕터 링크(중복 경로 없음)를 가진다.
- src/09-skills/index.md가 SKILL.md 형식·저장 구조·직접 작성 실습·자동 생성 제안 개념(검증 필요 헤지)·관리 CLI를 담는다.
- 번들 스킬 89개로 기술, 118/19,932 단언 없음, "5회 툴 호출" 트리거 단언 없음.
- 모든 명령/설정 코드블록에 `# 검증: hermes rolling, 2026-06-10` 주석.
- LOW 출력은 placeholder + 검증 필요 callout.
- 10~13 스켈레톤 placeholder 존재.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.9를 읽고 SKILL.md 형식으로 직접 스킬을 작성할 수 있고 자동 생성 트리거 개념을 이해한다 (LEARN-01, 성공 기준 #1). SUMMARY.md 스켈레톤이 후속 4개 챕터 플랜의 병렬 실행 기반을 제공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/04-learning-automation/04-01-SUMMARY.md`
</output>
