---
phase: 01-scaffold-ci
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/deploy.yml
autonomous: true

must_haves:
  truths:
    - "A GitHub Actions workflow exists that builds the mdBook with version 0.5.3 and deploys to GitHub Pages"
    - "The workflow installs mdBook via pre-built binary (not cargo install)"
    - "The workflow uses the actions/configure-pages + upload-pages-artifact + deploy-pages flow (not a gh-pages branch)"
    - "The workflow declares the permissions deploy-pages requires (pages: write, id-token: write)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline: build mdBook 0.5.3 + deploy to Pages"
      contains: "MDBOOK_VERSION: 0.5.3"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "mdBook 0.5.3 release binary"
      via: "curl download in install step"
      pattern: "mdbook-.*x86_64-unknown-linux-gnu.tar.gz"
    - from: ".github/workflows/deploy.yml build job"
      to: "deploy job"
      via: "upload-pages-artifact -> deploy-pages"
      pattern: "actions/deploy-pages@v5"
---

<objective>
Create the GitHub Actions workflow that builds the mdBook (pinned to 0.5.3 via pre-built binary) and deploys the output to GitHub Pages using the official configure-pages / upload-pages-artifact / deploy-pages flow.

Purpose: Automated deploy means every content chapter added in later phases lands on the public URL on `git push` to main, with no manual build/upload.
Output: .github/workflows/deploy.yml — a complete, version-pinned CI/CD pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-ci/01-RESEARCH.md

Satisfies requirement: INFRA-03 (GitHub Actions builds mdBook + deploy-pages auto-deploy).

Depends on 01-01: the workflow builds the book.toml/src/ scaffold created there. Confirm `book.toml` exists before authoring (the workflow's `mdbook build` step targets it).

CRITICAL constraints from research (do not violate):
- Override mdBook to 0.5.3. The official starter workflow pins 0.4.36 via `cargo install` — do NOT copy that. Use the pre-built binary download (saves 2-3 min, no Rust toolchain).
- Pin action versions: checkout@v4, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v5. v1/v2 of deploy-pages are deprecated and will error.
- permissions block MUST include `pages: write` and `id-token: write` (required by deploy-pages); keep `contents: read` (needed by checkout).
- This is the artifact-based flow (configure-pages -> upload-pages-artifact -> deploy-pages). Do NOT use a gh-pages branch or peaceiris actions.
- Trigger on push to `main` (the starter's `$default-branch` placeholder must become `main`) plus `workflow_dispatch`.
- This plan only WRITES the workflow file. It does NOT push or deploy — the GitHub remote does not exist yet and Pages source is not enabled. Live deploy verification happens in 01-03 (which contains manual GitHub-account steps).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .github/workflows/deploy.yml</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` with EXACTLY this verified content (mdBook 0.5.3, pre-built binary, official Pages artifact flow):

```yaml
name: Deploy mdBook to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MDBOOK_VERSION: 0.5.3
    steps:
      - uses: actions/checkout@v4

      - name: Install mdBook (pre-built binary)
        run: |
          tag="v${MDBOOK_VERSION}"
          url="https://github.com/rust-lang/mdBook/releases/download/${tag}/mdbook-${tag}-x86_64-unknown-linux-gnu.tar.gz"
          mkdir mdbook
          curl -sSL "$url" | tar -xz --directory=./mdbook
          echo "$(pwd)/mdbook" >> $GITHUB_PATH

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build with mdBook
        run: mdbook build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

Note the release-download URL uses `rust-lang/mdBook` (capital B). Write the file exactly as above.
  </action>
  <verify>`test -f .github/workflows/deploy.yml`; `grep -c 'MDBOOK_VERSION: 0.5.3' .github/workflows/deploy.yml` returns 1; `grep -c 'actions/deploy-pages@v5' .github/workflows/deploy.yml` returns 1; `grep -c 'pages: write' .github/workflows/deploy.yml` returns 1 and `grep -c 'id-token: write' .github/workflows/deploy.yml` returns 1; `grep -Ec 'cargo install' .github/workflows/deploy.yml` returns 0</verify>
  <done>deploy.yml exists with mdBook 0.5.3 pinned, pre-built binary install (no cargo install), the configure-pages/upload-pages-artifact/deploy-pages flow, and pages:write + id-token:write permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Validate workflow YAML syntax</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Confirm the workflow is syntactically valid YAML so it will be picked up by GitHub Actions. Use whatever is available on the machine, in order of preference:
1. `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy.yml'))"` (PyYAML)
2. If PyYAML is unavailable, `python3 -c "import sys; ... "` is not reliable; try `yq eval '.' .github/workflows/deploy.yml > /dev/null` if `yq` exists.
3. If neither is available, do a structural sanity check: confirm the file has top-level `name:`, `on:`, `permissions:`, and `jobs:` keys via grep, and that `build:` and `deploy:` jobs are present.

Report which method was used. Do not attempt to actually run the workflow (no remote exists yet).
  </action>
  <verify>The chosen validation method reports no error. At minimum: `grep -c '^jobs:' .github/workflows/deploy.yml` returns 1, and both `  build:` and `  deploy:` appear.</verify>
  <done>deploy.yml parses as valid YAML (or passes structural checks) and contains the build and deploy jobs.</done>
</task>

</tasks>

<verification>
- .github/workflows/deploy.yml exists and is valid YAML.
- mdBook pinned to 0.5.3 via pre-built binary; no `cargo install`.
- Uses configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v5, checkout@v4.
- permissions: contents:read, pages:write, id-token:write.
- Triggers on push to main + workflow_dispatch.
</verification>

<success_criteria>
- INFRA-03: workflow builds mdBook 0.5.3 and deploys via deploy-pages. (Live trigger/deploy is verified in 01-03 — this plan establishes the pipeline file.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-ci/01-02-SUMMARY.md`.
</output>
