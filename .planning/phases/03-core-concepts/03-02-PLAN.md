---
phase: 03-core-concepts
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/05-context-files/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.5 컨텍스트 파일 챕터가 mdBook에서 렌더링된다 (05-context-files/index.md, 빌드 오류 없음)"
    - "Ch.5가 컨텍스트 파일 우선순위 캐스케이드(.hermes.md → AGENTS.md → CLAUDE.md → .cursorrules, 첫 매치만 로드)를 설명한다"
    - "Ch.5가 SOUL.md는 ~/.hermes/SOUL.md에 전역으로 항상 로드되며 컨텍스트 파일과 별개임을 구분한다"
    - "Ch.5가 /personality 14개 내장 + config.yaml agent.personalities 커스텀을 다룬다"
    - "Ch.5가 .env(비밀) vs config.yaml(설정) 분리와 ${VAR} 치환 문법을 다룬다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-09' 주석으로 시작한다"
    - "검증 안 된 출력(배너 컨텍스트 로드 메시지)은 placeholder + 검증 필요 callout으로 표시된다"
  artifacts:
    - path: "src/05-context-files/index.md"
      provides: "Ch.5 컨텍스트 파일 본문 (계층/우선순위 + SOUL.md + /personality + .env 분리)"
      contains: ".hermes.md"
      min_lines: 90
  key_links:
    - from: "src/05-context-files/index.md"
      to: "src/06-memory/index.md"
      via: "다음 단계 mdBook 링크"
      pattern: "06-memory/index.md"
---

<objective>
Ch.5 "컨텍스트 파일" 본문을 작성한다 — 컨텍스트 파일 계층/우선순위, SOUL.md 구분, /personality 설정, .env/config.yaml 분리.

Purpose: 독자가 `.hermes.md`/`AGENTS.md`/`CLAUDE.md`/`.cursorrules` 계층과 SOUL.md를 이해하고 `/personality`로 페르소나를 설정할 수 있게 한다 (성공 기준 #2 / CORE-02).
Output: src/05-context-files/index.md (Ch.5 본문).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-concepts/03-RESEARCH.md
@.planning/phases/03-core-concepts/03-01-SUMMARY.md

이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (03-01이 단독 소유). 오직 src/05-context-files/index.md만 작성한다. 03-01이 SUMMARY에 05 항목을 이미 링크했고 mdBook이 빈 파일을 만들어 두었으므로 이 플랜은 그 파일을 본문으로 overwrite한다.

스타일 기준: src/01-install/index.md(Phase 2 완성 챕터)와 동일. 한국어. 구조: 개요 → 개념 → 실습 → 흔한 오류/주의 → 다음 단계. MANDATORY 주석: 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-09`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.5 컨텍스트 파일 본문 작성</name>
  <files>src/05-context-files/index.md</files>
  <action>
src/05-context-files/index.md를 본문으로 overwrite한다. RESEARCH.md "Ch.5: 컨텍스트 파일" 섹션의 HIGH confidence 사실만 사용. 한국어.

포함해야 할 내용 (모두 RESEARCH.md 근거):

1. 제목: `# 컨텍스트 파일`. 상단 "이 챕터를 시작하기 전에" callout (선행: Ch.4 에이전트 루프 — 3티어 프롬프트 개념).

2. "## 개요" — 컨텍스트 파일이 프롬프트의 Context 티어에 주입되어 프로젝트별 지시어를 에이전트에 전달하는 방법임. SOUL.md(전역 정체성)와 프로젝트 컨텍스트 파일의 차이를 이 챕터에서 명확히 한다.

3. "## 개념: 컨텍스트 파일 우선순위 캐스케이드" — RESEARCH.md "Context File Hierarchy & Precedence" 표 그대로(우선순위 1~4: `.hermes.md`/`HERMES.md` → `AGENTS.md` → `CLAUDE.md` → `.cursorrules`). **세션당 첫 매치 하나만 로드됨**을 강조. `.hermes.md`는 CWD에서 git root까지 상위 탐색하지만 나머지는 CWD 전용임을 명시. 하위 디렉터리 AGENTS.md의 점진적 발견(progressive discovery)은 한 단락으로 요약(에이전트가 툴 호출로 하위 디렉터리 진입 시 lazy하게 발견, 세션당 1회, 부모로 상위 탐색, 프롬프트 캐시 안정성 보존).

4. "## 개념: SOUL.md — 전역 에이전트 정체성" — RESEARCH.md "SOUL.md distinction": `~/.hermes/SOUL.md`(또는 `$HERMES_HOME/SOUL.md`)에 위치, 프로젝트 디렉터리에 두지 않음. 레이어 1(정체성 슬롯)로 모든 것보다 먼저 로드. 없으면 기본값 시드, 있으면 덮어쓰지 않음. 전역 — 모든 프로젝트에서 동일 페르소나. 컨텍스트 파일과 다름을 명확히.

5. "## 개념: 컨텍스트 파일 처리 규칙" — RESEARCH.md "Context File Contents & Processing": 20,000자 한도(초과 시 head 70% + tail 20% 절단), 보안 스캔("ignore previous instructions", `curl ... $API_KEY` 패턴 차단), YAML frontmatter 제거 후 주입, `# Project Context` 헤더 아래 주입.

6. "## 실습: .hermes.md 만들기" — RESEARCH.md "Exact Commands for Ch.5 — Hands-on" 코드블록 그대로(첫 줄 `# 검증: hermes rolling, 2026-06-09`): cd → `cat > .hermes.md << 'EOF' ... EOF`(Python 3.11/pytest/한국어 커밋 예시) → `hermes`. 컨텍스트 로드 확인 메시지는 LOW confidence이므로 코드블록 안에 `# [hermes 시작 배너에서 컨텍스트 파일 로드 메시지 확인 필요 — 로컬 캡처 필요]` placeholder, 그 아래 `> 검증 필요:` callout으로 "컨텍스트 파일 로드를 알리는 정확한 배너 메시지(있다면)는 문서화되어 있지 않으므로 로컬 실행 후 확인·교체 필요" 명시.
   - SOUL.md 확인/편집 코드블록(`# 검증: ...`): `cat ~/.hermes/SOUL.md`, `$EDITOR ~/.hermes/SOUL.md`.
   - 현재 설정 확인 코드블록(`# 검증: ...`): `hermes config show`.

7. "## 개념: /personality 명령" — RESEARCH.md "/personality Command": 문법 `/personality [name]`. 14개 내장 페르소나 표 그대로(helpful/concise/technical/creative/teacher/kawaii/catgirl/pirate/shakespeare/surfer/noir/uwu/philosopher/hype). 세션 스코프 — 세션 종료 시 리셋, SOUL.md를 수정하지 않음을 명시.
   - 커스텀 페르소나 config.yaml 코드블록(`# 검증: hermes rolling, 2026-06-09`): RESEARCH.md "Custom personalities in config.yaml"의 `agent.personalities.codereviewer` 예시 그대로. 전환: `/personality codereviewer`.

8. "## 개념: .env vs config.yaml 분리" — RESEARCH.md ".env vs config.yaml Separation": 해결 순서(CLI args → config.yaml → .env → 기본값). 분리 규칙 표(config.yaml=비밀 외 설정 / .env=비밀만 / auth.json=OAuth). `hermes config set` 라우팅 코드블록(`# 검증: ...`): model→config.yaml, OPENROUTER_API_KEY→.env 자동 라우팅, terminal.backend→config.yaml. 환경변수 치환 코드블록(`# 검증: ...`): `auxiliary.vision.api_key: ${GOOGLE_API_KEY}` (중괄호 필수).

9. "## 흔한 오류 / 주의" — RESEARCH.md "Pitfall Callouts for Ch.5" 4개 callout 그대로:
   - 주의 1: 컨텍스트 파일은 세션 시작 시 한 번만 로드(실행 중 수정 미반영, 재시작 필요).
   - 주의 2: 우선순위 첫 매치만 로드(.hermes.md와 AGENTS.md 공존 시 .hermes.md만 로드, 합치려면 .hermes.md로 병합).
   - 주의 3: API 키는 config.yaml에 저장 금지(${VAR}로 참조해도 키는 .env에).
   - 주의 4: 변수 치환 문법(${VAR_NAME} 필수, $VAR_NAME은 치환 안 됨).

10. "## 다음 단계" — 이전: `[Ch.4 에이전트 루프](../04-agent-loop/index.md)`, 다음: `[Ch.6 메모리](../06-memory/index.md)`.

제약:
- RESEARCH.md에 없는 사실/수치를 만들지 말 것 (특히 페르소나 개수는 정확히 14, 컨텍스트 파일 우선순위 순서는 RESEARCH.md 표 그대로).
- 모든 명령/설정 코드블록 첫 줄에 `# 검증: hermes rolling, 2026-06-09`. config.yaml YAML 블록도 설정이므로 주석 필수.
- 컨텍스트 로드 배너 메시지는 단정하지 말고 placeholder + 검증 필요 callout으로만.
  </action>
  <verify>`grep -q ".hermes.md" src/05-context-files/index.md && grep -q "AGENTS.md" src/05-context-files/index.md && grep -q "CLAUDE.md" src/05-context-files/index.md && grep -q ".cursorrules" src/05-context-files/index.md` 통과.
`grep -q "SOUL.md" src/05-context-files/index.md && grep -q "personality" src/05-context-files/index.md && grep -q "personalities" src/05-context-files/index.md` 통과.
14개 페르소나 표 일부 확인: `grep -q "shakespeare" src/05-context-files/index.md && grep -q "philosopher" src/05-context-files/index.md` 통과.
.env 분리/치환: `grep -q "config.yaml" src/05-context-files/index.md && grep -q '${GOOGLE_API_KEY}' src/05-context-files/index.md` 통과 (작은따옴표로 변수 치환 방지).
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-09" src/05-context-files/index.md` 통과.
LOW placeholder: `grep -q "로컬 캡처 필요" src/05-context-files/index.md` 통과.</verify>
  <done>05-context-files/index.md가 컨텍스트 파일 우선순위·SOUL.md 구분·/personality 14개·커스텀 설정·.env/config.yaml 분리·4개 주의 callout을 담은 Ch.5 본문이다.</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. exit code 0이면 통과. 출력에 "error" 문자열이 포함되면 실패로 보고하고 원인을 진단한다. SUMMARY.md는 수정하지 않았음을 확인(03-01 소유).
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음. `git diff --name-only src/SUMMARY.md` 출력 없음(SUMMARY.md 미변경).</verify>
  <done>mdbook build가 오류 없이 완료되고 SUMMARY.md는 변경되지 않았다.</done>
</task>

</tasks>

<verification>
- src/05-context-files/index.md가 컨텍스트 파일 우선순위 캐스케이드·SOUL.md 전역 구분·/personality 14개·.env/config.yaml 분리·${VAR} 치환을 담는다.
- 모든 명령/설정 코드블록에 `# 검증: hermes rolling, 2026-06-09` 주석이 있다.
- 배너 컨텍스트 로드 메시지는 placeholder + 검증 필요 callout으로 표시된다.
- SUMMARY.md는 수정되지 않았다.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.5를 읽고 `.hermes.md`/`AGENTS.md`/`SOUL.md` 계층을 이해하고 `/personality`로 페르소나를 설정할 수 있다 (CORE-02, 성공 기준 #2).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-concepts/03-02-SUMMARY.md`
</output>
