---
phase: 03-core-concepts
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/08-backends/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.8 터미널 백엔드 챕터가 mdBook에서 렌더링된다 (08-backends/index.md, 빌드 오류 없음)"
    - "Ch.8가 6개 백엔드(local/docker/ssh/singularity/modal/daytona) 개요를 표로 제시한다"
    - "Ch.8가 백엔드 전환 3가지 방법(config.yaml / hermes config set / hermes setup terminal)을 다룬다"
    - "Ch.8가 Docker 보안 하드닝이 자동 적용됨(--cap-drop ALL, --pids-limit 256 등)을 설명하고 Docker 핸즈온을 포함한다"
    - "Ch.8가 docker_run_as_host_user 파일 소유권 주의(A-13)를 다룬다"
    - "Ch.8가 Modal/Daytona/Singularity 키를 MEDIUM 신뢰도로 표시하고 로컬 검증 placeholder를 단다"
    - "Phase 5 심화 배포 참조는 plain prose만 사용하고 존재하지 않는 파일로의 mdBook 링크를 만들지 않는다"
    - "모든 명령/설정 코드블록이 '# 검증: ...' 주석으로 시작한다"
  artifacts:
    - path: "src/08-backends/index.md"
      provides: "Ch.8 터미널 백엔드 개요 본문 (6 백엔드 + 전환 + Docker 핸즈온)"
      contains: "docker"
      min_lines: 90
  key_links:
    - from: "src/08-backends/index.md"
      to: "Phase 5 (배포)"
      via: "plain prose 전방 참조 (링크 아님)"
      pattern: "Phase 5"
---

<objective>
Ch.8 "터미널 백엔드" 개요 본문을 작성한다 — 6개 백엔드 개요, 전환 방법, Docker 보안 하드닝 + 핸즈온. 심화 배포는 Phase 5 범주임을 prose로만 전방 참조.

Purpose: 독자가 각 백엔드의 용도를 이해하고 Docker 백엔드로 전환해 로컬 샌드박싱을 실습할 수 있게 한다 (성공 기준 #5 / PLAT-02 배경, overview 수준).
Output: src/08-backends/index.md (Ch.8 본문).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-concepts/03-RESEARCH.md
@.planning/phases/03-core-concepts/03-01-SUMMARY.md

이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (03-01 단독 소유). 오직 src/08-backends/index.md만 작성. mdBook이 만든 빈 08 파일을 본문으로 overwrite.

스타일 기준: src/01-install/index.md와 동일. 한국어. 구조: 개요 → 개념 → 실습 → 흔한 오류/주의 → 다음 단계. MANDATORY 주석: 모든 명령/설정 코드블록 첫 줄 `# 검증: ...`.
중요: 이 챕터는 overview 전용 — 심화 배포는 Phase 5. Phase 5 전방 참조는 **plain prose만** 사용하고 존재하지 않는 Phase 5 파일로의 mdBook 상대 링크를 절대 만들지 말 것.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.8 터미널 백엔드 본문 작성</name>
  <files>src/08-backends/index.md</files>
  <action>
src/08-backends/index.md를 본문으로 overwrite한다. RESEARCH.md "Ch.8: 터미널 백엔드" 섹션 사용. HIGH confidence는 단정, MEDIUM/LOW는 검증 필요 callout/placeholder로 표시. 한국어.

포함해야 할 내용 (모두 RESEARCH.md 근거):

1. 제목: `# 터미널 백엔드`. 상단 "이 챕터를 시작하기 전에" callout (선행: Ch.7 승인 모드 — 컨테이너 백엔드가 보안 경계 역할). 이 챕터는 **개요**이며 심화 배포는 이후 단계(Phase 5)에서 다룸을 plain prose로 명시(링크 금지).

2. "## 개요" — 터미널 백엔드는 에이전트의 터미널/파일 툴이 실제로 실행되는 환경을 결정. 이 챕터에서 6개 백엔드의 용도를 이해하고 Docker로 전환하는 실습을 한다.

3. "## 개념: 6개 백엔드" — RESEARCH.md "The Six Backends" 표 그대로(local/docker/ssh/singularity/modal/daytona + 격리 수준/파일 지속성/주요 용도/환경 요구사항). Modal(에페머럴, 서버리스, cold start)·Daytona(히버네이션, 세션 간 유지) 특이사항 한 줄씩.

4. "## 개념: 백엔드 선택·전환" — RESEARCH.md "How to Select / Switch Backend" 3가지 방법:
   - 방법 1 config.yaml 코드블록(`# 검증: hermes rolling, 2026-06-09`): `terminal.backend: docker`(주석으로 가능 값 나열).
   - 방법 2 코드블록(`# 검증: ...`): `hermes config set terminal.backend docker` / `... local`(되돌리기).
   - 방법 3 코드블록(`# 검증: ...`): `hermes setup terminal`(인터랙티브 마법사). 정확한 마법사 흐름은 MEDIUM이므로 `> 검증 필요:` callout으로 "정확한 마법사 상호작용은 백엔드별로 다르며 로컬 실행 후 확인 필요"(Open Question #3) 명시.
   - Docker 상태 확인 코드블록(`# 검증: 로컬 Docker 설치, 2026-06-09`): `docker info`.

5. "## 개념: Docker 백엔드 보안 하드닝" — RESEARCH.md "Docker security hardening": 텍스트 블록으로 자동 적용 플래그 나열(--cap-drop ALL / --cap-add DAC_OVERRIDE,CHOWN / --security-opt no-new-privileges / --pids-limit 256 / --tmpfs ...). **독자가 직접 설정할 필요 없음 — Hermes가 자동 적용**임을 강조. (이 플래그 목록은 명령이 아닌 설명용 텍스트이므로 코드 펜스로 두되 `# 검증:` 불필요.)
   - Docker 전체 config 코드블록(`# 검증: hermes rolling, 2026-06-09`): RESEARCH.md "Docker backend config.yaml 전체 설정" 그대로(docker_image 기본값, docker_run_as_host_user, docker_forward_env, docker_env, docker_volumes, docker_extra_args, container_cpu/memory/disk, container_persistent 등).

6. "## 실습: Docker 백엔드로 전환하기" — RESEARCH.md "최소 Docker 핸즈온" 코드블록(첫 줄 `# 검증: hermes rolling + Docker 설치, 2026-06-09`): backend를 docker로 set → hermes 실행(첫 툴 실행 시 컨테이너 자동 시작) → 채팅에서 파이썬 버전 확인 → local로 되돌리기.
   - 첫 실행 컨테이너 빌드/풀 메시지는 LOW이므로 placeholder + `> 검증 필요:` callout(Open Question #4): 코드블록 안에 `# [첫 Docker 백엔드 실행 시 컨테이너 빌드/풀 메시지 — 로컬 실행 후 캡처 필요]`.

7. "## 개념: 원격·클라우드 백엔드 (개요)" — RESEARCH.md의 SSH/Modal/Daytona/Singularity 섹션을 **개요 수준**으로. 각 백엔드의 핵심 config 키와 필요 자격증명만 제시:
   - SSH: env var(`TERMINAL_SSH_HOST`/`TERMINAL_SSH_USER`, 선택 PORT/KEY) 코드블록(`# 검증: ...`) + `terminal.backend: ssh`/`persistent_shell: true`. ControlMaster tar 동기화 한 줄.
   - Modal: config 코드블록(`# 검증: ...`)(backend: modal + container_*). 자격증명 `MODAL_TOKEN_ID`/`~/.modal.toml`. "Managed mode"(NousResearch가 리소스 사전 공급)는 MEDIUM이므로 `> 검증 필요:` callout(Open Question #5): "managed mode 정확한 설정 흐름은 공개 문서에 충분히 기술되지 않아 로컬 검증 필요".
   - Daytona: config 코드블록(`# 검증: ...`)(backend: daytona + container_disk 최대 10GB). 자격증명 `DAYTONA_API_KEY`. SDK가 서버 URL 자동 처리.
   - Singularity: config 코드블록(`# 검증: ...`)(backend: singularity). `apptainer`/`singularity` PATH 필요, HPC 전용. 스크래치 디렉터리 해결 순서.
   - 이 섹션 전체 상단에 `> 검증 필요:` callout으로 "Modal/Daytona/Singularity의 정확한 YAML 키와 설정 흐름은 MEDIUM 신뢰도이며 게재 전 로컬 검증이 필요하다"(Open Question #8, STATE.md 우려 carry-forward) 명시. 단정적 사실 서술 금지.

8. "## 흔한 오류 / 주의" — RESEARCH.md "Pitfall Callouts for Ch.8" 4개 callout 그대로:
   - 주의 1: Docker 데몬 미실행(`docker info`로 확인, 미실행 시 "Cannot connect to Docker daemon" 오류).
   - 주의 2: docker_run_as_host_user와 파일 소유권(A-13, CRITICAL — false 기본값 시 컨테이너 생성 파일이 root 소유, 편집/삭제에 sudo 필요; true로 호스트 UID 실행하면 해결되나 컨테이너 내 apt install이 권한 오류로 실패 가능).
   - 주의 3: Modal 에페머럴 특성(LOW — 세션 종료 시 파일 소실 가능, container_persistent: true, 스냅샷 정확 동작 로컬 검증 필요).
   - 주의 4: approvals.mode: off는 컨테이너 백엔드에서만 합리적(A-12 — local 백엔드 절대 off 금지). (Ch.7과 일관.)

9. "## 다음 단계" — 이전: `[Ch.7 툴 게이트웨이](../07-tools/index.md)`. 그리고 plain prose로 "터미널 백엔드를 활용한 실제 배포(VPS/서버리스 상세, 항상 켜진 에이전트)는 이후 배포 단계에서 자세히 다룹니다." (Phase 5로의 mdBook 링크 만들지 말 것 — 파일 미존재.)

제약:
- Phase 5 전방 참조는 plain prose만. `[...](../09-... )` 같은 상대 링크 금지(파일 없음 → 빌드 깨짐 위험/죽은 링크).
- Modal/Daytona/Singularity는 MEDIUM — 단정 금지, 검증 필요 callout 동반.
- Docker 보안 플래그는 Hermes 자동 적용이며 독자 책임이 아님을 명확히.
- 모든 명령/설정 YAML 코드블록 첫 줄에 `# 검증: ...`. 자동 적용 보안 플래그 목록은 설명용 텍스트라 예외.
  </action>
  <verify>6 백엔드: `grep -q "local" src/08-backends/index.md && grep -q "docker" src/08-backends/index.md && grep -q "ssh" src/08-backends/index.md && grep -q "singularity" src/08-backends/index.md && grep -q "modal" src/08-backends/index.md && grep -q "daytona" src/08-backends/index.md` 통과.
전환/하드닝: `grep -q "hermes config set terminal.backend" src/08-backends/index.md && grep -q "hermes setup terminal" src/08-backends/index.md && grep -q "cap-drop" src/08-backends/index.md && grep -q "docker_run_as_host_user" src/08-backends/index.md` 통과.
Phase 5 prose 참조 존재: `grep -q "Phase 5" src/08-backends/index.md` 통과. Phase 5로의 mdBook 링크 부재: `! grep -q "](../09" src/08-backends/index.md && ! grep -q "](../10" src/08-backends/index.md` 통과.
검증 필요 callout(MEDIUM/LOW): `grep -q "검증 필요" src/08-backends/index.md && grep -q "로컬 실행 후 캡처 필요" src/08-backends/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-09" src/08-backends/index.md` 통과.</verify>
  <done>08-backends/index.md가 6 백엔드 개요·전환 3방법·Docker 하드닝/핸즈온·원격 백엔드 개요·4개 주의 callout을 담은 Ch.8 본문이며, MEDIUM/LOW 항목은 검증 callout으로 표시되고 Phase 5 참조는 prose만 사용한다.</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. 출력에 "error" 문자열이 포함되면 실패로 보고하고 원인을 진단한다. 죽은 링크(Phase 5 파일)가 없는지 확인. SUMMARY.md는 수정하지 않았음을 확인.
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음. `git diff --name-only src/SUMMARY.md` 출력 없음.</verify>
  <done>mdbook build가 오류 없이 완료되고 SUMMARY.md는 변경되지 않았다.</done>
</task>

</tasks>

<verification>
- src/08-backends/index.md가 6 백엔드 개요표·전환 3방법·Docker 하드닝(자동)·Docker 핸즈온·원격 백엔드 개요를 담는다.
- docker_run_as_host_user 파일 소유권 주의(A-13)가 포함된다.
- Modal/Daytona/Singularity가 MEDIUM 신뢰도로 표시되고 검증 필요 callout이 동반된다.
- Phase 5 전방 참조는 plain prose만 사용하고 죽은 mdBook 링크가 없다.
- 모든 명령/설정 코드블록에 `# 검증: ...` 주석이 있다.
- SUMMARY.md는 수정되지 않았다.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.8을 읽고 6개 백엔드의 용도를 이해하고 Docker 백엔드로 전환해 로컬 샌드박싱을 실습할 수 있다 (성공 기준 #5, PLAT-02 배경).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-concepts/03-05-SUMMARY.md`
</output>
