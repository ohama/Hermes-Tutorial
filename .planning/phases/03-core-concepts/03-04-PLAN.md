---
phase: 03-core-concepts
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/07-tools/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.7 툴 게이트웨이 챕터가 mdBook에서 렌더링된다 (07-tools/index.md, 빌드 오류 없음)"
    - "Ch.7가 ~71개 툴/30+ 툴셋 카탈로그와 hermes tools 검색/설정 UI를 다룬다"
    - "Ch.7가 툴셋 비활성화를 toolsets 배열/hermes tools UI로 설명하고, 존재하지 않는 agent.disabled_toolsets 키를 사실로 문서화하지 않는다"
    - "Ch.7가 웹검색/이미지생성/TTS/브라우저 게이트웨이 기능과 use_gateway 설정을 다룬다"
    - "Ch.7가 승인 모드(manual/smart/off) + Tirith 보안 스캔을 다룬다"
    - "Ch.7가 Nous Tool Gateway 유료 경계를 명시하되 무료 풀 정확 수치를 단정하지 않는다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-09' 주석으로 시작한다"
  artifacts:
    - path: "src/07-tools/index.md"
      provides: "Ch.7 툴 게이트웨이 본문 (카탈로그 + hermes tools + 게이트웨이 + 승인 모드)"
      contains: "hermes tools"
      min_lines: 90
  key_links:
    - from: "src/07-tools/index.md"
      to: "src/08-backends/index.md"
      via: "다음 단계 mdBook 링크"
      pattern: "08-backends/index.md"
---

<objective>
Ch.7 "툴 게이트웨이" 본문을 작성한다 — 툴 카탈로그/툴셋, hermes tools 검색·설정, 웹검색/이미지/TTS/브라우저 게이트웨이, 승인 모드.

Purpose: 독자가 `hermes tools`로 도구를 탐색·설정하고, 게이트웨이 기능과 승인 모드를 이해하며, 무료/유료 경계를 알게 한다 (성공 기준 #4 / CORE-04).
Output: src/07-tools/index.md (Ch.7 본문).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-concepts/03-RESEARCH.md
@.planning/phases/03-core-concepts/03-01-SUMMARY.md

이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (03-01 단독 소유). 오직 src/07-tools/index.md만 작성. mdBook이 만든 빈 07 파일을 본문으로 overwrite.

스타일 기준: src/01-install/index.md와 동일. 한국어. 구조: 개요 → 개념 → 실습 → 흔한 오류/주의 → 다음 단계. MANDATORY 주석: 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-09`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.7 툴 게이트웨이 본문 작성</name>
  <files>src/07-tools/index.md</files>
  <action>
src/07-tools/index.md를 본문으로 overwrite한다. RESEARCH.md "Ch.7: 툴 게이트웨이" 섹션의 HIGH confidence 사실만 사용. 한국어.

포함해야 할 내용 (모두 RESEARCH.md 근거):

1. 제목: `# 툴 게이트웨이`. 상단 "이 챕터를 시작하기 전에" callout (선행: Ch.4 툴 디스패치 개념).

2. "## 개요" — Hermes는 ~71개 내장 툴을 30+ 툴셋으로 묶어 제공. 이 챕터에서 카탈로그 탐색, 활성/비활성, 게이트웨이 기능, 승인 모드를 다룬다.

3. "## 개념: 툴 레지스트리와 툴셋" — RESEARCH.md "Tool Registry Overview": ~71개 툴/30+ 툴셋, 등록 여부는 설치 능력·활성 플랫폼에 따라 다름. "Key toolsets by category" 표 그대로(파일&터미널/웹/AI/에이전트/메모리/자동화/특수/플랫폼). **kanban 툴셋은 opt-in 전용** — `all`/`*` 와일드카드에도 미포함임을 명시.

4. "## 실습: hermes tools로 도구 탐색·설정" — RESEARCH.md "hermes tools Command" 코드블록들(각 첫 줄 `# 검증: hermes rolling, 2026-06-09`):
   - `hermes tools`(curses 인터랙티브 UI) / `hermes tools --summary`(요약 출력 후 종료).
   - 세션 내 명령(텍스트 블록 — 슬래시 커맨드는 실행 명령이 아니므로 일반 코드 펜스로): `/tools list` / `/tools disable browser` / `/tools enable homeassistant`.
   - 세션별 툴셋 선택 CLI 코드블록(`# 검증: ...`): `hermes chat --toolsets web,file,terminal` / `--toolsets debugging` / `--toolsets all`.
   - 커스텀 툴셋 config 코드블록(`# 검증: ...`): RESEARCH.md `custom_toolsets.data-science` 예시 그대로.
   - 툴셋 비활성화 config 코드블록(`# 검증: ...`): RESEARCH.md "Toolset disabling via config" — `toolsets:` 배열로 제한(예: `- hermes-cli`). **반드시** `agent.disabled_toolsets` 키를 사용하지 말 것 — 존재하지 않는 키이며 사실로 문서화 금지.

5. "## 개념: 툴 동시성" — RESEARCH.md "Tool Concurrency": 복수 툴 호출 → ThreadPoolExecutor(최대 8). 경로 스코프 동시성 안전(다른 경로 동시 호출 안전, 같은 경로는 레지스트리가 순서 관리). 인터랙티브 툴(clarify/todo/memory/session_search/delegate_task) 항상 순차. (Ch.4와 일관 — 한 단락으로.)

6. "## 개념: Nous Tool Gateway — 유료 기능" — RESEARCH.md "Nous Tool Gateway — Paid Feature":
   - 게이트웨이가 웹검색/이미지생성/TTS/브라우저를 구독 서비스로 라우팅(서비스별 개별 API 키 불필요).
   - "Free vs. Paid boundary" 표 그대로(추론은 무료/유료 모두 가능, Tool Gateway는 무료 풀(한도 있음)/유료 무제한).
   - **무료 풀 정확 수치를 단정하지 말 것** — RESEARCH.md Open Question #2. "정확한 무료 할당량은 문서화되어 있지 않으므로 Nous Portal 가격 페이지에서 현재 정책을 확인하라"고 안내하고, 그 앞에 `> 검증 필요:` callout으로 명시.
   - Nous Portal 명령 코드블록(`# 검증: ...`): `hermes setup --portal` / `hermes portal info` / `hermes portal tools`.

7. "## 개념: 게이트웨이 기능과 use_gateway 설정" — RESEARCH.md "Tool Gateway per-tool use_gateway" + "Built-in Tool Gateway Capabilities":
   - per-tool 설정 코드블록(`# 검증: ...`): `web.backend: firecrawl` + `use_gateway: true`(true=게이트웨이 라우팅/false=직접 키, true가 직접 키보다 우선).
   - 웹검색(Firecrawl), 이미지 생성(FAL.ai 9개 모델 표 — 기본값 fal-ai/flux-2/klein/9b 포함), TTS(OpenAI TTS voices), 브라우저(Browser Use 헤드리스 Chromium, browser_navigate/click/type/vision, 게이트웨이 사용 시 Browserbase 계정 불필요).

8. "## 개념: 승인 모드와 보안" — RESEARCH.md "Approval Mode" + Tirith:
   - 승인 config 코드블록(`# 검증: ...`): `approvals.mode: manual / timeout: 60 / cron_mode: deny / destructive_slash_confirm: true`.
   - 모드 표 그대로(manual 기본·가장 안전 / smart 보조 LLM 위험도 평가 / off 전체 비활성화 = HERMES_YOLO_MODE=true).
   - Hardline blocklist(off에서도 항상 차단: `rm -rf /`, 포크 폭탄, 마운트된 root의 `mkfs.*`).
   - Tirith 스캔 config 코드블록(`# 검증: ...`): `security.tirith_enabled: true / tirith_timeout: 5 / tirith_fail_open: true`. 탐지 대상(homograph URL, `curl | bash` 파이프, 터미널 인젝션).

9. "## 흔한 오류 / 주의" — RESEARCH.md "Pitfall Callouts for Ch.7" 3개 callout 그대로:
   - 주의 1: approvals.mode: off 위험성(A-12 — local 백엔드 절대 금지, 컨테이너 백엔드에서만 합리적, 활성화 시 빨간 "⚠ YOLO mode" 배너).
   - 주의 2: Tool Gateway 무료/유료 경계(A-7 — 무료는 추론 가능, 게이트웨이는 유료/무료 풀 한도, `hermes portal info`로 확인, 구독 없이 호출 시 "subscription required" 오류).
   - 주의 3: agent.disabled_toolsets 키 없음(MEDIUM — toolsets 배열/hermes tools UI 사용, 존재하지 않는 키는 조용히 무시됨).

10. "## 다음 단계" — 이전: `[Ch.6 메모리](../06-memory/index.md)`, 다음: `[Ch.8 터미널 백엔드](../08-backends/index.md)`.

제약:
- `agent.disabled_toolsets`를 동작하는 설정 키로 문서화 금지(존재하지 않음). 비활성화는 toolsets 배열 / hermes tools UI / `/tools disable`.
- 무료 풀 수치 단정 금지(Open Question #2) — 가격 페이지 확인 안내 + 검증 필요 callout.
- 모든 명령/설정/YAML 코드블록 첫 줄에 `# 검증: hermes rolling, 2026-06-09`.
- RESEARCH.md에 없는 툴/툴셋/모델을 만들지 말 것.
  </action>
  <verify>`grep -q "hermes tools" src/07-tools/index.md && grep -q "toolsets" src/07-tools/index.md && grep -q "use_gateway" src/07-tools/index.md` 통과.
승인/보안: `grep -q "approvals" src/07-tools/index.md && grep -q "manual" src/07-tools/index.md && grep -q "smart" src/07-tools/index.md && grep -q "Tirith" src/07-tools/index.md` 통과.
존재하지 않는 키를 동작 설정으로 쓰지 않음(언급은 주의 callout에서만): `grep -c "disabled_toolsets" src/07-tools/index.md` 결과가 1 이하이고, 등장한다면 "존재하지 않" 또는 "없" 문맥의 주의 callout 안에 있어야 한다(실행 검증은 `grep -A2 "disabled_toolsets" src/07-tools/index.md | grep -q "없"` 로 확인 — 키를 사용 권장하는 형태가 아님을 보장).
무료 풀 수치 미단정 + 검증 callout: `grep -q "검증 필요" src/07-tools/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-09" src/07-tools/index.md` 통과.</verify>
  <done>07-tools/index.md가 툴 카탈로그·hermes tools·게이트웨이 기능/use_gateway·승인 모드/Tirith·3개 주의 callout을 담은 Ch.7 본문이며, agent.disabled_toolsets를 동작 키로 문서화하지 않고 무료 풀 수치를 단정하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증</name>
  <files></files>
  <action>
프로젝트 루트에서 `mdbook build`를 실행해 exit 0과 무오류를 확인한다. 출력에 "error" 문자열이 포함되면 실패로 보고하고 원인을 진단한다. SUMMARY.md는 수정하지 않았음을 확인.
  </action>
  <verify>`mdbook build` exit 0; 출력에 "error" 문자열 없음. `git diff --name-only src/SUMMARY.md` 출력 없음.</verify>
  <done>mdbook build가 오류 없이 완료되고 SUMMARY.md는 변경되지 않았다.</done>
</task>

</tasks>

<verification>
- src/07-tools/index.md가 ~71툴/30+툴셋 카탈로그·hermes tools·use_gateway·승인 모드(manual/smart/off)·Tirith를 담는다.
- agent.disabled_toolsets를 동작 설정 키로 문서화하지 않는다(주의 callout에서만 "없음"으로 언급).
- Nous 무료 풀 정확 수치를 단정하지 않고 가격 페이지 확인을 안내한다.
- 모든 명령/설정 코드블록에 `# 검증: hermes rolling, 2026-06-09` 주석이 있다.
- SUMMARY.md는 수정되지 않았다.
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.7를 읽고 `hermes tools`로 검색/이미지/TTS/브라우저를 설정하고 승인 모드를 이해할 수 있다 (CORE-04, 성공 기준 #4).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-concepts/03-04-SUMMARY.md`
</output>
