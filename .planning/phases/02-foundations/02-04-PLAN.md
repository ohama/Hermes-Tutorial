---
phase: 02-foundations
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/03-model/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.3 모델 설정 챕터가 mdBook에서 렌더링된다 (03-model/index.md)"
    - "Ch.3가 hermes model 마법사와 hermes setup --portal로 공급자·API 키를 설정하는 방법을 설명한다"
    - "Ch.3가 핵심 공급자(Nous Portal/OpenRouter/Anthropic/OpenAI)와 인증 방식을 제시한다"
    - "Ch.3가 64K 최소 컨텍스트 요건과 ~13,935 고정 오버헤드를 설명한다"
    - "Ch.3가 display.language: ko 부분 한국어 지원을 정확히(정적 메시지만 번역, 응답/로그/오류는 영어) 설명한다"
    - "Ch.3가 5개 주의 callout(Claude Pro/Max≠API, setup 키 스킵 #16394, 키는 .env, 64K 미만, /model vs hermes model)을 포함한다"
    - "API 키는 ~/.hermes/.env, 비밀 외 설정은 config.yaml로 분리됨을 설명하고, 모든 명령 코드블록이 # 검증: 주석으로 시작한다"
  artifacts:
    - path: "src/03-model/index.md"
      provides: "Ch.3 모델 설정 본문 (공급자 + API 키 + 64K + 한국어 + 주의)"
      contains: "64,000"
  key_links:
    - from: "src/03-model/index.md"
      to: "src/02-first-run/index.md"
      via: "이전 챕터 링크"
      pattern: "02-first-run/index.md"
---

<objective>
Ch.3 "모델 설정" 본문을 작성한다: hermes model 마법사, setup --portal, 핵심 공급자·API 키 저장 위치, config.yaml 모델 섹션, 64K 컨텍스트 요건, display.language ko 부분 지원, 그리고 5개 CRITICAL/주의 callout.

Purpose: 독자가 공급자를 연결하고 API 키를 안전히 설정하며 64K 요건을 충족하는 모델을 선택할 수 있게 한다.
Output: src/03-model/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundations/02-RESEARCH.md

이 플랜은 SUMMARY.md를 건드리지 않는다 (02-01이 03-model/index.md 링크를 이미 등록). 오직 src/03-model/index.md만 생성. 디렉터리 없으면 생성.
RESEARCH.md "Ch.3: 모델 설정" 섹션이 모든 명령·표·config·주의의 출처다. STATE.md 정정사항(ko 부분 지원)을 반드시 반영한다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.3 본문 — 공급자 설정 마법사 + 공급자 표 + 키 저장 위치</name>
  <files>src/03-model/index.md</files>
  <action>
src/03-model/index.md 새로 작성 (디렉터리 없으면 mkdir). 한국어. RESEARCH "Chapter file template" 구조.

1. 제목 `# 모델 설정` + 시작 전 callout (선행: Ch.1 설치 완료; 공급자 API 키 또는 Nous Portal 계정 필요).

2. "## hermes model 마법사" — RESEARCH "hermes model Flow":
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes model
   ```
   대화형 마법사: 새 공급자 추가, API 키 설정, OAuth 흐름. **활성 채팅 세션 밖** 터미널에서 실행. 세션 내 /model은 이미 설정된 공급자 간 전환만 가능 (이 차이는 주의 5에서도 강조).

3. "## 가장 쉬운 시작: hermes setup --portal" — RESEARCH "hermes setup --portal":
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes setup --portal
   ```
   동시에 3가지: Nous Portal OAuth 로그인(브라우저 열림) / Nous를 공급자로 설정 / Tool Gateway 활성화. 신규 사용자 권장 경로. 완료 후 hermes 실행 시 배너에 Nous 모델 표시.
   (Nous Portal 무료/유료 경계는 RESEARCH Open Question #7로 LOW — 가격 세부를 단정하지 말 것. 필요 시 "공식 Nous Portal 요금 페이지 확인" 정도만 언급.)

4. "## 주요 공급자" — RESEARCH "Verified Providers" Tier 1 표 (Nous Portal=OAuth, OpenRouter=OPENROUTER_API_KEY, Anthropic=ANTHROPIC_API_KEY [Claude Pro/Max와 별개 — 주의], OpenAI=API 키). OpenAI 환경변수명은 RESEARCH에서 MEDIUM/LOW (관례상 OPENAI_API_KEY) — 표에 `OPENAI_API_KEY (관례; hermes model 마법사로 확인 권장)`처럼 불확실성을 표기하고 단정하지 말 것 (Open Question #4).
   Tier 2는 한 줄로만 name-drop: Google Gemini(OAuth 또는 GOOGLE_API_KEY/GEMINI_API_KEY), DeepSeek(DEEPSEEK_API_KEY), NVIDIA NIM(NVIDIA_API_KEY), Ollama(로컬, 키 불필요), Custom Endpoint(OpenAI 호환). 40+ 전체 목록은 공식 문서 참조.

5. "## API 키 저장 위치" — RESEARCH "API Key Storage":
   - 비밀(API 키) → ~/.hermes/.env
   - 비밀 외 설정 → ~/.hermes/config.yaml
   - OAuth 자격증명 → ~/.hermes/auth.json
   `hermes config set`이 값을 올바른 파일로 자동 라우팅; config.yaml을 수동 편집해 키를 넣지 말 것.
   저장 위치 확인:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   cat ~/.hermes/.env
   ```

모든 명령 코드블록 `# 검증:` 주석으로 시작. 불확실 항목(OpenAI 키 이름, Portal 가격)은 단정하지 말고 불확실성 표기.
  </action>
  <verify>`grep -q "hermes model" && grep -q "setup --portal" && grep -q "OPENROUTER_API_KEY" && grep -q "ANTHROPIC_API_KEY" && grep -q ".hermes/.env"` (src/03-model/index.md) 통과.</verify>
  <done>마법사·portal·공급자 표·키 저장 위치가 작성되고 불확실 항목이 표기됨.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.3 본문 — config.yaml 모델 섹션 + 64K 요건 + display.language ko</name>
  <files>src/03-model/index.md</files>
  <action>
Task 1 파일에 이어서 작성.

1. "## config.yaml 모델 섹션" — RESEARCH "config.yaml — Model Section":
   ```yaml
   # 검증: hermes rolling, 2026-06-09
   model:
     default: nous/hermes-3-405b   # 또는 provider/model-id 형식
     provider: nous                 # nous|openrouter|anthropic|openai|custom|auto
     context_length: 131072
     reasoning_effort: medium       # none|minimal|low|medium|high|xhigh
   ```
   현재 설정 확인:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes config show
   ```

2. "## 64K 최소 컨텍스트 요건" — RESEARCH "64K Context Requirement":
   "Hermes Agent는 최소 64,000 토큰의 컨텍스트 윈도우를 가진 모델이 필요합니다. 이보다 작은 모델은 시작 시 거부됩니다."
   이유: 매 호출마다 약 13,935 토큰 고정 오버헤드(도구 정의 ~8,759 + 시스템 프롬프트 ~5,176) 소비. 64K 미만은 실제 대화 공간이 거의 없음.
   대부분의 호스티드 모델(Claude, GPT, Gemini, Qwen, DeepSeek)은 쉽게 충족.
   Ollama는 명시적 컨텍스트 지정 필요 (LOW confidence — 플래그 구문 미확정이므로 placeholder/주의로):
   ```bash
   # 검증: ollama (버전 로컬 확인 필요), 2026-06-09
   # 예시: 64K 컨텍스트로 실행 (정확한 플래그는 로컬 검증 필요)
   ollama run llama3.2 --context-size 65536
   ```
   `> 검증 필요: Ollama 컨텍스트 플래그 구문(--context-size vs -c 65536)은 로컬에서 확인 후 확정하십시오.`

3. "## 한국어 UI (display.language: ko)" — RESEARCH "display.language Korean Support" (STATE.md 정정 반영):
   ko는 지원 목록에 포함됨. 전체 지원: en | zh | zh-hant | ja | de | es | fr | tr | uk | af | ko | it | ga | pt | ru | hu.
   단 범위가 좁음: CLI 승인 프롬프트, 일부 게이트웨이 슬래시 커맨드 응답 등 **소수의 정적 사용자 메시지만** 번역. 에이전트 응답·로그·도구 출력·오류 메시지는 영어 유지.
   ```yaml
   # 검증: hermes rolling, 2026-06-09
   display:
     language: ko    # 승인 프롬프트 등 일부 정적 메시지를 한국어로 번역
                     # 에이전트 응답·로그·도구 출력은 영어 유지
   ```
   `> 참고: 한국어(ko) 설정은 CLI 승인 프롬프트와 일부 게이트웨이 슬래시 커맨드 응답만 번역합니다. 에이전트의 실제 응답과 오류 메시지는 영어입니다.`
   **금지:** "한국어 미지원"이라고 쓰지 말 것. "완전한 한국어 UI"라고도 쓰지 말 것. 정확히 "부분 한국어 지원"으로.

모든 명령/config 코드블록 `# 검증:` 주석으로 시작. Ollama 플래그는 불확실성 표기.
  </action>
  <verify>`grep -q "64,000" && grep -q "13,935" && grep -q "language: ko" && grep -q "부분 한국어\|정적 메시지" && ! grep -q "한국어 미지원\|미지원" src/03-model/index.md` 식으로 64K·오버헤드·ko 부분지원 존재 & '미지원' 부재 확인 (육안 병행).</verify>
  <done>config.yaml 섹션 + 64K 요건 + ko 부분 지원이 정확히 작성됨.</done>
</task>

<task type="auto">
  <name>Task 3: Ch.3 본문 — 5개 주의 callout + 다음 단계 + 빌드 검증</name>
  <files>src/03-model/index.md</files>
  <action>
Task 2 파일에 이어 "## 흔한 오류 / 주의" 섹션을 작성한다. RESEARCH "Pitfall Callouts for Ch.3" 5개를 blockquote(`>`) admonition으로 옮긴다 (인라인 코드는 백틱; 명령 코드블록을 쓸 경우 `# 검증:` 주석 첨부):

   - 주의 1 — Claude Pro/Max 구독 ≠ API 접근 (A-6, CRITICAL): claude.ai의 Pro/Max 구독은 서드파티 API 접근 불허. Hermes에서 Claude 사용하려면 console.anthropic.com에서 별도 API 키(sk-ant-...) 발급 필요. 대안: OpenRouter로 Claude 접근.
   - 주의 2 — hermes setup Silent API Key Skip (A-5, CRITICAL): ~/.hermes/.env에 해당 공급자 키 항목이 이미 있으면(값이 틀려도) setup의 키 입력 프롬프트가 조용히 건너뛰어짐 (GitHub Issue #16394). 녹색 체크마크가 떠도 잘못된 키일 수 있음. 해결: ~/.hermes/.env 직접 편집해 해당 줄 삭제 후 setup 재실행, 또는 `hermes config set PROVIDER_API_KEY NEW_KEY`로 덮어쓰기.
   - 주의 3 — API 키를 config.yaml에 저장 금지 (A-4): 키는 반드시 ~/.hermes/.env. config.yaml에 직접 넣으면 git 추적 시 노출 위험. hermes model 마법사나 hermes auth add 사용 시 자동으로 .env 저장.
   - 주의 4 — 64K 미만 모델 (A-8): 컨텍스트 < 64,000 토큰 모델은 시작 시 거부됨. 무료/소형 모델 선택 시 공급자 사양에서 context window 확인. 고정 오버헤드(~13,935 토큰)가 매 호출 소비.
   - 주의 5 — /model과 hermes model 차이 (A-16): 세션 내 /model은 이미 설정된 공급자 간 전환만. 새 공급자 추가는 세션 종료 후 터미널에서 hermes model 실행.

   그 다음 "## 다음 단계" — Phase 3+로의 안내: `이제 Hermes를 설치·실행·연결했습니다. 다음 단계에서는 컨텍스트·메모리·도구·게이트웨이·배포를 다룹니다.` 이전 `[Ch.2 첫 실행](../02-first-run/index.md)` 링크.

   마지막으로 프로젝트 루트에서 `mdbook build` 실행, exit 0 및 무오류 확인 (이 Task에 빌드 검증 포함).
  </action>
  <verify>`grep -q "#16394" && grep -q "sk-ant" && grep -q "config.yaml" && grep -q "/model" src/03-model/index.md` 통과 (5개 주의 핵심어). `mdbook build` exit 0; stderr "error" 없음; book/에 03-model 페이지 존재.</verify>
  <done>5개 주의 callout + 다음 단계가 작성되고 mdbook build가 오류 없이 완료된다.</done>
</task>

</tasks>

<verification>
- src/03-model/index.md 존재, hermes model / setup --portal 명령 포함.
- 핵심 공급자 표(인증 방식 포함) + API 키 저장 위치(.env/config.yaml/auth.json).
- config.yaml 모델 섹션 + 64K 요건(13,935 오버헤드) + display.language ko 부분 지원.
- 5개 주의 callout(Claude Pro/Max, #16394, .env, 64K, /model vs hermes model).
- '한국어 미지원'이라 쓰지 않고 정확히 '부분 한국어 지원'으로 기술.
- 모든 명령/config 코드블록 `# 검증:` 주석.
- `mdbook build` exit 0.
</verification>

<success_criteria>
독자가 Ch.3을 따라 hermes model로 공급자·API 키를 설정하고 64K 요건을 충족하는 모델을 선택할 수 있다. 한국어 지원이 정확히 설명되고, 발명된 명령/출력이 없다. (Phase 성공 기준 #4 충족)
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundations/02-04-SUMMARY.md`
</output>
