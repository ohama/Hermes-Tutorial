---
phase: 02-foundations
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/02-first-run/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.2 첫 실행 챕터가 mdBook에서 렌더링된다 (02-first-run/index.md)"
    - "Ch.2가 hermes 실행(기본 CLI) 및 hermes --tui로 첫 대화를 시작하는 절차를 설명한다"
    - "Ch.2가 첫 대화 성공 기준(배너에 모델 표시, 오류 없이 응답, 멀티턴)을 제시한다"
    - "Ch.2가 주요 CLI 명령어 일람표(hermes --help 포함, 'hermes help' 단독 명령 금지)와 슬래시 커맨드·키바인딩을 제공한다"
    - "Ch.2가 배너/출력을 검증-필요 placeholder로 표기하고 발명하지 않는다"
    - "모든 명령 코드블록이 # 검증: 주석으로 시작한다"
  artifacts:
    - path: "src/02-first-run/index.md"
      provides: "Ch.2 첫 실행 본문 (실행 + 대화 + 명령어 일람)"
      contains: "hermes --tui"
  key_links:
    - from: "src/02-first-run/index.md"
      to: "src/01-install/index.md, src/03-model/index.md"
      via: "이전/다음 챕터 링크"
      pattern: "0[13]-.*index.md"
---

<objective>
Ch.2 "첫 실행" 본문을 작성한다: hermes 실행(CLI/TUI), 첫 대화 완료, 성공 기준, 주요 CLI 명령어·슬래시 커맨드·키바인딩 일람, 그리고 출력 placeholder 처리.

Purpose: 독자가 Hermes를 실제로 실행해 첫 대화를 마치고 CLI 표면을 탐색할 수 있게 한다.
Output: src/02-first-run/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundations/02-RESEARCH.md

이 플랜은 SUMMARY.md를 건드리지 않는다 (02-01이 02-first-run/index.md 링크를 이미 등록). 오직 src/02-first-run/index.md만 생성한다. 디렉터리 없으면 생성.
RESEARCH.md "Ch.2: 첫 실행" 섹션이 모든 명령·표·출력 가이드의 출처다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.2 본문 — 실행 + 첫 대화 + 배너 placeholder + 성공 기준</name>
  <files>src/02-first-run/index.md</files>
  <action>
src/02-first-run/index.md 새로 작성 (디렉터리 없으면 mkdir). 한국어. RESEARCH "Chapter file template" 구조.

1. 제목 `# 첫 실행` + 시작 전 callout (선행: Ch.1 설치 완료 + which hermes 인식됨; 모델 미설정 시 Ch.3 먼저 필요할 수 있음).

2. "## hermes 실행하기" — RESEARCH "What hermes Launches":
   - `hermes` (플래그 없음) → 클래식 CLI 모드 (prompt_toolkit 기반 TUI, 웹 UI 아님).
   - `hermes --tui` → 모던 TUI (모달 오버레이, 마우스 선택, 논블로킹 입력). 공식 문서는 신규 사용자에게 TUI 권장. 두 인터페이스는 세션·슬래시 커맨드·기능 공유.
   코드블록:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes
   ```
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes --tui
   ```

3. "## 환영 배너" — RESEARCH "Welcome Banner Contents": 배너는 현재 모델명·작업 디렉터리·사용 가능 도구·설치된 스킬 표시. 하단 상태표시줄: 모델, 토큰 사용량(현재/최대), 컨텍스트 채움 %(색상), 예상 비용, 압축 횟수, 백그라운드 작업, 경과 시간.
   배너는 모델·스킬에 따라 다르므로 **정확한 텍스트를 발명하지 말 것**. RESEARCH의 schematic placeholder 블록을 사용:
   ```text
   # [로컬 실행 후 캡처 필요 — 실제 배너는 모델과 설치 환경에 따라 다름]
   ╔══════════════════════════════════════╗
   ║  Hermes Agent  │  <모델명>           ║
   ║  📁 ~/         │  tools: N  skills: M ║
   ╚══════════════════════════════════════╝

   [상태표시줄: model | N/M tokens | X% | $0.00 | 0 bg | 0s]

   > (입력 프롬프트)
   ```
   그리고 `> 검증 필요: 실제 배너는 로컬 실행 후 스크린샷으로 교체해야 합니다.` callout.

4. "## 첫 대화" — RESEARCH "First Conversation Steps":
   1) hermes 또는 hermes --tui 실행 → 2) 배너 대기 → 3) 간단한 테스트 메시지 입력 후 Enter. 예시 메시지(코드블록 아닌 인용 또는 text 블록):
   - 한국어: `이 레포의 주요 파일을 5줄로 요약해줘.`
   - 영어(어떤 모델에서도 안전): `What files are in my current directory?`
   4) 관찰: 에이전트 응답, 필요 시 도구(터미널 ls, 파일 읽기 등) 활성화 → 5) 멀티턴 동작 확인.
   에이전트 실제 응답 출력은 발명 금지 — `[로컬 실행 후 캡처 필요]` placeholder로.

5. "## 첫 대화 성공 기준" — RESEARCH 공식 quickstart 인용: 배너에 선택한 모델/공급자 표시 / Hermes가 오류 없이 응답 / 필요 시 도구 사용 / 한 턴 이상 정상 대화 지속.

6. "## 세션 재개" — RESEARCH "Session Resume":
   ```bash
   # 검증: hermes rolling, 2026-06-09
   # 가장 최근 세션 재개
   hermes --continue
   ```

모든 명령 코드블록은 `# 검증:` 주석으로 시작. 배너·응답 등 환경 의존 출력은 placeholder + 검증-필요 callout, 발명 금지.
  </action>
  <verify>`grep -q "hermes --tui" src/02-first-run/index.md && grep -q "hermes --continue" src/02-first-run/index.md && grep -q "검증 필요" src/02-first-run/index.md && grep -q "성공 기준" src/02-first-run/index.md` 통과. 배너 블록이 placeholder 형식인지 육안 확인.</verify>
  <done>실행·첫 대화·성공 기준·세션 재개가 작성되고 환경 의존 출력은 placeholder 처리됨.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.2 본문 — CLI 명령어 일람 + 슬래시 커맨드 + 키바인딩 + 주의 + 다음 단계</name>
  <files>src/02-first-run/index.md</files>
  <action>
Task 1 파일에 이어서 작성.

1. "## 주요 CLI 명령어" — RESEARCH "Top-Level Commands" 표를 그대로 옮긴다 (hermes / --tui / --continue / chat -q / model / setup / setup --portal / tools / tools --summary / gateway / doctor / doctor --fix / version / config show / sessions list).
   **중요:** `hermes help`라는 단독 명령은 존재하지 않는다. 전역 플래그 `hermes --help`(또는 `-h`)만 있다. 표에 `hermes help`를 넣지 말 것. 별도 줄로 `hermes --help`를 명시:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes --help
   ```
   그리고 `> 참고: 'hermes help' 단독 명령은 없습니다. 도움말은 전역 플래그 hermes --help (또는 -h)를 사용합니다.` callout.
   (ROADMAP 성공 기준 #3이 'hermes help 등'을 언급하나, 실제로는 hermes --help가 정확한 형태이므로 이를 명확히 정정해 기술한다.)

2. "## 세션 내 슬래시 커맨드" — RESEARCH "Key In-Session Slash Commands" 표 (/help, /model, /tools, /skills browse, /voice on, /reasoning high, /title, /status, /compress, /rollback). 대소문자 무시(/HELP = /help) 명시.

3. "## 키바인딩" — RESEARCH "Keybindings" 표 (Enter 전송 / Alt+Enter·Ctrl+J·Shift+Enter 줄바꿈 / Ctrl+G 외부 에디터 / Ctrl+C 중단(연속 두 번 강제 종료) / Ctrl+D 종료).

4. "## 흔한 오류 / 주의" — RESEARCH "Pitfall Callouts for Ch.2":
   - 주의 — 공급자 미설정 시 첫 실행 실패: hermes 실행 시 모델/공급자 미설정이면 "No provider configured" 류 오류 가능 → Ctrl+D로 종료 후 Ch.3 모델 설정 먼저 완료. (정확한 오류 문자열은 미확정이므로 "류"로 표현하거나 RESEARCH의 표현을 따르고 단정적으로 발명하지 말 것.)

5. "## 다음 단계" — `다음 챕터: [Ch.3 모델 설정](../03-model/index.md)`, 이전 `[Ch.1 설치](../01-install/index.md)` 선택적.

제약: 표의 모든 명령은 RESEARCH에 검증된 것만. `hermes help` 발명 금지. 출력 발명 금지.
  </action>
  <verify>positive: `grep -q "hermes --help" src/02-first-run/index.md && grep -q "/help" src/02-first-run/index.md && grep -q "Ctrl+D" src/02-first-run/index.md && grep -q "No provider" src/02-first-run/index.md` 모두 통과. portable negative (단독 `hermes help` 부재 — 기본 셸에서 그대로 실행 가능): `grep -vn -- "--help" src/02-first-run/index.md | grep -q "hermes help" && echo FAIL || echo OK` 가 `OK`를 출력. 슬래시 커맨드·키바인딩 표 존재 육안 확인.</verify>
  <done>CLI 명령어 일람(hermes --help 정정 포함) + 슬래시 커맨드 + 키바인딩 + 공급자 미설정 주의 + 다음 단계가 작성됨.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 검증</name>
  <files></files>
  <action>프로젝트 루트에서 `mdbook build` 실행, exit 0 및 무오류 확인. 02-first-run/index.md 렌더링 확인.</action>
  <verify>`mdbook build` exit 0; stderr "error" 없음; book/에 02-first-run 페이지 존재.</verify>
  <done>mdbook build 오류 없이 완료, Ch.2 렌더링.</done>
</task>

</tasks>

<verification>
- src/02-first-run/index.md 존재, hermes/hermes --tui/hermes --continue 명령 포함.
- 첫 대화 절차 + 성공 기준 제시.
- CLI 명령어 일람표(hermes --help 포함, hermes help 단독 명령 없음 정정) + 슬래시 커맨드 + 키바인딩 표.
- 배너/응답 등 환경 의존 출력은 placeholder + 검증-필요 callout.
- 공급자 미설정 주의 callout.
- 모든 명령 코드블록 `# 검증:` 주석.
- `mdbook build` exit 0.
</verification>

<success_criteria>
독자가 Ch.2를 따라 hermes를 실행해 첫 대화를 완료하고 주요 CLI 명령어(hermes --help 등)와 슬래시 커맨드를 확인할 수 있다. 존재하지 않는 명령이나 발명된 출력이 없다. (Phase 성공 기준 #3 충족)
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundations/02-03-SUMMARY.md`
</output>
