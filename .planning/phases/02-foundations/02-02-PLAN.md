---
phase: 02-foundations
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/01-install/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.1 설치 챕터가 mdBook에서 렌더링된다 (01-install/index.md)"
    - "Ch.1이 macOS/Linux/WSL2 (curl|bash)와 Windows (PowerShell iex/irm) 설치 명령을 제공한다"
    - "Ch.1이 PATH 재로드 단계(source ~/.bashrc / ~/.zshrc, which hermes)를 설치 직후에 명시한다"
    - "Ch.1이 hermes doctor 검증 단계를 포함하되 출력은 검증-필요 placeholder로 표기한다"
    - "Ch.1이 PATH 미반영·sudo 금지·Windows 네이티브 제한 주의 callout을 포함한다"
    - "모든 명령 코드블록이 # 검증: 주석으로 시작한다"
  artifacts:
    - path: "src/01-install/index.md"
      provides: "Ch.1 설치 본문 (OS별 명령 + PATH + doctor + 주의)"
      contains: "install.sh"
  key_links:
    - from: "src/01-install/index.md"
      to: "src/00-intro/index.md, src/02-first-run/index.md"
      via: "이전/다음 챕터 링크"
      pattern: "0[02]-.*index.md"
---

<objective>
Ch.1 "설치" 본문을 작성한다: OS별 설치 명령, PATH 재로드(첫 실행 #1 실패 원인), hermes doctor 검증, 설치 위치, 그리고 PITFALLS 기반 주의 callout.

Purpose: 독자가 자신의 플랫폼에 Hermes를 설치하고 PATH를 반영한 뒤 `hermes doctor`로 설치를 검증할 수 있게 한다.
Output: src/01-install/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundations/02-RESEARCH.md

이 플랜은 SUMMARY.md를 건드리지 않는다 (02-01이 이미 01-install/index.md 링크를 등록함). 오직 src/01-install/index.md만 생성한다. 디렉터리 src/01-install/가 없으면 생성한다.
RESEARCH.md "Ch.1: 설치" 섹션이 모든 명령·표·주의 callout의 출처다 — 그대로 사용하고 발명하지 말 것.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ch.1 본문 — 사전 요구사항 + OS별 설치 + PATH 재로드</name>
  <files>src/01-install/index.md</files>
  <action>
src/01-install/index.md를 새로 작성한다 (디렉터리 없으면 mkdir). 한국어. RESEARCH.md "Chapter file template" 구조를 따른다.

1. 제목 `# 설치` + 시작 전 체크리스트 callout (선행: Git 설치됨 / 인터넷 연결).

2. "## 사전 요구사항" — RESEARCH.md "Prerequisites" 표를 그대로 옮긴다:
   | 요구사항 | 플랫폼 | 비고 | (Git=유일한 수동 요구, PowerShell=Windows 기본 포함, Python 3.11/Node v22/ripgrep/ffmpeg = 자동 설치, 직접 설치 금지)

3. "## 설치 명령 (macOS / Linux / WSL2)" — 코드블록 (HIGH confidence, 그대로):
   ```bash
   # 검증: hermes rolling (설치 시점 최신), 2026-06-09
   curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
   ```

4. "## 설치 명령 (Windows 네이티브 PowerShell)" — 코드블록:
   ```powershell
   # 검증: hermes rolling (설치 시점 최신), 2026-06-09
   iex (irm https://hermes-agent.nousresearch.com/install.ps1)
   ```

5. Hermes Desktop GUI 인스톨러는 별도 callout으로만 언급 (RESEARCH MEDIUM): `> 참고: macOS/Windows용 Hermes Desktop GUI 인스톨러도 있습니다 (hermes-agent.nousresearch.com/desktop). 이 튜토리얼은 개발자 중심으로 CLI 설치(curl/ps1)를 기본 경로로 사용합니다.` Desktop을 기본 경로로 만들지 말 것.

6. Termux는 LOW confidence이므로 명령을 transcribe하지 말 것. RESEARCH "주의 4 — Termux" callout만 포함 (아래 Task 2의 주의 모음에 넣어도 됨): `> 참고 (Android/Termux): 표준 curl|bash 설치가 음성 의존성 오류로 실패할 수 있습니다. Termux 설치는 공식 문서의 별도 안내를 참조하십시오.` (정확한 플래그를 추측해 적지 말 것 — RESEARCH Open Question #5.)

7. "## PATH 재로드 (가장 흔한 첫 실행 실패)" — 설치 명령 바로 다음 섹션. RESEARCH "PATH Reload Step" 그대로:
   ```bash
   # 검증: bash/zsh shell reload, 2026-06-09
   # bash 사용자:
   source ~/.bashrc
   # zsh 사용자 (macOS 기본):
   source ~/.zshrc
   ```
   대안 export 블록:
   ```bash
   # 검증: manual PATH export, 2026-06-09
   export PATH="$HOME/.local/bin:$PATH"
   ```
   이 export는 현재 세션에만 유효하고, 영구 적용은 ~/.bashrc 또는 ~/.zshrc에 추가해야 함을 명시.
   설치 인식 확인:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   which hermes
   ```
   예상 출력(HIGH): `/Users/<username>/.local/bin/hermes` (macOS) 또는 `/home/<username>/.local/bin/hermes` (Linux). 비어 있으면 PATH 미반영.

8. "## 설치 위치" — RESEARCH "Install Locations" 표 (사용자 설치 vs root 설치 경로).

모든 명령 코드블록은 RESEARCH의 정확한 `# 검증:` 주석 첫 줄을 포함해야 한다.
  </action>
  <verify>`grep -q "install.sh" && grep -q "install.ps1" && grep -q "source ~/.zshrc" && grep -q "which hermes"` (src/01-install/index.md 대상) 모두 통과. 모든 ```bash/```powershell 블록 첫 본문 줄이 `# 검증:`로 시작하는지 육안 확인.</verify>
  <done>OS별 설치 명령 + PATH 재로드 + 설치 위치가 정확한 검증 주석과 함께 작성됨.</done>
</task>

<task type="auto">
  <name>Task 2: Ch.1 본문 — hermes doctor/version 검증 + 주의 callout + 다음 단계</name>
  <files>src/01-install/index.md</files>
  <action>
Task 1에서 만든 파일에 이어서 다음 섹션을 추가한다.

1. "## 설치 검증: hermes doctor" — 명령 (MEDIUM, 명령은 확정이나 출력 미확정):
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes doctor
   ```
   설명: "설정과 의존성 문제를 점검하고 수정 방법을 제안합니다." 출력은 LOW confidence이므로 **절대 발명하지 말 것**. placeholder 블록으로 표기:
   ```text
   # [로컬 실행 후 캡처 필요 — 출력은 환경에 따라 다름]
   [hermes doctor 실제 출력 — 로컬 실행 후 캡처 교체 필요 (검증 미완)]
   ```
   그리고 `> 검증 필요: 위 출력은 자리표시자입니다. 실제 hermes doctor 출력은 로컬 실행 후 캡처로 교체해야 합니다.` callout.
   자동 복구:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes doctor --fix
   ```
   설명: 설정/의존성 자동 복구 시도 (PATH 복구 시나리오 등).

2. "## 버전 확인: hermes version" — 명령:
   ```bash
   # 검증: hermes rolling, 2026-06-09
   hermes version
   ```
   출력 형식 힌트(MEDIUM): "출력 예시: `version: 0.8.0 (2026.4.8) [af4abd2f]` (롤링 릴리스이므로 실행 시점에 따라 다름)". 이 예시 버전이 현재 버전이라고 단정하지 말고 "예시"로 명확히 표기.

3. "## 흔한 오류 / 주의" — RESEARCH "Pitfall Callouts for Ch.1"의 callout들을 blockquote(`>`) admonition으로 옮긴다:
   - 주의 1 — PATH 미반영 (A-1, CRITICAL): "hermes: command not found" → source ~/.bashrc(~/.zshrc) 또는 새 탭, which hermes로 확인.
   - 주의 2 — sudo 사용 금지 (A-2, CRITICAL): sudo 설치 시 /usr/local/bin·root 소유 문제. 이미 했다면 `sudo rm /usr/local/bin/hermes` 후 표준 재설치.
   - 주의 3 — Windows 네이티브 제한 (A-3): PowerShell 설치는 CLI/게이트웨이 지원하나 브라우저 대시보드 채팅·일부 POSIX PTY 제한, 전체 기능은 WSL2 권장.
   - 주의 4 — Termux 별도 경로 (A-17, MEDIUM): (Task 1에서 넣지 않았다면 여기) 표준 curl|bash가 음성 의존성으로 실패 가능, 공식 문서 별도 안내 참조.
   (sudo rm 명령처럼 코드블록을 쓰면 `# 검증: hermes rolling, 2026-06-09` 주석을 붙이거나, callout 안에서는 인라인 코드 백틱으로 표기.)

4. "## 다음 단계" — `다음 챕터: [Ch.2 첫 실행](../02-first-run/index.md)`. 이전 챕터 `[Ch.0 소개](../00-intro/index.md)` 링크도 선택적으로.

제약: RESEARCH에 없는 doctor/version 출력·에러 메시지·체크 항목을 사실로 transcribe하지 말 것. 불확실 출력은 placeholder + `> 검증 필요` callout으로만.
  </action>
  <verify>`grep -q "hermes doctor" && grep -q "hermes version" && grep -q "검증 필요" && grep -q "command not found" && grep -q "sudo"` (src/01-install/index.md 대상) 통과. doctor 출력 자리표시자가 발명된 실제 출력이 아닌 placeholder 형식인지 육안 확인.</verify>
  <done>doctor/version 검증 + 4개 주의 callout + 다음 단계 링크가 작성되고, 불확실 출력은 placeholder 처리됨.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 검증</name>
  <files></files>
  <action>프로젝트 루트에서 `mdbook build` 실행, exit 0 및 무오류 확인. 01-install/index.md가 정상 렌더링되는지 확인.</action>
  <verify>`mdbook build` exit 0; stderr에 "error" 없음; book/ 산출물에 01-install 페이지 존재.</verify>
  <done>mdbook build가 오류 없이 완료되고 Ch.1이 렌더링된다.</done>
</task>

</tasks>

<verification>
- src/01-install/index.md 존재, macOS/Linux/WSL2 curl|bash + Windows ps1 명령 포함.
- PATH 재로드(source ~/.bashrc/~/.zshrc, export, which hermes) 섹션이 설치 직후에 있음.
- hermes doctor/version 명령 포함, 불확실 출력은 placeholder + 검증-필요 callout.
- 4개 주의 callout(PATH, sudo, Windows, Termux) 존재.
- 모든 명령 코드블록이 `# 검증:` 주석으로 시작.
- `mdbook build` exit 0.
</verification>

<success_criteria>
독자가 Ch.1을 따라 자신의 OS에 Hermes를 설치하고, PATH를 반영하고, hermes doctor로 검증할 수 있다. 발명된 명령/출력이 없다. (Phase 성공 기준 #2 충족)
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundations/02-02-SUMMARY.md`
</output>
