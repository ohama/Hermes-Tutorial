---
phase: 05-platforms-operations
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/18-security/index.md
autonomous: true

must_haves:
  truths:
    - "Ch.18 보안 하드닝 챕터가 mdBook에서 렌더링된다 (18-security/index.md, 빌드 오류 없음)"
    - "Ch.18이 승인 모드(approvals.mode) + .env 기반 API 키 관리 + 운영 런북을 다룬다 (PLAT-03, 성공기준 #4)"
    - "Ch.18이 approvals.mode 값(manual/smart/off)을 설명하고 프로덕션 권장값을 manual(off 아님)로 명시하며 off 위험을 경고한다"
    - "Ch.18이 비밀은 ~/.hermes/.env, 비밀 외 설정은 config.yaml로 분리하고 chmod 600 + Git 미커밋을 다룬다"
    - "Ch.18이 hermes doctor를 공급망(Python 패키지 CVE) 어드바이저리 검사기로 정확히 기술하고, 설정 검증은 hermes config check로 분리하며, doctor가 설정을 검증한다고 주장하지 않는다"
    - "Ch.18이 hermes security audit 명령을 어디에도 문서화하지 않는다 (존재하지 않음을 명시하는 정정 callout 포함)"
    - "Ch.18이 PII 리댁션을 일반 기능으로 약속하지 않고 security.redact_secrets(MCP 오류의 자격증명 패턴 난독화)로 정확히 범위를 한정한다"
    - "Ch.18이 GitHub Issue #16394 silent API-key-skip 주의를 포함한다"
    - "Ch.18의 모든 키/토큰 예시는 placeholder이며 실제처럼 보이는 값을 쓰지 않고, .env 전용 저장·Git 미커밋 callout이 포함된다"
    - "Ch.18의 Phase 6 레퍼런스 부록에 대한 전방 참조는 plain prose이며 존재하지 않는 06-* 파일로의 mdBook 링크를 만들지 않는다"
    - "모든 명령/설정 코드블록이 '# 검증: hermes rolling, 2026-06-11'(YAML/bash) 주석으로 시작한다"
    - "이 플랜은 src/SUMMARY.md를 수정하지 않는다"
  artifacts:
    - path: "src/18-security/index.md"
      provides: "Ch.18 보안 본문 (approvals.mode + .env 키 관리 + Tirith + 운영 런북(doctor/config check/status) + 게이트웨이 인증 + 10단계 체크리스트)"
      contains: "approvals"
      min_lines: 150
  key_links:
    - from: "src/18-security/index.md"
      to: "src/17-deploy/index.md"
      via: "이전 챕터 인라인 링크"
      pattern: "17-deploy/index.md"
---

<objective>
Ch.18 "보안 하드닝" 본문을 작성한다 — 마지막 콘텐츠 챕터. 승인 모드, .env 기반 API 키 관리, 운영 런북을 정확한 사실로 다룬다.

Purpose: 독자가 approvals.mode를 프로덕션에 맞게 설정하고, API 키를 .env로 안전하게 관리하며, hermes doctor(공급망)·hermes config check(설정 검증)·hermes status(런타임)로 운영 상태를 진단하는 런북을 적용할 수 있게 한다 (성공 기준 #4, PLAT-03). 연구가 정정한 세 가지 사실(doctor 범위, security audit 부재, PII 범위)을 정확히 반영한다.
Output: src/18-security/index.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-platforms-operations/05-RESEARCH.md
@.planning/phases/05-platforms-operations/05-01-SUMMARY.md

기존 파일 (참고용, 건드리지 말 것):
- src/12-cron/index.md — Phase 4 스타일/주석 관례/챕터 구조 참고.
- src/14-gateways/index.md — 선행(게이트웨이 인증·DM 페어링). 인라인 링크 참조 가능.

스타일 기준:
- 한국어. 구조: 개요 → 승인 모드 → API 키 관리 → Tirith 스캐닝 → 운영 런북(doctor/config check/status) → 게이트웨이 인증 + 10단계 체크리스트 → 흔한 오류/주의 → 마무리.
- MANDATORY 주석 관례: 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`(bash/YAML). 체크리스트는 `# 검증: 공식 보안 문서 기반, 2026-06-11`.
- 모든 키/토큰 예시는 placeholder 값.

**중요 정정 사실 (연구 RESOLVED — 반드시 정확히 반영):**
1. `hermes doctor`는 **공급망 어드바이저리(Python 패키지 CVE) 검사기**이다. 설정 검증이 아니다. 설정 검증은 `hermes config check`. doctor가 설정을 검증한다고 절대 쓰지 말 것.
2. `hermes security audit` 명령은 **존재하지 않는다**. 어디에도 문서화하지 말 것. 존재하지 않음을 알리는 정정 callout을 둔다.
3. PII 리댁션은 **일반 기능이 아니다**. `security.redact_secrets`(MCP 오류 메시지의 자격증명 패턴 난독화)로만 범위 한정. 일반 PII 필터로 약속하지 말 것.

**중요:** 이 플랜은 src/SUMMARY.md를 절대 수정하지 않는다 (05-01이 단독 소유).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 승인 모드 + API 키 관리 + Tirith 작성</name>
  <files>src/18-security/index.md</files>
  <action>
src/18-security/index.md를 새로 작성한다(placeholder 덮어씀). RESEARCH.md "Ch.18: 보안 하드닝"의 Approvals/API Key/Tirith 섹션 사용. 이 Task에서 상단부터 Tirith까지 작성하고, Task 2에서 운영 런북/게이트웨이 인증/체크리스트/함정/마무리를 이어 붙인다.

1. 제목: `# 보안 하드닝`
   상단 "이 챕터를 시작하기 전에" callout: 선행 [Ch.14 메시징 게이트웨이](../14-gateways/index.md)(게이트웨이 인증·DM 페어링), [Ch.17 프로덕션 배포](../17-deploy/index.md)(컨테이너 격리). 이 챕터가 프로덕션 운영의 마지막 단계임을 한 줄로 잇는다.

2. "## 개요" — 프로덕션 봇은 실제 봇 토큰·API 키를 다루므로 (1) 위험 명령 승인, (2) 비밀 관리, (3) 운영 진단 런북 세 축으로 하드닝한다.

3. "## 승인 모드 (approvals)" — RESEARCH YAML 코드블록(첫 줄 `# 검증: hermes rolling, 2026-06-11`): `approvals.mode: manual`, `timeout: 300`, `cron_mode: manual`. 모드 표(manual=프로덕션 기본·권장 / smart=보조 LLM 위험도 평가 / off=모든 승인 비활성화=`--yolo`). Hardline blocklist(`rm -rf /`, 포크 폭탄, `mkfs.*`는 off에서도 항상 차단). 프로덕션 권장은 **manual 유지**. RESEARCH "프로덕션 권장: manual 유지" callout 그대로:
   ```
   > 경고: approvals.mode: off 는 위험한 명령에 대한 모든 안전 체크를 비활성화합니다.
   > 로컬 백엔드에서는 절대 사용하지 마십시오.
   > Docker/Modal/Daytona 컨테이너가 보안 경계 역할을 할 때만 고려하십시오.
   > 활성화 시 빨간색 배너 "⚠ YOLO mode"가 표시됩니다.
   ```

4. "## API 키 관리 (.env)" — RESEARCH ".env vs config.yaml 분리 원칙" 표(.env=비밀만 / config.yaml=비밀 외 설정). bash 코드블록(첫 줄 `# 검증: ...`): `chmod 600 ~/.hermes/.env`, `hermes config set ANTHROPIC_API_KEY sk-ant-...`(→ .env 자동 라우팅; 단 예시 키는 placeholder 형태 `<YOUR_ANTHROPIC_API_KEY>` 사용), `hermes config set OPENROUTER_API_KEY <YOUR_OPENROUTER_API_KEY>`, `cat ~/.hermes/.env`. Git 노출 방지 코드블록(`.gitignore`에 `.hermes/` 추가). 이 섹션이 .env 전용 저장·Git 미커밋 요구사항을 충족한다.
   - RESEARCH "Silent API Key Skip 버그 (A-5, GitHub Issue #16394)" callout 그대로: .env에 키 항목이 이미 있으면(값이 잘못돼도) hermes setup이 조용히 건너뛰고 녹색 체크마크가 표시될 수 있음. 해결: .env 직접 편집 후 재실행 또는 `hermes config set`로 덮어쓰기.

5. "## Tirith 보안 스캐닝" — RESEARCH YAML 코드블록(첫 줄 `# 검증: ...`): `security.tirith_enabled: true`, `tirith_timeout: 5`, `tirith_fail_open: true`, `redact_secrets: true`. Tirith 감지 패턴(호모그래프 URL 스푸핑, `curl | bash` 파이프-인터프리터, 터미널 주입). SHA-256 검증으로 GitHub 자동 설치.
   - "### redact_secrets의 정확한 범위 (PII 정정)" — RESEARCH "PII 데이터 처리" callout 그대로: Hermes의 "PII 리댁션"은 **일반 개인정보 필터가 아니다**. 실제 기능은 MCP 오류 메시지에서 자격증명 패턴(`ghp_...`, `sk-...`, Bearer 토큰 등)을 난독화하는 것이며 `security.redact_secrets: true`(기본값)로 제어된다. 별도의 PII 리댁션 설정 키는 존재하지 않는다. 일반 PII 필터로 약속하지 말 것.

제약:
- 모든 키 예시는 placeholder(`<YOUR_ANTHROPIC_API_KEY>` 등). `sk-ant-...`/`sk-or-...`를 실제처럼 보이는 완전한 값으로 쓰지 말 것(명령 형태 설명 시 placeholder 사용).
- 모든 명령/설정 코드블록 첫 줄 `# 검증: hermes rolling, 2026-06-11`.
- PII를 일반 필터로 약속 금지. RESEARCH에 없는 키/명령 생성 금지.
  </action>
  <verify>파일 placeholder 덮어씀: `! grep -qx "# placeholder" src/18-security/index.md` 통과.
승인 모드: `grep -q "approvals" src/18-security/index.md && grep -q "manual" src/18-security/index.md && grep -q "YOLO" src/18-security/index.md` 통과.
프로덕션 권장 manual(off 권장 아님): `grep -q "manual 유지" src/18-security/index.md` 통과.
API 키 관리: `grep -q "chmod 600" src/18-security/index.md && grep -q "16394" src/18-security/index.md` 통과.
PII 정정: `grep -q "redact_secrets" src/18-security/index.md && grep -q "일반" src/18-security/index.md` 통과.
placeholder 키: `grep -q "<YOUR_ANTHROPIC_API_KEY>" src/18-security/index.md` 통과.
주석 관례: `grep -q "# 검증: hermes rolling, 2026-06-11" src/18-security/index.md` 통과.</verify>
  <done>승인 모드(manual 권장, off 경고)·API 키 관리(.env/chmod 600/#16394)·Tirith·redact_secrets(PII 범위 정정)가 정확히 작성되고 키 예시는 placeholder다.</done>
</task>

<task type="auto">
  <name>Task 2: 운영 런북 + 게이트웨이 인증 + 체크리스트 + 함정 + 마무리</name>
  <files>src/18-security/index.md</files>
  <action>
Task 1이 작성한 src/18-security/index.md 뒤에 다음을 이어 붙인다. RESEARCH.md의 doctor/운영 런북/게이트웨이 인증/체크리스트 섹션 사용.

6. "## 운영 런북: 진단 명령" — **정정 핵심.** RESEARCH "hermes doctor — 공급망 보안 검사" 그대로:
   - `hermes doctor`는 **공급망 어드바이저리 검사기**(활성 Python venv의 알려진 취약 패키지 CVE 감지, 2–4단계 수정 지침). bash 코드블록(첫 줄 `# 검증: ...`): `hermes doctor`, `hermes doctor --ack <advisory-id>`.
   - **설정 검증은 별도 명령** `hermes config check`(설정 파일 유효성), `hermes config show`. doctor가 설정을 검증한다고 절대 쓰지 말 것.
   - 런타임 상태: `hermes status`(에이전트/인증/플랫폼), `hermes gateway status`, `hermes logs -f --level WARNING`.
   - "### 운영 런북 절차" — RESEARCH 7단계 bash 코드블록(첫 줄 `# 검증: ...`): doctor → config check → status → gateway status → logs --level ERROR → update --check → insights. doctor 출력(어드바이저리 없을 때 형식)은 검증 안 됨 → `[로컬 실행 후 캡처 필요]` placeholder + `> 검증 필요` 한 줄.
   - "### hermes security audit는 존재하지 않음" — RESEARCH 정정 callout 그대로:
     ```
     > 참고: 'hermes security audit' 명령은 존재하지 않습니다 (공식 문서 미존재).
     > 보안 작업은 다음으로 나뉩니다:
     >   hermes doctor       → 공급망 어드바이저리(Python 패키지 CVE)
     >   hermes pairing list → 인증된 사용자 관리
     >   hermes config check → 설정 검증
     >   (Tirith 스캔은 명령 실행 시 자동)
     ```
     이 챕터 어디에서도 `hermes security audit`를 사용 가능한 명령으로 제시하지 말 것.

7. "## 게이트웨이 인증 + 프로덕션 체크리스트" — RESEARCH "Gateway Security Configuration":
   - 인증 우선순위 5단계 목록(ALLOW_ALL_USERS=true 절대 금지 → GATEWAY_ALLOWED_USERS → 플랫폼별 허용목록 → DM 페어링 → 기본 전체 거부).
   - YAML 코드블록(첫 줄 `# 검증: ...`): `unauthorized_dm_behavior: pair`, `security.allow_private_urls: false`, `security.allow_lazy_installs: false`.
   - "### 프로덕션 10단계 체크리스트" — RESEARCH 10단계 그대로(첫 줄 `# 검증: 공식 보안 문서 기반, 2026-06-11`).

8. "## 흔한 오류 / 주의" — 핵심 정정 3개를 짧은 callout으로 재확인:
   - `hermes doctor`는 설정 검증 도구가 아니라 공급망 CVE 검사기(설정 검증은 `hermes config check`).
   - `hermes security audit`는 존재하지 않음.
   - "PII 리댁션"은 일반 PII 필터가 아니라 MCP 오류의 자격증명 패턴 난독화(`security.redact_secrets`).
   추가로 `GATEWAY_ALLOW_ALL_USERS=true` 절대 금지를 한 줄 재확인.

9. "## 마무리" — 이 챕터로 튜토리얼 본문(Ch.0–18)이 끝남을 알린다. **전방 참조는 plain prose만 사용:** "다음 단계로, CLI 명령어·슬래시 커맨드·설정 키의 전체 목록은 이후 레퍼런스 부록에서 정리됩니다." 같은 문장으로 Phase 6 레퍼런스를 **plain prose로만** 언급한다. **존재하지 않는 06-* 파일로의 mdBook 링크(`[...](../06-*/...)`)를 절대 만들지 말 것** — 06 레퍼런스 부록은 아직 존재하지 않으므로 링크하면 빌드가 깨진다. 이전 챕터 링크 [Ch.17 프로덕션 배포](../17-deploy/index.md)만 실제 링크로 둔다.

제약:
- doctor를 설정 검증 도구로 쓰지 말 것. security audit를 사용 가능 명령으로 제시하지 말 것. PII를 일반 필터로 약속하지 말 것.
- 모든 키 예시 placeholder. 모든 명령/설정 코드블록 첫 줄 `# 검증: ...`.
- Phase 6 전방 참조는 plain prose만; 06-* mdBook 링크 금지.
  </action>
  <verify>운영 런북: `grep -q "hermes doctor" src/18-security/index.md && grep -q "hermes config check" src/18-security/index.md && grep -q "공급망" src/18-security/index.md && grep -q "hermes status" src/18-security/index.md` 통과.
security audit 정정(사용 명령으로 제시 안 함): `grep -q "존재하지 않" src/18-security/index.md` 통과. 부정 확인 — 명령 호출 형태로 제시 금지(설명/정정 맥락만 허용): 실행 코드블록 라인에 단독 명령으로 등장하지 않음을 사람이 확인(자동: `! grep -qE '^\s*hermes security audit' src/18-security/index.md` 통과).
게이트웨이 인증 + 체크리스트: `grep -q "unauthorized_dm_behavior" src/18-security/index.md && grep -q "GATEWAY_ALLOW_ALL_USERS" src/18-security/index.md` 통과.
Phase 6 전방 참조 plain prose(06-* mdBook 링크 부재): `! grep -qE '\]\(\.\.?/06-' src/18-security/index.md` 통과.
이전 챕터 링크: `grep -q "17-deploy/index.md" src/18-security/index.md` 통과.
LOW placeholder + 헤지: `grep -q "로컬 실행 후 캡처 필요" src/18-security/index.md && grep -q "검증 필요" src/18-security/index.md` 통과.
SUMMARY 미수정: `git diff --name-only src/SUMMARY.md` 출력이 비어 있음.
빌드: `mdbook build` exit 0, 출력에 "error" 없음.</verify>
  <done>운영 런북(doctor=공급망/config check=설정 검증/status)·게이트웨이 인증·10단계 체크리스트·3개 정정 함정·plain prose Phase 6 전방 참조가 추가되고, security audit는 사용 명령으로 제시되지 않으며, 06-* mdBook 링크가 없고, SUMMARY.md는 수정되지 않으며, mdbook build가 exit 0이다.</done>
</task>

</tasks>

<verification>
- src/18-security/index.md가 approvals.mode(manual 권장, off 경고)·.env 키 관리(chmod 600, #16394)·Tirith·운영 런북·게이트웨이 인증·10단계 체크리스트를 담는다.
- hermes doctor는 공급망 CVE 검사기로, 설정 검증은 hermes config check로 정확히 분리된다 (doctor가 설정을 검증한다고 주장하지 않음).
- hermes security audit는 사용 가능 명령으로 제시되지 않고, 존재하지 않음을 정정한다.
- PII는 security.redact_secrets(MCP 오류 자격증명 난독화)로만 범위 한정, 일반 필터로 약속하지 않는다.
- 모든 키는 placeholder. Phase 6 전방 참조는 plain prose이며 06-* mdBook 링크가 없다.
- 이 플랜은 src/SUMMARY.md를 수정하지 않는다 (`git diff --name-only src/SUMMARY.md` 비어 있음).
- `mdbook build` exit 0, 오류 출력 없음.
</verification>

<success_criteria>
독자가 Ch.18을 읽고 approvals.mode를 프로덕션(manual)에 맞게 설정하고, API 키를 .env로 안전히 관리하며, hermes doctor(공급망)·hermes config check(설정)·hermes status(런타임) 운영 런북을 적용할 수 있다 (성공 기준 #4, PLAT-03). 정정된 세 사실(doctor 범위, security audit 부재, PII 범위)이 정확히 반영된다.
</success_criteria>

<output>
After completion, create `.planning/phases/05-platforms-operations/05-04-SUMMARY.md`
</output>
